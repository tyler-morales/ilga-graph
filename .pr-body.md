## Summary

Adds a dedicated home page at `/` with issue-focused hero, "What's happening," mission statement, CTA, and share section. Advocacy stays at `/advocacy` with its own centered hero. Content pages `/the-issue` and `/legislator-brief`; home form redirects to `/advocacy/?zip=`.

## Highlights

- **Home at GET /** — `routers/home.py`, `templates/home.html`; hero in `_hero_home.html` (split layout, learn links to /the-issue and /legislator-brief).
- **Advocacy hero** — Separate `_hero_advocacy.html` (centered); headline "Find your legislators." / "Take action on Kei vehicle registration."; magnifying-lens effect on desktop.
- **Home sections** — What's happening (What/Why/Fix), mission statement (serif block), mission CTA, share (copy link + optional native Share).
- **Content router** — `/the-issue`, `/legislator-brief`; sitemap includes them.
- **Typography** — Home page uses sans-serif everywhere except the mission statement block (serif).
- **Pre-merge** — Lint clean (E501 + format); tests 339 pass.

## Testing

`make test` (339 passed), `make lint` (ruff check + format). Manual: GET / (home), GET /advocacy/, GET /the-issue, GET /legislator-brief.
