# TODOS

**State of the system**

- **Guided advocacy funnel:** Intro card (first carousel slide) uses a **decision tree** for outreach preference: "Are you okay sending one-click emails?" → "Can you make a 2-minute scripted call?" → "What about a 30-second script?" (elevator scaffold). Pref values: no (email only), yes (call and email), call_only, elevator (scaffold; same as call_only for now). Call preference saved as cookie (anon) and `User.call_pref` (logged in); on signup/login cookie is merged into user; `adv_call_pref` cookie + user record; members filtered by pref (email-only → has_email; call_only/elevator → has_phone; yes → has_phone or has_email); set_call_pref returns HX-Trigger: refreshResults; next member locked until current complete; refreshAdvocacyCarouselLocks in initMemberCarousel and after setOutreachDone. **Goal state on load/refresh:** Server-rendered `_ilgaUserEmail` when user is logged in so goal bar and truck reflect DB state; anonymous progress from localStorage applied via initGoalBlockFromStorage (after results load and after HTMX swap); refreshAdvocacyResults and htmx:afterSwap both call initGoalBlockFromStorage + refreshAdvocacyCarouselLocks so truck, bar, "Now" link, and carousel locks stay in sync.
- Modularity roadmap 1–5 done: ETL in `etl.py`, `ILGA_LOAD_ONLY=1`, scorecards/Moneyball cached, GraphQL batch loaders, `ILGA_PROFILE=dev|prod`
- Home at `/` (dedicated home hero, stacked links to /the-issue and /legislator-brief); Advocacy at `/advocacy` (dedicated centered hero); content pages /the-issue, /legislator-brief; Moneyball v2 (shell bill filter, institutional bonus, chair-first Power Broker)
- GraphQL `search` query (members, bills, committees); Committee Power Dashboard; Institutional Power Badges; Power Card redesign
- True Influence Engine (`influence.py`); prediction table v5; ML pipeline bug audit; Intelligence story redesign; Legislative Power Map at `/explore`
- **Future:** Value recap (Founding Sales Ch 9): for signed-in users with account/summary view, show "You've contacted N office(s)" and bill status. See docs/reference/founding-sales-chapters.md §9.

---

## Refactor (current & backlog)

| Date       | Area                    | Summary |
|------------|-------------------------|--------|
| 2026-03-03 | Advocacy: prefer-at-least-one contact method | **Edge case:** When user selects "No" through the full preference tree (no email, no call, no elevator), we no longer persist that choice. Backend returns the tree again with an alert: "Please select at least one method to contact your legislator (for example, one-click email or a scripted call)." Pref tree extracted to _advocacy_intro_pref_tree.html; _advocacy_intro_pref_choose_one.html shows alert + tree; set_call_pref rejects pref=no and renders choose-one template (no cookie/DB). advocacy.py, _advocacy_intro_card.html (include tree partial), advocacy-form.css .intro-card__pref-error. |
| 2026-03-03 | Advocacy: goals section sticky on scroll | **UX:** Goals section (`.outreach-goal--horizontal`) is `position: sticky; top: 0` so when navigating from home to advocacy and scrolling, the goal bar stays visible and doesn’t scroll away. Solid background + light shadow so carousel content doesn’t show through. advocacy-cards.css. |
| 2026-03-03 | Advocacy: preference → goals → member card state sync | **Fix (5 bugs):** (1) **setOutreachDone** — completion now uses carousel class `pref-call-only` / `pref-email-only` instead of inferring from button presence, so call_only/elevator users’ slides mark complete after a call. (2) **updateGoalBlockFromStorage** — district complete uses `done >= visibleTotal` instead of hardcoded `done === 4`, so call_only/elevator (2 visible steps) can transition to broker phase. (3) **pref-call-only** — htmx:afterSwap adds `pref-call-only` to carousel for call-only/elevator; CSS hides `.outreach-email` on member cards so email buttons don’t show. (4) **Dedupe** — removed redundant `refreshAdvocacyCarouselLocks()` from second htmx:afterSwap for #advocacy-intro-pref-section. (5) **Auto email-drawer** — removed 500ms timeout from pref swap; trigger runs inside `refreshAdvocacyResults()` .then() after DOM update, using slide-level `--locked` check instead of non-existent `.outreach-btn--locked`. index.html, advocacy-cards.css. |
| 2026-03-03 | Home → advocacy: scroll to member cards on zip submit | **Fix:** Submitting ZIP from the home page hero now scrolls to the member cards on the advocacy page. Home hero form sets `sessionStorage.__ilga_scroll_to_results` on submit (same as advocacy hero); advocacy page scroll target is `#member-carousel` when present so users land on the cards, not just the top of results. _hero_home.html (submit listener), index.html (scroll to member-carousel). |
| 2026-03-03 | Why-you-care: change-answer + CTA prominence | **Group change with results:** "Need to change your answer?" moved to directly after footer (X of 1000 responses); smaller font (0.8125rem), muted color, tight margin above (footer margin-bottom 0.25rem) so it reads with the results block. **CTA visibility:** Primary CTA in .wyc-branch-content__after-change-cta is solid orange (filled), larger (1.125rem, 0.75rem 1.5rem padding), font-weight 600 so "Start outreach" stands out. _why_you_care_branch.html order: footer → change → share → after-change-cta; why-you-care.css. |
| 2026-03-03 | Poll: remove sign-in-to-save nudge | **Deleted:** "Sign in to save your answer and share your story" banner and inline poll sign-in from anonymous poll results. Turnstile already verifies respondents, so the nudge is redundant. _why_you_care_branch.html: removed entire `kei_poll_initial_anon` block (nudge + poll-signin-wrap). _kei_poll_anonymous_success.html: removed nudge block, poll-signin-wrap, and "Sign in to share your story" line. base.html: removed initPollSignin script. base.css: removed .kei-poll-nudge-block and responsive overrides. |
| 2026-03-03 | Turnstile disabled in dev | **Fix:** When ILGA_PROFILE=dev, Turnstile is off: no widget shown (avoids "Unable to connect" in dev), verification skipped so poll and bug report submit without token. config.TURNSTILE_DISABLED = (PROFILE==dev or keys missing); updates, content, home, advocacy, feedback use it for widget + verify. .env.example note. |
| 2026-03-03 | Admin poll totals: distinct respondents | **Fix:** Admin dashboard "total responses" and polls list response_count now use distinct respondent count (one per person). kei_poll_context.get_distinct_respondent_count(db, poll_id); _get_active_polls_summary uses campaign poll distinct count only; _list_polls_with_counts uses distinct count per poll. Matches public progress bar. |
| 2026-03-03 | Poll: one response per person in progress bar | **Fix:** Progress bar "x of 1000" now counts distinct respondents (one per person who completed all 3 questions), not total PollResponse rows. kei_poll_context._get_kei_status_results uses COUNT(DISTINCT respondent_key) for total_responses; respondent_key = user_id or session_id (id fallback). Chart by_status remains row counts per option. Debug instrumentation in _get_kei_status_results and updates.kei_status_post (log row_total vs distinct_total, rows added per submit) for verification. |
| 2026-03-02 | Timeline: content cells light gray + gold trim, empty white | **UX:** Milestone content cells (label + description, e.g. "Secure sponsor…", "Work with Legislative R…") stay light gray with gold inset outline (`--color-timeline-content-trim` #b8860b); empty cells and open-state empty cells explicitly white. timeline.css: milestone-label-cell + milestone-desc-cell get box-shadow inset gold trim; open-state overrides: content cells keep #e8e8e8, empty-cell → #fff; @supports fallback same. |
| 2026-03-02 | Timeline: content cells light gray, empty cells white | **UX:** Phase/item/month cells with content (phase label, header, bar, phase definition, milestone label/description) use light gray (`--color-bg-timeline-content` #e8e8e8); empty cells and details colspan cell use white. timeline.css: label-cell, header-cell, month-cell, bar-cell base, phase-definition, details-label-cell, milestone-label/desc, details-cell; empty-cell → #fff. |
| 2026-03-02 | Timeline: phase description spills into white area, fixed on scroll | **UX:** Phase definition text (details row) now extends from the Phase column into the white timeline area to the right, and does not slide when the table is scrolled horizontally (label cell is already sticky, so spilled content stays fixed). CSS: `.timeline-waterfall__details-label-cell` overflow visible + z-index; `.timeline-waterfall__phase-definition` width min(55ch, calc(100vw - 14rem - 32px)), background, so text flows into timeline; responsive width at 480px. Template: added `timeline-waterfall__phase-definition-row` to details row for open-state styling. timeline.css, timeline.html. |
| 2026-03-02 | Timeline: click-to-toggle phases only | **UX:** Phases no longer auto-open on load or on scroll. User must click phase label, bar, or month header to open/close a phase. Removed syncOpenPhaseToScroll, getMonthIndexAtViewportCenter, initial open of current phase; month click toggles (close if already open). Copy/aria updated to "click to open or close". timeline.html. |
| 2026-03-02 | Timeline: drawer styling | **UX:** Open phase block in waterfall reads as a drawer: phase group wrapper (tbody), JS toggles `.timeline-waterfall__phase-group--open` on open/close; CSS gives drawer content (phase def + milestone rows) shared light teal tint, 3px teal left border on label column, optional indent; phase row gets same left border so it reads as drawer header. timeline.html, timeline.css. |
| 2026-03-02 | Timeline: "now" line extends with drawers | **UX:** Vertical now line spans from first phase row to bottom of scroll content so it grows when phase drawers open (details/milestone rows) and shrinks when they close. positionNowLine() uses scrollContent.getBoundingClientRect().bottom instead of last phase row; openDetailsForPhase/closeDetails call requestAnimationFrame(positionNowLine) after DOM updates. timeline.html. |
| 2026-03-02 | Timeline: "now" vertical line | **Feature:** Campaign timeline shows a vertical glowing-yellow "now" line at the current date (current month column). Backend: `_timeline_now_month_index(month_keys, today)` returns 0-based column index; passed as `now_month_index` to template. Template: `.timeline-waterfall__scroll-content` wrapper so line scrolls with table; `.timeline-waterfall__now-line` (3px, yellow + box-shadow glow) positioned by JS from month header and phase rows. CSS: timeline.css scroll-content + now-line; JS positionNowLine() on load and resize. timeline.html, timeline.css, content.py. |
| 2026-03-02 | Timeline: grid milestones, no sticky details | **Refactor:** Campaign timeline milestones sit in a grid aligned with month columns (each description in the correct month block); details scroll with the table (waterfall). **Backend:** TIMELINE_PHASES milestones have start_ym/end_ym; _timeline_waterfall_data enriches each milestone with start_col/end_col. **Template:** Phase-def row + one row per milestone (label cell + empty cells + colspan description cell + empty); all detail rows toggle together via data-phase-id. **Removed:** Sticky/sliding details (applyDetailsOffset, setDetailsMonthContext, "For [month]" context); timeline-waterfall__details-inner--sticky and __details-context CSS. timeline.html, timeline.css, content_constants.py, content.py, test_content_glossary.py. |
| 2026-03-02 | Poll: Turnstile-only response count, single source | **Consistency:** A poll response is counted only after Turnstile verification at the end of the last question (POST /updates/kei-status). **Turnstile for all:** Anonymous and logged-in users must pass Turnstile on that submit; no response rows from /personalize-poll or POST /personalize. **Single source:** Public UI and admin list/results both read kei poll count from PollResponse (campaign poll slug); kei_poll_context._get_kei_status_results uses PollResponse; admin already used PollResponse. **Removed:** _dual_write_kei_poll / _dual_write_kei_impact from advocacy.py (personalize flows only set user/cookies); _get_kei_poll_all_responses (dead) from admin.py. updates.py, advocacy.py, kei_poll_context.py, admin.py, tests. |
| 2026-03-02 | Story images: hide missing + purge orphans | **Fix:** Marquee only shows approved CommunityStory items whose image file exists on disk (`_story_image_path_exists` in content.py); blank/broken images no longer appear. **Purge:** Admin stories page has "Purge orphan story images" button; POST /admin/stories/purge-orphan-images (admin + CSRF) deletes files in uploads/stories that are not referenced by any CommunityStory.image_path. Removed debug instrumentation from get_marquee_items and _save_story_image. content.py, stories.py, admin_stories.html, admin-dashboard.css. |
| 2026-03-02 | Why-you-care marquee    | **Content:** Added Mercedes Weikal image (1989 Suzuki Every from Japan) to MARQUEE_IMAGES; removed six dummy picsum.photos placeholders. Caption: snippet from her story (tiny cars since 15, five imports, thrifty/easy to park/smiles, \"If these vehicles remain banned, enthusiasts in Illinois like me will have less choice\"); location Lake County. content_constants.py. |
| 2026-03-02 | Call script: full vs short pitch toggle | **Feature:** Call drawer offers a script-length toggle: "Full script" (~2 min) or "Short pitch" (~30 sec elevator version). Short script is derived from full (opening, staffer line, one short_pitch bubble); content from script_sections.short_pitch in advocacy_helpers (SCRIPT_SHORT_PITCH / SCRIPT_SHORT_PITCH_BROKER). Template: _advocacy_drawer_call.html — toggle above no-answer hint, .drawer-script-full (opening through three points), .drawer-script-short (hidden by default). JS: initDrawerScriptLengthToggle in index.html; constituent checkbox updates #drawer-intro-location-phrase-short. CSS: advocacy-drawer.css .drawer-script-length-toggle, .drawer-script-full[hidden], .drawer-script-short[hidden]. Docs: app-overview.md. |
| 2026-03-02 | Advocacy: outreach preference decision tree | **Feature:** Intro card replaced binary "Prefer to call" / "Email only" with a step-by-step decision tree (email ok? → 2-min call? → 30-sec elevator?). Pref values: no, yes, call_only, elevator (elevator = scaffold, same behavior as call_only). Backend: constants.ADV_CALL_PREF_VALUES; users.call_pref String(16); migration 20260302110000; set_call_pref and auth merge accept all four values; _visible_steps and members_for_carousel filter by pref (call_only/elevator → call steps only, phone-only members). **Deleted/consolidated:** Binary pref card and email-only dialog removed; tree + _advocacy_intro_pref_saved.html with data-pref (email-only, call-and-email, call-only, elevator). _advocacy_intro_card.html (tree steps), index.html (tree click handler, afterSwap for call-only/elevator copy), _results_partial.html (outreach_complete, goal dots for new prefs). Tests: _visible_steps call_only/elevator. |
| 2026-03-02 | Advocacy: preferences card redesign | **Copy:** Intro card title → "How would you like to outreach your legislators?"; visible subcopy "Call: word-for-word script. Email: one-click send." (reassurance, minimal). Saved state: "Script ready. Let's go." / "Got it — email only. One-click send." Email-only dialog shortened to one line: "Calls get noticed; we give you a word-for-word script. Prefer email? One-click send." _advocacy_intro_card.html, _advocacy_intro_pref_saved.html. |
| 2026-03-02 | Poll: Turnstile hint only with widget | **Fix:** "This check helps us count real responses." now lives inside the turnstile wrapper so it only appears when the turnstile is shown (after impact choice). Card and inline poll: outer div `...-turnstile-wrap` has --hidden; container + hint + screen-reader span are inside it; base.html toggles --hidden on the wrap (fallback to container). _macros.html, base.html. |
| 2026-03-02 | Poll: Turnstile only after last choice | **UX:** Turnstile widget now renders only after the user selects their impact choice (step 3). Card and inline poll use a hidden container; base.html initKeiPollBranch calls showAndRenderTurnstile() when kei_impact_radio is selected; Cloudflare script loaded with ?render=explicit; report_bug uses explicit render on load. _macros.html (container + hidden class), base.html (explicit API + render on impact), report_bug.html (explicit render), base.css .kei-poll__turnstile-wrap--hidden. |
| 2026-03-02 | Advocacy: mobile next-step bar removed | **Deleted:** Sticky bottom bar "Next: Call your Senator" no longer shown on mobile; `.mobile-next-bar` hidden via `display: none`. Goal "Now:" link in main content remains. advocacy-form.css. |
| 2026-03-02 | Advocacy: intro pref card full height + slide transition | **UX:** Intro preferences card is full available height (flex on slide + card + pref viewport); pref section styled as a clear panel (padding, light bg, border-radius). On selection: current card slides out left (exiting class + 300ms delay), then HTMX swap; new "saved" content slides in from right (entering → entered). Call path: form hx-trigger delay:300ms, button type="button"; email path: dialog confirm button triggers exiting + htmx.ajax after 300ms. _advocacy_intro_card.html (viewport wrapper, delayed trigger, dialog button id); advocacy-cards.css (intro slide/card/pref flex, viewport, panel + transition); advocacy-form.css (saved gap); index.html (click handler for exiting, afterSwap entering/entered). |
| 2026-03-02 | Advocacy: no-email drawer (call pref, no email) | **Fix:** When user has call preference and a member has no email (and user didn't get one during the call), do not show the full email compose drawer. Return `_advocacy_drawer_no_email.html` from GET /advocacy/drawer?view=email when member has no email, and from POST wrapup when recipient is empty. No-email partial: short message + "Done — next legislator" button; handleNoEmailDone records email as skipped (outcome=skipped_no_email) or updates anon progress and closes drawer so the user keeps moving. advocacy.py, _advocacy_drawer_no_email.html, index.html handleNoEmailDone, advocacy-drawer.css. |
| 2026-03-02 | Advocacy: small-screen full width | **Layout:** On viewports ≤768px, goal card, intro/member cards, and carousel now use full available width. Breakpoint aligned: carousel full-width rules use 768px (was 47.9375rem) so they match results-layout column switch; .results-main and .outreach-goal get width/max-width 100% in tablet block; .member-carousel gets width: 100%. advocacy-cards.css. |
| 2026-03-02 | Advocacy: completed members to back + dimmed | **UX:** When a user has completed all actions for a member (call + email, or email-only), that member moves to the back of the carousel and the card is shown at lower opacity (existing .member-carousel__slide--done 0.4). Server: members_for_carousel sorted by completed (incomplete first). Client: on setOutreachDone when slide becomes complete, appendChild(slide) to carousel so it moves to end without full refresh. advocacy.py, index.html; CSS already had --done opacity. |
| 2026-03-02 | Advocacy: bug fixes (no-email button, intro refresh, broker label, ordering) | **Fixes:** (1) Removed locked "No email — call to get one" button; email button only when member has email; kept "Emailed!" done-state and hint text. (2) set_call_pref now fires HX-Trigger: refreshResults for both pref=yes and pref=no so intro card is removed from carousel immediately. (3) Broker progress bar label/ARIA use visible_broker_done/visible_broker_total. (4) Comment at _build_district_steps documenting goal/carousel Moneyball-order contract. _results_partial.html, advocacy.py, TODOS.md. |
| 2026-03-02 | Advocacy: email-only hides no-email members | **Fix:** When user chooses "Email only" on the intro card, members with no email on file are no longer shown in the carousel. Backend already filtered `members_for_carousel` by `has_email` when `user_call_pref == "no"`; in-session switch did not refresh the list. set_call_pref now returns `HX-Trigger: refreshResults` when pref=no; client listens for `refreshResults` and calls refreshAdvocacyResults() to re-POST /advocacy/search so the carousel is replaced with the filtered list (and intro slide is omitted). advocacy.py set_call_pref; index.html refreshResults listener. |
| 2026-03-02 | Advocacy: up-next cards dimmed, one focus | **UX:** Cards that are "up next" (locked until previous complete or until call/email pref is set) are now visually dimmed (opacity 0.5) and remain non-interactive (pointer-events: none). Single rule in advocacy-cards.css for `.member-carousel__slide--locked .card`; removed redundant pref-required-only opacity. refreshAdvocacyCarouselLocks sets/removes `aria-disabled` on slides so screen readers know locked slides are disabled. |
| 2026-03-02 | Goals + member cards, contact prefs | **Goals + member cards:** Goal steps only include email when member has effective email (public or community); district/broker done/total derived from steps; "Now:" never suggests "Email your Senator" when senator has no email. Optional copy when email-only and 0 district emails: "No district legislators have email on file. Call to get one, or check their website." advocacy.py: _build_district_steps, _visible_steps (module-level); broker_goal_steps conditional email; tests/test_advocacy_goal_steps.py. **Follow-up:** Updates page CTA: align district_steps with email availability if we add effective-email to that context. |
| 2026-03-02 | Advocacy: progress bar UX — truck animation, checkpoint celebration | **Progress bar refinement:** Truck now animates along the bar (CSS transition on `left` 0.4s ease-out) when goals complete so the user sees the truck "drive" to the next checkpoint; respects `prefers-reduced-motion` (no transition when reduced). Checkpoint dots get a short scale micro-celebration (1 → 1.15 → 1 over 0.3s) when marked done, gated on `prefers-reduced-motion: no-preference`. Bar fill and label unchanged. Docs: app-overview.md updated with goal-gradient/perceived-control and truck animation; advocacy-cards.css. |
| 2026-03-02 | Advocacy: progress bar label, email-only goal, checkpoint order | **Progress bar:** Label and ARIA use visible step counts (visible_district_done/visible_district_total) so "X of Y actions" and truck position match the dots; district phase shows only when visible_district_total > 0 (email-only can have 2 steps). **Email-only:** Goal heading "Email 1/2 legislators"; broker phase unlocks when visible district steps complete (2/2 email or 4/4 call+email); data-goal-email-only on section; JS updateGoalBlockFromStorage uses visibleTotal for label/aria and districtComplete = 2/2 when email-only. **Order:** your_legislators and district_steps fixed order Senator then Rep; members_for_carousel sorted by role (Senator, Rep, Broker) so checkpoints and cards align left-to-right. advocacy.py, _results_partial.html, index.html. **If truck still shows 1 after call+email for one member:** verify both call and email record OutreachEvent (outreach router + wrapup/email-record). |
| 2026-03-02 | Advocacy: carousel/checkpoint order by Moneyball then broker | **Order:** District legislators (Senator, Rep) sorted by Moneyball score descending (higher first); Power Broker last. So display order: (1) district legislator with higher Moneyball, (2) other district legislator, (3) Power Broker. your_legislators sorted by moneyball_score; members_for_carousel built from your_legislators then broker (no role-class sort). JS updateGoalBlockFromStorage builds districtSteps from DOM dot order so "Now" link matches visual order. advocacy.py, index.html. |
| 2026-03-02 | Advocacy: goal state on refresh, truck/bar/next in sync | **Fix:** On refresh or after HTMX results load, goal tracker (truck, progress bar, "Now" link) and carousel "up next" now reflect current state. **Logged-in:** Server passes `user` into advocacy index context; template sets `window._ilgaUserEmail` from `user.email` so initGoalBlockFromStorage skips and server-rendered district_goal_done / district_truck_pct / goal_next_step are used. **Anonymous:** initGoalBlockFromStorage runs after refreshAdvocacyResults and after htmx:afterSwap (#results) to patch bar, truck, and "Now" from localStorage; refreshAdvocacyCarouselLocks runs after so mobile next bar and slide locks match. advocacy.py ctx["user"], index.html server-rendered _ilgaUserEmail, refreshAdvocacyResults + htmx:afterSwap call initGoalBlockFromStorage + refreshAdvocacyCarouselLocks. |
| 2026-03-02 | Advocacy: truck over dots, dots centered on bar | **Truck:** Moved truck span from inside bar to bar-track (after bar-dots) so it can sit above dots; z-index: 2 on truck, z-index: 1 on dots; --truck-pct on bar-track; JS sets --truck-pct on barTrack/brokerTrack. **Dots:** Center-aligned on progress bar (top: 5px = bar center, translateY(-50%)). **Style:** All dots same (22px circle, 2px border); completed = green fill + green border, incomplete = white fill + grey border. _results_partial.html, advocacy-cards.css, index.html. |
| 2026-03-02 | Advocacy: goal section refinements | **Goal header:** "This week's goal:" with colon; goal label inline (directly after title). **Dynamic label:** "Contact 1 legislator" / "Contact 2 legislators" from contact preference—email-only = count district members with email on file; phone = 2 (both district). **Truck:** Position on current (last completed) checkpoint via _truck_on_checkpoint_pct(); JS truckOnCheckpointPct(). **Checkpoints:** Bar dots 22px, track min-height 22px + bar-dots top:50% so dots extend outside bar. advocacy.py district_goal_label, district_truck_pct/broker_truck_pct; _results_partial.html title + span; advocacy-cards.css .outreach-goal__bar-dot 22px, __bar-track min-height. |
| 2026-03-02 | Advocacy: progress bar, truck, checkpoints, Now step | **Progress bar sync:** Truck and fill now use checkpoint-aligned positions (0, 33.33, 66.66, 100% for 4 dots via space-between) so the truck lands on checkpoints at all times, including 0 done (truck on first dot). Backend: visible district/broker steps (filter by user_call_pref), _checkpoint_fill_pct(), district_fill_pct/broker_fill_pct, visible_* in context; goal_next_step from first incomplete *visible* step so "Now:" shows user's actual next step (e.g. email-only when call_pref no). Template: bar uses --truck-pct and fill width from district_fill_pct/broker_fill_pct; truck moved out of bar-fill into bar, positioned by CSS left: calc(var(--truck-pct)*1%). CSS: bar position relative, padding 0 6px to align with dots; truck left + translate(-50%, 45%). JS: updateGoalBlockFromStorage uses checkpointPct() and visible dot count, sets fill width and --truck-pct. advocacy.py, _results_partial.html, advocacy-cards.css, index.html. |
| 2026-03-02 | Advocacy: goal bar dots + heading inline | **Goal section:** "This week's goal" and goal value (e.g. Contact your district legislators) on one line (head row flex, label right). Progress bar shows checkpoint circles that fill as user completes each step; numbered list and duplicate Call/Email links removed. "Now:" link unchanged; JS updateGoalBlockFromStorage uses bar dots (data-goal-member, data-goal-kind) for done state and computes first-incomplete from step order + data-goal-senator-label/rep-label for link text. _results_partial.html, advocacy-cards.css (.outreach-goal__head-row, __head-label, __bar-track, __bar-dots, __bar-dot, __bar-dot--done), index.html (setAction/setBrokerAction → dots; Now link built from steps). Removed .outreach-goal__copy, dead list styles. |
| 2026-03-02 | Advocacy: intro → first member slides into view | **UX:** After user chooses call or email, first member card slides into view (smooth scroll to slide index 1 while intro still in DOM), then intro is removed and carousel scroll reset so the card doesn’t “just appear”. index.html htmx:afterSwap: scrollToMemberSlide(1) first, then after 450ms remove intro and set carousel.scrollLeft = 0. |
| 2026-03-02 | Advocacy: remove intro card after pref | **UX:** Once user chooses call or email, intro card is removed from the carousel so only member cards remain. Server: intro slide rendered only when `user_call_pref is none`; progress total uses `#carousel-progress-total` and conditional count. Client: after HTMX set-call-pref swap, remove `.member-carousel__slide--intro` from DOM, update progress total, scroll to first member (index 0). _results_partial.html, index.html. |
| 2026-03-02 | Advocacy: goal in main  | **Goal section moved** from left sidebar to main content above the member carousel. Horizontal layout (`.outreach-goal--horizontal`): progress bar full width, step list as horizontal checkpoints/pills, truck emoji moves with progress. Removed "Your outreach targets" heading and "One target at a time." copy; carousel keeps `aria-labelledby` via visually hidden h2. Sidebar now has Kei poll + My Outreach only. _results_partial.html, advocacy-cards.css, app-overview.md. |
| 2026-03-02 | Why-you-care: minimal copy, poll → outreach | **Copy (Founding Sales Ch2 narrative, minimal read):** Pre-poll: one line — problem ("Unclear law blocks kei registration in Illinois") + why answer matters ("Your answer helps us show legislators who's affected and who supports a fix"). Poll intro: "Your response helps us show legislators who's affected and who supports the fix." Wide-net line: "Every response helps show breadth of support." Branch bodies shortened to one sentence each so CTAs lead (owner: "Registrations denied or revoked, titles branded—your voice helps us show legislators who's affected."; would_want/would_not_want one line). Goal: get users to fill out the poll, then hopefully do outreach. content_constants.py WHY_YOU_CARE_PRE_POLL_LINE, KEI_POLL_WHY_WE_ASK, KEI_POLL_WIDE_NET_LINE, WHY_YOU_CARE_BRANCHES. |
| 2026-03-02 | Advocacy pref: auto-scroll + email drawer | **UX:** After user sets call or email preference (HTMX set-call-pref), auto-scroll first member card into view (no "Let's go" click required). If preference is email-only, after swap also open the drawer with email view for the first member so they see the email script. Exposed scrollToMemberSlide on window from _results_partial; index.html htmx:afterSwap for #advocacy-intro-pref-section runs refreshAdvocacyCarouselLocks, scrollToMemberSlide(1), and when data-pref="email-only" opens drawer with openAdvocacyDrawer('email', ...) for first member. _results_partial.html, index.html. |
| 2026-03-02 | Advocacy pref: unlock member calls after choice | **Fix:** After user sets call/email preference (HTMX set-call-pref), member call buttons stayed locked because hx-swap="innerHTML" leaves the target #advocacy-intro-pref-section with data-call-pref-section; refreshAdvocacyCarouselLocks() uses that to gate. In htmx:afterSwap for that target, remove data-call-pref-section before calling refreshAdvocacyCarouselLocks() so first member slide unlocks. index.html. |
| 2026-03-02 | Modals: shared styles + mobile full-width | **Refactor:** All app modals (email-only-pref, no-email, story/statement submit) share `.app-modal` in base.css: same border, radius, shadow, backdrop. On mobile (max-width 480px) modals use `width: calc(100% - 1rem)` for fuller width. Templates: added `app-modal` to dialog elements; advocacy-form.css: removed duplicate container/backdrop from .no-email-modal and .email-only-pref-dialog (content-only styles kept). |
| 2026-03-02 | Advocacy intro: preference phrasing + layout | **Copy:** Primary action "I'll call" → "Prefer to call" (user sets how they'll interact with member cards, not "call right now"); dialog "I'll call with the script" → "Prefer to call". **Layout:** "Email only" link centered under primary button (.intro-card__alt-line text-align: center). **UI:** Phone icon on primary button (intro-card__pref-btn--phone + SVG). Refs: Hardball Ch7 (direct contact valued); Founding Sales Ch5 — calling framed as preference/support, not immediate commitment. _advocacy_intro_card.html, advocacy-form.css. |
| 2026-03-02 | Advocacy mobile: intro simplify + pref gate | **Intro card:** One line "One call has the most impact." + I'll call button + Email only link; removed goal line and intro_card_why_call. **Pref gate:** Member cards locked (lower opacity + overlay "Choose call or email above first") until user selects call or email; refreshAdvocacyCarouselLocks checks intro for [data-call-pref-section] and adds --locked + --pref-required to all member slides when pref not set; after HTMX set-call-pref swap we refresh locks so first member unlocks. _advocacy_intro_card.html, _results_partial.html (overlay msg--prev/msg--pref), index.html refreshAdvocacyCarouselLocks + htmx:afterSwap, advocacy-cards.css (--pref-required overlay msg + card opacity). |
| 2026-03-02 | Why-you-care: explainer placement | **Layout:** One explainer for "Why should you care?" (WHY_YOU_CARE_PRE_POLL_LINE) is now the section subtitle directly under the heading (outside #why-you-care-flow so it isn’t replaced by HTMX). The other (KEI_POLL_WIDE_NET_LINE) moved from above the poll to below the poll progress bar. _why_you_care.html: .why-you-care-section-intro after H2; .wyc-poll-under-goal after kei_poll_inline. _why_you_care_flow_ambient.html same. CSS: .why-you-care-section-intro (centered, under heading); .wyc-poll-under-goal (under progress bar). Removed .wyc-pre-poll-line, .wyc-poll-wide-net from flow. |
| 2026-03-02 | Advocacy goal visibility and confidence | **Goal in main flow:** Intro card shows one-line goal echo ("Your goal: Contact your district legislators (4 actions).") so users see scope without scrolling to sidebar; .intro-card__goal styled subordinate. **Confidence line:** INTRO_CARD_WHY_CALL in content_constants (canonical from FAQ_ADVOCACY adv3); passed in advocacy results context; rendered in _advocacy_intro_card.html. **Sidebar emphasis:** .outreach-goal gets left border accent (4px #4a6fa5) so goal section reads as primary checklist. _advocacy_intro_card.html, advocacy-cards.css, content_constants.py, content.py, advocacy.py. |
| 2026-03-02 | Why-you-care flow UX (cut fat, tighten branch) | **Cut the fat:** Pre-poll shows one line (WHY_YOU_CARE_PRE_POLL_LINE) instead of three cards; main "why" lives in branch. content_constants.py WHY_YOU_CARE_PRE_POLL_LINE, WHY_YOU_CARE_CTA_NUDGE; _why_you_care.html and _why_you_care_flow_ambient.html use .wyc-pre-poll-line; why-you-care.css .wyc-pre-poll-line. **Tighten branch:** Optional CTA nudge above primary ("Based on your answer — one more step:"); primary CTA has .wyc-branch-content__cta-primary (slightly larger); _why_you_care_branch.html nudge + updates.py/home context. **Tracking:** wyc_branch_viewed and data-wyc-step CTAs unchanged; use for drop-off analysis. Plan: keep reasons→poll→branch order; instrument branch view + CTA clicks. |
| 2026-03-02 | Poll goal + UX (mom's two issues) | **Poll goal:** KEI_POLL_GOAL_RESPONSES (1000) + optional campaign poll_goal_responses; show "X of 1,000" + progress bar in form/results (home, updates, sidebar); KEI_POLL_WIDE_NET_LINE; get_kei_poll_goal() in campaign_config; kei_poll_goal in all poll context. **Poll error UX:** On 400/403/429 from kei-status when poll_id=home-kei-poll, return full _why_you_care_flow_ambient.html with kei_error_message so form stays and user can retry. **No-email button:** Label "No email — call to get one", aria-label "No email on file for this office — call to get staffer email"; modal title "We don't have an email for this office yet"; modal message explains call to get email then we can add it. **Email drawer:** When recipient_email empty, show prominent gmail-banner "No email on file. Add one from your call or paste below." **Turnstile:** Hint "This check helps us count real responses." below widget. content_constants, campaign_config, kei_poll_context, updates, home, _macros, _why_you_care*, success partials, _results_partial, _advocacy_drawer_email, why-you-care.css, base.css. |
| 2026-03-02 | Call preference persistence | **Feature:** "I'll call" / "Email only" on advocacy intro is saved as user preference for both anonymous and logged-in users. Anonymous: `adv_call_pref` cookie only. Logged-in: POST /advocacy/set-call-pref writes to `User.call_pref` and sets cookie. Context resolves `user_call_pref` from user record when present, else cookie. On signup/login (verify-code): cookie value is merged into `User.call_pref` if user has none; response sets cookie from user so UI stays in sync. GET /auth/me returns `call_pref`. Migration 20260302100000 adds `users.call_pref`. constants.ADV_CALL_PREF_*; advocacy.py set_call_pref(user, db), _build_search_results_context user-first; auth.py verify_code merge + cookie. |
| 2026-03-02 | Advocacy intro card: 3–4 points, one action | **UI:** Card reduced to 3 reading points + one explicit action: (1) headline "We'll show you who to call and a short script.", (2) "One call has the most impact.", (3) primary button "I'll call". Removed "Getting started" label and "Are you comfortable…" question. De-emphasized line: "Email only" only (.intro-card__alt-line). **Deleted:** "Skip for now →" link and .intro-card__skip-btn/.intro-card__alt-sep (skip is via swipe). _advocacy_intro_card.html, advocacy-form.css, _results_partial.html click handler. |
| 2026-03-02 | Advocacy intro card simplify | **UI:** Removed top "1. Call / 2. Email" visual block; trimmed copy; "Email only, please" opens modal (why call); single-column header; .email-only-pref-dialog. |
| 2026-03-01 | Advocacy intro card UX  | **UI:** Explain card distinct from member cards: .card-intro--explain (warm bg #fdfbf7, left gold accent, no photo); headline larger (1.35rem), semantic h2; "Email only, please" as plain-text link. When user chooses email-only (HTMX set-call-pref): _advocacy_intro_pref_saved.html has data-pref="email-only"; afterSwap adds .pref-email-only to #member-carousel; advocacy-cards.css hides .outreach-phone on non-intro slides. |
| 2026-03-01 | Guided advocacy funnel | **Feature:** Intro card as first carousel slide (goal explainer, social proof, "Are you comfortable calling?" → Yes / Email only; cookie `adv_call_pref`). Only members with actionable contact (phone and/or email; email-only pref filters to email-only). Lock overlay on subsequent slides until previous complete; refreshAdvocacyCarouselLocks on load and after setOutreachDone. Mobile sticky next-step bar. advocacy.py: user_call_pref, total_advocates, member filtering, POST /set-call-pref. _advocacy_intro_card.html, _advocacy_intro_pref_saved.html; _results_partial intro slide, lock overlay, mobile-next-bar, user_call_pref in member_card and goal steps. index.html: refreshAdvocacyCarouselLocks, setOutreachDone updates slide data-completed + refreshLocks. advocacy-cards.css: --intro, --locked, slide-lock-overlay; advocacy-form.css: intro-card, mobile-next-bar. |
| 2026-03-01 | Advocacy: mobile member cards stacked | **Layout:** Member carousel on mobile (max-width 47.9375rem) is single column, full-width cards, no horizontal scroll. advocacy-cards.css: @media (max-width: 47.9375rem) for .member-carousel (flex-direction: column, overflow visible, scroll-snap none) and .member-carousel__slide (width 100%, flex auto). |
| 2026-03-01 | Advocacy UX Overhaul     | **Poll:** Cloudflare Turnstile in kei_poll/kei_poll_inline; anonymous votes counted after token verify (updates.py); _get_kei_status_results counts KeiPollResponse (all votes). Messaging: "Your vote is saved. Sign in to track your impact." **Advocacy:** Email button locked when no effective email (modal "Call them and ask for staffer's email"); Call → "Call Script", Email → "Email Script". **Carousel:** Single ranked list (email+phone first, phone only, email only, neither; completed last); scroll-snap one-at-a-time carousel; progress "1 of N"; refresh after wrapup/"I sent it!" so completed move to end. advocacy.py: effective email per card, members_for_carousel; _results_partial carousel + no-email dialog; index.html initMemberCarousel, refreshAdvocacyResults. |
| 2026-03-01 | Git: white-label commit + PR #50 update | **Commit:** feat(white-label): config-driven poll, email, 404, mission; poll cleanup; docs (21 files). **Fix:** campaign_config.py E501 comment; test_auth_outreach.test_my_history_* and test_campaigns.test_record_with_valid_campaign_id_* patch rate_limit_verify_code so verify-code returns 200. **PR:** gh pr edit 50 — title and body from main..HEAD + TODOS. |
| 2026-03-01 | Poll results: remove "How it affects you" section | **Deleted:** Impact results block (heading + chart) and the "How it affects you: [label]" status line from poll result views. Templates: _kei_poll_anonymous_success.html, _kei_poll_logged_in_success.html, _why_you_care_branch.html. Status poll results and Total Votes unchanged; account.html still shows "How it affects you" in profile. |
| 2026-03-01 | White-label: config-driven poll, email, 404, bill URLs, mission | **Feature:** campaign.json + CampaignConfig extended with poll_prompt_query, welcome_email_intro, welcome_email_poll_link_text, strategic_mission, mission_attribution, error_page_facts, error_page_fact_label, bill_status_urls. updates.py redirects and prompt_kei use poll_prompt_query; email_utils welcome email uses config copy and poll URL; main.py error context uses error_page_facts/error_page_fact_label; config.get_bill_status_urls uses campaign bill_status_urls first. Templates: _mission_statement (mission_attribution), terms (issue_summary), 404 (error_page_fact_label); content/home pass strategic_mission from campaign. docs/architecture/white-label-decoupling-gaps.md updated with Done/Remaining. |
| 2026-03-01 | White-label decoupling gaps (plan) | **Docs:** docs/architecture/white-label-decoupling-gaps.md — current-state list of what still needs to be driven by campaign.json (content in content_constants, URL/prompt coupling, welcome email, 404 facts, bill URLs, templates, account). Use when doing multi-tenant/white-label work. |
| 2026-03-01 | Hero ticker: show outreach event count | **Feature:** Hero ticker now shows total outreach actions (calls + emails combined) when > 0: "Illinois constituents have made **X** outreach action(s) so far (calls and emails)." Uses existing calls_total from get_outreach_aggregate (total call + email events). Home and advocacy heroes (_hero_home.html, _hero_advocacy.html). "Over N constituents" framing still deferred until 100+ unique people (script/email). |
| 2026-03-01 | Founding Sales alignment | **Refactor:** Narrative audit (brief + the_issue): added "What changed / Why now" to legislator brief fallback and The Issue; narrative audit checklist in docs/reference/founding-sales-chapters.md. Materials: one-pager as single click target in email drawer (Download one-pager PDF + aria-label); materials checklist and deck/script/email mapping in founding-sales-chapters. Prospecting: ICP and "who will be excited" in docs. Cadence: Ch 5 target and current single-touch noted in docs; hero CTA line ("Your call or email matters" / "One call or email helps") on home and advocacy heroes; .hero-action-line in advocacy-form.css. Objections: "Just send me something" and "Call back later" added to call drawer pushbacks; Ch 7 objection table in founding-sales-chapters. Mindset (Ch 1) and value recap (Ch 9): hero line; value recap for signed-in users ("You've contacted N offices; bill status") documented as future enhancement in founding-sales-chapters. |
| 2026-03-01 | Account page layout cleanup | **Refactor:** account.html reorganized into single Profile section (email read-only, ZIP + newsletter in .account-field blocks, Save), Your answers section, and .account-footer (Member since + Privacy). base.css: .account-section glossary-style border/padding, .account-field spacing, .account-footer de-emphasized; account-section styling aligned with glossary-section pattern. |
| 2026-03-01 | Founding Sales reference | **Docs:** docs/reference/founding-sales-chapters.md — chapter breakdown and line ranges for reference/founding-sales.txt; TOC summary; "How we use this" for sales (narrative, materials, prospecting, outreach, pitch, pipeline) and advocacy (narrative, brief/call script/email, targeting, cadence, mindset, follow-up). Rule: cite chapter/line range when using Founding Sales for copy or scripts. |
| 2026-03-01 | Minimal user profile    | **Feature:** GET /account (auth required), POST /account (CSRF, zip + wants_updates); GET /auth/me extended with zip_code, wants_updates, kei_impact_slug, created_at; PATCH /auth/me for zip + wants_updates (validates ZIP with state.zip_to_district). **UI:** account.html (email read-only, ZIP input, newsletter checkbox, Your answers, Member since, privacy link); Account link in footer when logged in; base.css account-flash, account-form, account-dl. **Tests:** test_auth_outreach.py TestPatchMe, extended TestAuthVerifyRoundtrip /me response; test_updates.py TestAccountPage (401 anon, 200 authed, CSRF required, POST saves wants_updates). **Docs:** campaign-decoupling.md User profile paragraph; main.py app.state.templates for account router. |
| 2026-03-01 | Test fix: advocacy router + poll data in test_updates | **Fixture:** client fixture now reloads advocacy_router_mod (with db_mod) so /advocacy/personalize-poll uses test DB; without it the app used the router loaded at import time (wrong DB). **Data:** POLL_SUBMIT_DATA uses kei_impact_slug "other" so it passes both KEI_POLL_IMPACT_SLUGS (/updates/kei-status) and KEI_IMPACT_OPTIONS["would_want"] (/advocacy/personalize-poll). |
| 2026-03-01 | Poll integration: personalize-poll tests + migrations | **Tests:** test_updates.py — advocacy router mounted in _make_test_app; new class TestAdvocacyPersonalizePoll: test_personalize_poll_requires_csrf (403 without token), test_personalize_poll_authenticated_sets_user_and_dual_writes (user + optional kei_impact PollResponse), test_personalize_poll_anonymous_sets_cookies (voted/choice/impact cookies), test_personalize_poll_invalid_impact_does_not_persist_impact. **Migrations:** 20260301000000 (users.kei_impact_slug, kei_personal_note), 20260301100000 (seed kei_impact poll) — ensure both are committed with the branch (git add alembic/versions/202603*.py). |
| 2026-03-01 | Call script: human tone, realtime stats, legislator-relevant facts | **Copy:** Call script and voicemail rewritten for human, professional tone (conversational openings, "I'd like to leave a message", natural the_problem/the_legal_why/the_ask; SCRIPT_KEI_EXPLAINER_QUESTION "Ever heard of Kei vehicles?"; SCRIPT_ELEVATOR_THREE and fallbacks aligned). **Stats:** _volume_phrase uses calls_total when >= SOCIAL_PROOF_MIN_PEOPLE (real number in "Over N people across Illinois have already been in touch..."); else "Lots of Illinois residents...". Per-office: get_outreach_count_for_member in outreach.py; drawer passes contact_count_this_office to build_script_sections_*; when >= OFFICE_CONTACT_MIN (2), script adds "A few other constituents have already been in touch with your office about this." advocacy_helpers.py, _advocacy_drawer_call.html (fallbacks + voicemail + divider labels), advocacy.py, outreach.py. |
| 2026-03-01 | Call script: remove Stay informed block | **Deleted:** "Stay informed" email capture (heading, copy, email input, Subscribe button) removed from end of call script in advocacy drawer. Template: _advocacy_drawer_call.html — removed drawer-subscribe-wrap block. JS: index.html — removed subscribeWrap reveal/scroll in poll success and catch branches. CSS: advocacy-drawer.css — removed .drawer-subscribe-wrap and .drawer-subscribe-p; done-view subscribe (index.html drawer-done-subscribe) unchanged. |
| 2026-03-01 | Conversation UI componentization | **Refactor:** One canonical bubble enter animation: `.bubble-in` and `.bubble-in-stagger` in advocacy-drawer.css; old `*-just-in` classes kept as aliases. All conversation orchestration (personalize flow, end-call wrap-up) moved to `static/js/conversation.js` as `ConversationUI` (revealBubble, revealAfter, revealSequence, animateOptions, initKeiPersonalizeFlow, initDrawerEndCall). Contract: anything that enters the call conversation must be revealed via `ConversationUI.revealBubble(el)`. index.html trimmed (~150 lines removed); call sites use `ConversationUI.initDrawerEndCall(body)` and `ConversationUI.revealBubble(el)`. base.html loads conversation.js before block content. |
| 2026-03-01 | Kei personalize: step 1 option bubbles fade up, no flash | **UI:** First-question option buttons ("I have or had a kei", "I don't have a kei") were flashing instead of fading up when the drawer content was injected via HTMX. Cause: no explicit initial state, so one frame could paint at full opacity before the animation's from {} applied. **Fix:** .kei-options-animate .kei-option-btn now get initial opacity: 0 and transform: translateY(6px) so they start at the animation "from" state and consistently fade up like steps 2/3. advocacy-drawer.css. |
| 2026-03-01 | Kei personalize: option bubbles same blue + animation as user | **UI:** Option choice bubbles (Do you have a Kei?, Which best describes you?, How does this affect you?) now use the same blue as user bubbles (--bubble-you-bg, --bubble-you-color) instead of light blue + dark text. Unselected = same blue; selected = darker blue + ring. Removed --bubble-option-bg/--bubble-option-color; .kei-option-btn uses --bubble-you-bg/--bubble-you-color and hover uses --bubble-you-bg-hover. Animation already shared (drawer-reminder-in + kei-options-animate for all steps via showStep). advocacy-drawer.css. |
| 2026-03-01 | Bubble component tokens | **Refactor:** Extracted bubble design tokens into CSS custom properties in advocacy-drawer.css. :root now defines --bubble-radius, --bubble-pad, --bubble-you-bg/--bubble-you-color/--bubble-you-bg-selected, --bubble-them-bg/--bubble-them-color, --bubble-enter-duration, --bubble-enter-easing, --bubble-transition, --bubble-bar-fill-duration/delay. .chat-bubble, .chat-you, .chat-them and .kei-option-btn use tokens; option buttons use same blue as user bubbles. All drawer-reminder-in animations and poll-voted::after transition reference tokens. |
| 2026-03-01 | Kei impact results: all options in chart | **Feature:** Impact results now show all 12 options (status-specific + universal), not just the 4 universal (support_cause, know_someone, civic_duty, other). **Constants:** KEI_IMPACT_ALL_OPTIONS in constants.py (flattened from KEI_IMPACT_OPTIONS). **Backend:** kei_poll_context._get_kei_impact_results() returns "options" in result dict (full list + any DB slugs not in list as "Other"); _impact_options_for_results() builds list. **Templates:** _kei_poll_logged_in_success, _kei_poll_anonymous_success, _why_you_care_branch use kei_impact_results.options for chart and selected-label when present (fallback to kei_impact_options). **Advocacy:** When saving impact from personalize/persist-poll, dual-write to kei_impact poll via _dual_write_kei_impact() so status-specific choices (work_commute, recreation, etc.) are counted. |
| 2026-03-01 | Kei poll: third question "How does this affect you?" | **Feature:** Main poll (card + inline) now has three steps: Q1 have/don't have kei, Q2 status, Q3 how it affects you (I support the cause, I know someone affected, Civic duty, Other). **Constants:** KEI_POLL_IMPACT_SLUGS, KEI_POLL_IMPACT_OPTIONS, KEI_IMPACT_SLUG_COOKIE in constants.py. **DB:** Migration 20260301100000 seeds kei_impact poll (id=2) + 4 options; POST kei-status writes PollResponse for kei_impact; User.kei_impact_slug set. **Backend:** updates.py accepts kei_impact_slug, validates, persists user + cookie; kei_poll_context returns kei_impact_selected, kei_impact_results, _get_kei_impact_results(); advocacy uses KEI_IMPACT_SLUG_COOKIE from constants. **UI:** _macros.html kei_poll/kei_poll_inline step 3 panel, progress 1/3; base.html JS shows step 3 after status, submit enabled only when status + impact selected; success views show impact + optional impact chart. **Admin:** Kei poll results page links to "View impact breakdown" (kei_impact poll). **Definitions:** metrics_definitions.KEI_IMPACT_DEFINITION; campaign-decoupling.md User + Polls. **Tests:** test_updates.py POST kei-status includes kei_impact_slug; test invalid/missing impact. |
| 2026-03-01 | Call drawer: poll choice saved on answer, not on "See my script" | **Flow:** Kei poll choice (kei_status from step 2, kei_impact_slug from step 3) is now persisted when the user answers each question, not when they click "See my script". New POST /advocacy/personalize-poll accepts kei_status and kei_impact_slug, validates, updates user or sets cookies, returns JSON. Frontend: savePersonalizePoll(keiStatus, keiImpactSlug) called from personalize click handler when option is kei_status or kei_impact_slug. advocacy.py; index.html. |
| 2026-03-01 | Drawer conversation: auto-scroll when new bubbles appear | **UI:** Conversation, poll, and call script drawers now auto-scroll so new bubbles are always in view. Kei personalize: after each showStep(2/3/4) and when step 4 is revealed on init, first revealed bubble scrolls into view. Call script: when subscribe wrap is revealed after poll selection (success or catch), subscribeWrap.scrollIntoView. index.html showStep(), initKeiPersonalizeFlow(), poll pill click handler (then + catch). |
| 2026-03-01 | Personalize bubbles: match call script animation | **UI:** All KEI personalize bubbles (grey APP + blue options) now use the same fade-in + slide as call script wrap-up: `drawer-reminder-in` 0.35s ease (opacity 0→1, translateY(6px)→0). Grey bubbles get `drawer-bubble-just-in` on first paint (step 1) or when revealed (showStep); blue options keep staggered delay via `kei-options-animate`. Removed `kei-bubble-up` keyframes; reduced-motion disables animation for both. advocacy-drawer.css, _kei_personalize_drawer.html (step 1), index.html showStep + initKeiPersonalizeFlow. |
| 2026-03-01 | Message bubble component: pointer cursor | **UI:** Bubble option buttons (Kei personalize "How does this affect you?", etc.) did not show pointer cursor on hover. **Fix:** In shared chat-bubble block (advocacy-drawer.css), added `button.chat-bubble, a.chat-bubble { cursor: pointer; }` so all interactive message bubbles get consistent pointer cursor regardless of parent. |
| 2026-03-01 | Call drawer: scroll to top after "See my script" | **Bug:** After filling poll and clicking "See my script," drawer stayed scrolled down. **Fix:** In htmx:afterSwap when target is #advocacy-drawer-body, set body.scrollTop = 0 so script view shows from top. index.html. |
| 2026-03-01 | Personalize poll: unselected = light blue, no reply bubble | **UI:** All poll option buttons (step 1 and 2/3) use same unselected style: light blue bg (#d0e3f7), dark blue text (#004a94); selected keeps dark blue + white + ring. **Fix:** Removed addYouBubble() on option click — selection still recorded in form state (hidden inputs) and step flow; no right-aligned "you" bubble (avoids layout breakage). Language continuity: option label text unchanged; form values remain option slugs/labels from template. advocacy-drawer.css .kei-option-btn default + .chat-label; index.html personalize poll handler. |
| 2026-03-01 | KEI personalize drawer: button states + See my script CTA | **UI:** Personalize poll: option buttons get hover (darker blue), :active scale(0.98), selected state = same bubble shape with darker blue (#005bb5) instead of bordered box; "See my script" is a green CTA button (.kei-see-script-btn), not a bubble; poll bubbles use same .chat-bubble .chat-you/.chat-them as script drawer, no extra border. advocacy-drawer.css, _kei_personalize_drawer.html. |
| 2026-03-01 | Call script drawer: poll selection + iOS feel | **UI:** "How interested did they seem?" poll in wrap-up: clear selected state (2px ring + darker tint when selected), 44px min-height tap targets, :active scale(0.98) tap feedback, focus-visible outline; stronger per-score selected backgrounds; .drawer-interest-pill-dot 24px + checkmark when selected. advocacy-drawer.css only; poll-voted bar chart keeps border-right: none. |
| 2026-03-01 | Call script: Kei explainer before THE PROBLEM | **Flow:** After "they'll say yes or transfer," script now asks "Are you familiar with Kei vehicles?" then gives a fuller, formal two-sentence definition (SCRIPT_KEI_EXPLAINER_QUESTION, SCRIPT_KEI_EXPLAINER_SHORT from KEI_GLOSSARY), then THE PROBLEM (Illinois-only via the_problem_after_explainer). advocacy_helpers.py: constants; build_script_sections_senator/rep/broker add kei_explainer_question, kei_explainer_short, the_problem_after_explainer. _advocacy_drawer_call.html: QUICK CONTEXT divider + two You bubbles (question, short), THE PROBLEM uses the_problem_after_explainer when present. **Copy:** Quick context wording expanded and formalized—Japanese vehicle class, size/engine limits, highway-built, 25-year federal rule—sourced from content_constants KEI_GLOSSARY. Voicemail unchanged (full script). |
| 2026-03-01 | Drawer poll: constituent state + styles | **State:** Constituent checkbox now correct on personalize (poll) and preserved when going to script. Backend: GET drawer passes is_constituent to _kei_personalize_drawer.html (root has data-is-constituent); POST /advocacy/personalize accepts constituent form field, parses to bool, passes is_constituent_override into _render_call_drawer so script reflects user choice. Personalize form + skip form: hidden constituent input; JS sets value from header checkbox on submit/skip. **HTMX afterSwap:** When drawer body swaps (e.g. poll→script), prefer existing data-drawer-constituent if set, else sync from root; call initDrawerConstituentCheckbox(body) so script view gets phrase updates and listener. **Styles:** Poll (personalize) aligned with rest of drawer: skip button uses same green link style as no-answer hint; note input/char count use drawer font vars and 18px radius; kei-submit-btn align-self flex-end. |
| 2026-03-01 | Call script: state your name | **Copy:** Every call script opening now includes "My name is [CALLER_NAME]." so callers are prompted to identify themselves. advocacy_helpers.py: SCRIPT_OPENING_NAME_LINE; _generic_opening, build_personalized_opening (personalized path), build_script_sections_senator/rep/broker all use it. Template fallback and voicemail already had [CALLER_NAME]. |
| 2026-03-01 | Call script: human tone | **Copy:** Call script and voicemail rewritten to sound conversational and friendly. advocacy_helpers.py: _generic_opening, _personalized_opening_sentence (full-sentence openings with "Hi — I'm in your district and…" / "Hi — I'm calling because…"), build_script_sections_* (the_problem, the_legal_why, the_ask, easy_yes_close, closing), SCRIPT_ELEVATOR_THREE. Template: _advocacy_drawer_call.html fallbacks and voicemail script aligned. Same facts/canonical content; tone only. |
| 2026-03-01 | Kei personalize: "You" bubble placement (no 3rd stray bubble) | **Bug:** After selecting "I don't have a kei" (step 1), the "You" reply bubble was appended to the end of the chat, so it appeared below step 2 options as a stray 3rd blue bubble. **Fix:** addYouBubble(text, insertAfterEl) now inserts the reply immediately after the step's options container (kei-options-1/2/3) so order is: Q → options → You reply → next Q → options. index.html personalize poll click handler. |
| 2026-03-01 | Personalize poll: two-step Kei question + bubble animation | **Flow:** First question "Do you have a Kei vehicle?" shows only two bubbles (I have or had a kei / I don't have a kei); on selection, second question "Which best describes you?" shows 3 or 2 dynamic options (status sub-options). Then impact (step 3) and optional note (step 4). **UI:** Step 1 and step 2 option bubbles use .kei-options-animate for bubble-up animation (translateY + opacity, staggered delay); when user clicks a bubble, unselected siblings get lower opacity (.kei-step-options--has-selection). constants.py: KEI_FIRST_OPTIONS, KEI_STATUS_BY_FIRST; advocacy.py passes kei_first_options, kei_status_with_first; _kei_personalize_drawer.html step 1 = kei_first_options, step 2 = kei_status_with_first (data-kei-first), step 3 = impact, step 4 = note. advocacy-drawer.css: @keyframes kei-bubble-up, .kei-options-animate, .kei-step-options--has-selection. prefers-reduced-motion disables animation. |
| 2026-03-01 | Personalize poll: selected state + skip | **Bug:** Phone-script personalization poll: (1) Option buttons did not show which choice was selected; (2) Skip used JS form.submit() so HTMX never intercepted → full-page POST. **Fix:** _kei_personalize_drawer.html: option buttons get aria-pressed + .kei-option-btn--selected on click; setOptionSelected() clears siblings; skip button is type="submit" form="kei-personalize-skip-form" so native submit fires and HTMX intercepts; main form uses form submit listener to sync note/constituent (no preventDefault + form.submit()). advocacy-drawer.css: .kei-option-btn--selected / [aria-pressed="true"] — darker blue (#004a94) + white ring (box-shadow 0 0 0 2px #fff, 0 0 0 4px #004a94) so selection is clearly visible. |
| 2026-03-01 | iOS script personalization | **Feature:** Pre-drawer conversation flow captures Kei impact + optional note; call script and email "This matters to me because" personalized. **Data:** users.kei_impact_slug, users.kei_personal_note (migration 20260301000000); cookies for anon (kei_impact_slug, kei_personal_note, kei_personalization_skipped). **Flow:** GET /advocacy/drawer?view=call shows _kei_personalize_drawer.html when caller profile incomplete (no kei_status/impact/note/skip); if user already answered the poll (kei_status from cookie or user), skip questions and show call script directly with script personalized from their responses. Step 1 (Do you have a Kei?) or Step 2 (How does it affect you?) + Step 3 (optional note); POST /advocacy/personalize saves and returns call drawer HTML. **Backend:** advocacy_helpers CallerProfile, build_personalized_opening(), build_personalized_email_why(), _kei_email_body_core(caller=); KEI_IMPACT_OPTIONS in constants; _render_call_drawer(); advocacy.py _caller_profile_from_request, _caller_profile_complete (treats kei_status as complete → skip personalize), personalize route. **Templates:** _kei_personalize_drawer.html (iOS bubbles, skip link, HTMX swap); advocacy-drawer.css .drawer-personalize-*. **Deleted/consolidated:** Call drawer build logic extracted to _render_call_drawer (GET drawer + POST personalize). |
| 2026-03-01 | Poll sidebar buttons     | **UI:** Results drawer "Share your story" uses .btn .btn--story; "Need to change your answer?" is gray text link-style (no outline button). _kei_poll_*_success.html, _why_you_care_branch.html: kei-poll-change-btn has no .btn/.btn--secondary or icon; base.css .kei-poll-change-btn = muted color, underline on hover, focus-visible ring. Anonymous "Not yet" and sign-in use btn btn--secondary. |
| 2026-03-01 | Kei vehicle definition in call script + email | **Copy:** Added one-sentence definition ("compact trucks, vans, and cars originally built for highway use in Japan — federally legal to import under the 25-year rule") at first mention of "kei vehicles" in: advocacy_helpers.py (the_problem for senator/rep/broker, _kei_email_body_core), _advocacy_drawer_call.html (THE PROBLEM fallback bubble, voicemail script). Sourced from KEI_GLOSSARY in content_constants.py. |
| 2026-03-01 | Social proof "One of N": deferred until 100+ people | **Product:** Do not show "One of N Illinois residents" or hero ticker count anywhere until at least 100 people have done at least one form of outreach. advocacy_helpers.py: SOCIAL_PROOF_MIN_PEOPLE = 100; _volume_phrase always returns generic "Illinois residents have been reaching out..."; _kei_email_body_core uses same generic line (no N). Heroes: _hero_home.html, _hero_advocacy.html — when calls_total > 0 show "Illinois constituents are taking action." (no number). When ready: add unique-constituent count, gate script/email/hero on that count >= 100. |
| 2026-03-01 | Call script: one-pager = states that overturned Kei bans | **Copy:** Call script and email no longer say "21+ states"; they now say the one-pager shows which states have successfully overturned their Kei bans. advocacy_helpers.py: SCRIPT_ELEVATOR_THREE (3), the_ask (senator/rep/broker), broker the_legal_why, _kei_email_body_core. _advocacy_drawer_call.html: THE ASK fallback, pushback "We need more info", three points (3), voicemail. |
| 2026-03-01 | Pristine call script + email (Hardball Ch7) | **Refactor:** Call script and email rewritten for awareness stage. advocacy_helpers.py: SCRIPT_ELEVATOR_THREE district-first; build_script_sections_* (senator/rep/broker) — human opening, the_problem (with _volume_phrase(calls_total)), the_legal_why, stage-correct the_ask ("be aware", one-pager, no commitment), easy_yes_close; build_email_subject_line(..., district=) for subject; _kei_email_body_core(..., calls_total, district_label) — constituent line, social proof "I'm one of N", 2-point ask (review one-pager, connect if interest). Router: _build_search_results_context(..., calls_total); cards get district + calls_total for script/email; drawer call view builds script_sections + passes calls_total; drawer email views get agg for calls_total. Template: _advocacy_drawer_call.html uses script_sections for opening/problem/legal_why/ask/easy_yes_close/conclusions; "Three points" moved after push-back. |
| 2026-03-01 | Email drawer subject     | **Feature:** Auto-place user's district in email subject when available. advocacy.py: build_email_subject_line(..., district=district) in email_first drawer and call wrapup so subject shows "Constituent request (District N): ..." for staff triage; cards already passed district. |
| 2026-02-28 | Campaign decoupling (Warlord config + DB + leaks) | **Refactor:** Warlord config: campaign_name, primary_color, issue_summary in config/campaign.json and CampaignConfig; config.py defaults SITE_NAME and META_DESCRIPTION from campaign; primary_color/campaign_name/issue_summary exposed to all template globals and base.html CSS vars. DB: ILGA_TENANT derives ILGA_DB_PATH when unset (data/{tenant}_{profile}.db); docs/architecture/campaign-decoupling.md Provisioning section; scripts/provision_tenant.py (tenant id + dev\|prod, run\|migrate). Router leaks: admin and updates use get_campaign_config().poll_slug for Poll lookup; auth, feedback, updates, email_utils—all "Land of Kei" / landofkei.com fallbacks removed (cfg.SITE_NAME, cfg.APP_BASE_URL only). |
| 2026-02-28 | Campaign decoupling (white-label stage) | **Refactor:** Kei advocacy decoupled for future multi-campaign. docs/architecture/campaign-decoupling.md documents scrapers (generic ILGA), DB (OutreachEvent generic; User.kei_status/Kei* tables campaign-specific), advocacy (logic generic; hero/topic/brief from config). config/campaign.json holds hero copy, default_topic, brief_pdf_filename, brief_pdf_url_path, one_pager_points, poll_slug. campaign_config.py loads JSON (ILGA_CAMPAIGN_CONFIG override); CampaignConfig dataclass with Kei defaults when file missing. advocacy.py: hero from get_campaign_config(), _default_topic(), _one_pager_points() (fallback STRATEGIC_FIVE_POINTS), _brief_pdf_path(); GET /advocacy/brief.pdf and legacy /advocacy/IL_Kei_Vehicle_Registration_Fix_Brief.pdf serve configured file; drawer email template uses brief_pdf_url/brief_pdf_download_name. Swap campaign = replace config/campaign.json. |
| 2026-02-28 | Subscribe hidden when already subscribed | **Feature:** When a logged-in user has wants_updates=True, all subscribe components are hidden site-wide until they log out. Middleware sets request.state.user for HTML requests; base.html (footer), _mission_cta.html (home CTA), the_issue.html (Stay informed), index.html (drawer-done-subscribe) wrap subscribe blocks in `{% if not (request.state.user and request.state.user.wants_updates) %}`. Test app in test_updates.py adds _user_for_html_middleware; TestSubscribeComponentVisibility (footer shown when anonymous, hidden when subscribed, shown when logged in but unsubscribed). |
| 2026-02-28 | Why-you-care branch: one sign-in, show Share only when logged in | **Cleanup:** Single sign-in nudge when anonymous: "Sign in to save your answer and share your story." with one "Sign in" button. When anon: hide secondary CTA (no disabled Share button in row), hide footer Share section entirely. When logged in: show primary + secondary CTAs and Share section as before. Removed duplicate "Sign in to share your story" buttons and wyc-trigger-signin handler (base.html). |
| 2026-02-28 | Share your story disabled when anonymous | **Bug:** "Share your story" was shown as an active button after anonymous poll submit; story/statement submission requires login. **Fix:** _why_you_care_branch.html: when anon, no longer show Share button at all (see cleanup above). components.css: .btn:disabled / .btn--story:disabled for visual disabled state. |
| 2026-02-28 | Why-you-care pill: 5 animated icons by selection | **Icons:** Pill shows a different animated icon per poll outcome. Slug from selection: registered, revoked, denied (owner branch) or would_want, would_not_want. home.py + updates.py set `wyc_pill_icon_slug` when rendering branch. _macros.html wyc_pill_icon: registered (check circle), revoked (circle minus), denied (circle slash), would_want (cart), would_not_want (document). Animations: pulse, downward nudge, shake, bounce, scale. why-you-care.css: 5 modifier classes + keyframes; prefers-reduced-motion disables all. Removed `icon` from WHY_YOU_CARE_BRANCHES. |
| 2026-02-28 | Why-you-care branch CTAs: flipped to outline | **Design:** Branch "Start outreach" and "Share your story" buttons use outline style (light bg, colored text/border) instead of solid. why-you-care.css: .wyc-branch-content__ctas .btn--primary and .btn--story overrides; primary = orange outline (Start outreach), story = blue outline (Share your story); hover tints (#fff4e6, #e8f0fe). |
| 2026-02-28 | Welcome email unsubscribe link | **Feature:** Welcome email now includes a one-click Unsubscribe link (same tokenized flow as campaign update emails). email_utils: send_welcome_email(..., unsub_url=...); plain and HTML bodies append Unsubscribe when unsub_url present. auth.py and updates.py (subscribe_post) build unsub_url and pass to send_welcome_email. Tests: auth test asserts call includes unsub_url with /updates/unsubscribe and token=; test_updates.py TestWelcomeEmailUnsubscribe (body contains Unsubscribe when unsub_url provided, no Unsubscribe when omitted). |
| 2026-02-28 | Poll 401 on prod (kei-status) | **Bug:** POST /updates/kei-status returned 401 Unauthorized on prod because api_key_middleware required X-API-Key for non-GET requests and /updates was not exempt. **Fix:** middleware.py api_key_middleware now exempts `path.startswith("/updates")`. Same-origin poll and subscribe-email POSTs do not require an API key. |
| 2026-02-28 | Poll 403 on prod (CSRF cookie Secure) | **Bug:** With PROFILE=prod, CSRF cookie was set with `secure=True`. On HTTP (e.g. local prod at 127.0.0.1:8000 or any HTTP origin) the browser does not send Secure cookies, so cookie was missing on POST → CSRF validation failed → 403. **Fix:** middleware.py csrf_cookie_middleware now sets `secure=request.url.scheme == "https"` so the cookie is sent back over HTTP when testing locally; real prod over HTTPS still gets Secure. |
| 2026-02-28 | Poll 403 CSRF token missing in POST body | **Bug:** Logs showed has_token: false, has_cookie: true — cookie was sent but form body had no csrf_token (e.g. when macro rendered without request or HTMX omitted it). **Fix:** base.html htmx:configRequest now adds X-XSRF-TOKEN header from cookie for all HTMX POSTs so backend receives token via header even when form field is missing. updates.py why_you_care_flow passes request explicitly in context so ambient partial includes CSRF in form. **Cleanup:** Removed debug instrumentation from middleware.py (401 log) and updates.py (403 CSRF log). |
| 2026-02-28 | Why-you-care: three items on home when not logged in | **Fix:** Home router did not set `why_you_care_default_cards` in template globals, so "Why should you care?" showed empty cards + poll only. Added `WHY_YOU_CARE_DEFAULT_CARDS` to home.py globals so the three insight cards (Real people / Fair law / Your voice) render for all users on initial load. |
| 2026-02-28 | Admin flow definitions  | **Flow definitions page:** GET /admin/flows shows drawer (call answered, call no-answer, email) and why-you-care step slugs plus WYC default cards and branches; data from outreach_steps.py and content_constants.py. Nav "Flows"; dashboard Growth & funnel link; Stats page points to Flow definitions first. Docs: app-overview, glossary. |
| 2026-02-28 | WYC funnel tracking     | **Funnel:** Why-you-care section now uses step definitions and POST /outreach/step (outreach_type=wyc, member_id nullable) for outcome-focused tracking. Step slugs: wyc_poll_submitted, wyc_branch_viewed, wyc_clicked_to_advocacy, wyc_clicked_to_the_issue, wyc_share_story_clicked, wyc_change_answer_clicked. **Backend:** outreach_steps.py WYC_STEPS + ALLOWED_STEPS["wyc"]; migration 20260228150000 member_id nullable on outreach_step_events; routers/outreach.py accepts wyc, optional member_id. **Client:** base.html recordWycStep(), delegated click on [data-wyc-step], branch_viewed on htmx:afterSwap, poll_submitted on htmx:beforeRequest to /updates/kei-status. **_why_you_care_branch.html:** data-wyc-step on primary/secondary CTAs, share story, change answer. **Docs:** db-and-outreach.md WYC slugs and Hardball context (Ch7 making the case to the base, Ch6 mobilizing grassroots). |
| 2026-02-28 | Security and privacy audit | **CSRF:** POST /updates/kei-status and POST /updates/subscribe-email now require and validate CSRF (token from form or X-XSRF-TOKEN header); kei poll forms and subscribe_form macro + drawer form include hidden csrf_token. **Rate limiting:** security.py — rate_limit_subscribe_email, rate_limit_kei_status (anon only), rate_limit_story_submit, rate_limit_statement_submit (per-IP, config RATE_LIMIT_*). updates.py and stories.py call limiters; 429 when over limit. **File uploads:** file_validation.py — magic_matches_image_content_type (JPEG/PNG/WebP/GIF); updates _save_update_image and feedback _save_bug_report_image now validate magic bytes; stories use shared helper. **Privacy:** privacy.html — new bullet "Community stories and statements" (what we collect, how we use, retention). **Removed:** auth.py debug log block in _send_code_email. **Tests:** test_updates.py subscribe-email and kei-status POSTs use _data_with_csrf; macros use `request is defined` guard for csrf_token. |
| 2026-02-28 | Share your story modal centering | **Fix:** Story/statement submit dialog was off-center (left/up). Global `* { margin: 0 }` in base.css overrides the UA default `margin: auto` for `<dialog>`. Added `margin: auto` to `.story-submit-dialog` so the modal centers in the viewport when shown with showModal(). |
| 2026-02-28 | Poll sign-in nudge: no blue bg, blue text on hover | **Fix:** "Sign in to save your answer" had unwanted blue background; should be transparent with blue text on hover only. base.css: .kei-poll-nudge-block__signin-link / .kei-poll-nudge-block .auth-strip-signin-btn — background transparent, color inherit; hover keeps background transparent, color #1a73e8. |
| 2026-02-28 | Caution icon (poll nudge) | **Fix:** Poll sign-in nudge caution icon was triangle + tiny dot only; replaced with proper triangle + exclamation (vertical stem + dot). **Macro:** `icon_caution()` in _macros.html; used in _why_you_care_branch.html and _kei_poll_anonymous_success.html. |
| 2026-02-28 | Why-you-care: poll centered, CTAs removed, why-we-ask line | **Poll:** .why-you-care-flow centered (flex column, align-items: center, text-align: center); .wyc-inline-poll max-width 360px; .wyc-cards width 100%. **CTAs removed:** "Learn about the issue" and "Start outreach" links removed from _why_you_care.html and _why_you_care_flow_ambient.html (ambient state only; branch view keeps its CTAs). **Why we ask:** KEI_POLL_WHY_WE_ASK in content_constants.py ("We use responses to show legislators how many people are affected and who supports the fix."); .wyc-poll-intro above poll in both templates; content.py global kei_poll_why_we_ask. |
| 2026-02-28 | Why-you-care: CTA buttons, poll bg, branch animation | **Buttons:** Ambient and branch CTAs use .btn .btn--primary / .btn--secondary (Learn about the issue, Start outreach) in _why_you_care.html, _why_you_care_flow_ambient.html, _why_you_care_branch.html. **Poll results:** .wyc-branch-content__results has background, padding, border-radius (why-you-care.css). **Animation:** .wyc-branch-content uses wyc-branch-in keyframes (fade + translateY); after poll submit, branch headline scrolls into view (base.html htmx:afterSwap). Reduced-motion: animation disabled. |
| 2026-02-28 | Why-you-care: unified button styles + icons | **No link-style:** Sign-in, "Not yet", "Need to change your answer?" use .btn .btn--secondary (outline) instead of gray/blue underline. **Story CTA:** "Share your story" and story/statement modal Submit use .btn .btn--story (orange from --color-hero-cta). **Icons:** components.css .btn-icon; all branch and modal buttons have inline SVG icons (external-link, camera, log-in, check, x, refresh). **Templates:** _why_you_care_branch.html, _why_you_care.html, _story_submit_modal.html, _statement_submit_modal.html. **CSS:** components.css .btn--story, .btn:focus-visible; base.css form buttons use shared .btn, only disabled state kept. |
| 2026-02-28 | Why-you-care adventure flow | **Home:** Replaced two-column (poll \| three reasons) with single-column choose-your-path flow. **Ambient:** Three insight cards (why_you_care_default_cards) + inline poll (Do you have a kei? Yes/No → sub-options) + generic CTAs; no card chrome, pill buttons. **After submit:** HTMX swaps #why-you-care-flow with branch partial: tailored headline/body/CTAs (owner / would_want / would_not_want from WHY_YOU_CARE_BRANCHES), results chart, Share your story, "Need to change your answer?" (GET /updates/why-you-care-flow resets to ambient). **Content:** content_constants.py WHY_YOU_CARE_DEFAULT_CARDS, WHY_YOU_CARE_BRANCHES; content.py exports + globals. **Templates:** _why_you_care.html (flow + branch include), _why_you_care_branch.html, _why_you_care_flow_ambient.html; _macros.html kei_poll_inline. **CSS:** why-you-care.css — .why-you-care--flow, .wyc-cards, .wyc-card, .wyc-inline-poll, .wyc-branch-content; removed .why-you-care-marquee--in-grid. **JS:** base.html initKeiPollBranch handles data-kei-poll-inline; initWycResetFlow for change-answer. **Routes:** home.py passes why_you_care_branch + kei_status_total; updates.py kei_status_post returns branch partial for poll_id=home-kei-poll; GET /updates/why-you-care-flow. **Deleted/consolidated:** Two-col grid layout; WHY_SHOULD_YOU_CARE_TEASER_HEADING unused in flow. |
| 2026-02-28 | Simplify codebase       | **content_constants:** All canonical copy (STRATEGIC_*, FAQ_*, glossaries, TIMELINE_PHASES, MARQUEE_IMAGES, BRIEF_*, etc.) moved to `routers/content_constants.py`; content.py imports and re-exports for existing callers; view helpers (_inline_glossary_terms, apply_inline_glossary, get_strategic_states_tooltips, _brief_map_fill_status, _brief_aamva_fix_state_abbrs) remain in content.py. **Site into home:** Site routes (favicon, sitemap, robots, /advocacy and /intelligence redirects) merged into home.py; site.py deleted; main mounts only home_router for those paths. **Legal into content:** GET /privacy and GET /terms moved to content router; legal.py deleted. **kei_poll_context:** `kei_poll_context.py` at package root holds get_kei_poll_initial_state, get_kei_poll_sidebar_context, _get_kei_status_results, _validate_kei_status, poll IDs and cookies; home, content, advocacy, updates import from it. |
| 2026-02-18 | main.py split           | app_state, constants, startup_banner, date_parse, member_lookup; routers (advocacy 02-19, intelligence + bills + explore 02-20); base.html CSS → static/css; Cursor skills extract-route-group, graphql-resolver-and-loaders. main.py ~4780 → ~1800 lines. |
| 2026-02-18 | Advocacy helpers        | All advocacy logic in `advocacy_helpers.py`; state as first arg; member lookup in `member_lookup.py`, re-exported. |
| 2026-02-19 | Unified outreach drawer | Single "Reach out" button; step strip (Call · Email); drawer close-then-open; files: _results_partial, index, base. |
| 2026-02-19 | UI consistency          | Member card grid fix, error display single place, drawer naming, recommendation chips from Python, legislator_drawer_context. |
| 2026-02-19 | Snapshot mocks          | `make snapshot-mocks` samples cache JSON types from mocks; zip_to_district subset for 40 mock members. |
| 2026-02-20 | Refactor completed      | CSS → static/css (variables, base, advocacy×4, intelligence×2); intelligence → routers/intelligence.py; explore → routers/explore.py; SHAP → routers/bills.py. |
| 2026-02-18/20 | Legacy purge        | Removed intelligence/explore from main, inline CSS; POST /advocacy/drawer/after-call; dead ML fulltext; test mode/deep link; unused CSS. |
| 2026-02-21 | main.py refactor        | Lifespan → startup.py; site routes (/, /advocacy, favicon, sitemap, robots) → routers/site.py; /logs, /health, /api/dev/members → routers/admin.py; GraphQL Query + ML types → graphql_query.py; CORS/API key/CSRF/security/request-logging → middleware.py. main.py ~1921 → ~277 lines. Tests pass; _member_career_start re-exported from main for test compat. |
| 2026-02-27 | Dead code purge (UI)    | Audit scripts: scripts/audit_css_usage.py (extract class/id from templates, parse CSS, report candidate-dead with safelist); scripts/audit_js_usage.py (definitions vs references, safelist entry points); scripts/audit_template_reachability.py (TemplateResponse/get_template/include/extends → unreachable templates). Removed dead CSS: base.css .beta-banner-link and :hover/::after/:focus (no template or script reference). No JS removals (candidates are callbacks). Reports: audit_css_report.txt, audit_js_report.txt, audit_template_reachability_report.txt. See docs/development/ui-cleanup.md. |
| 2026-02-28 | Kei poll: remove step-2 nudge, disable submit until Q2 | **Removed:** "One more — then see results" nudge. **Submit:** Disabled until user has chosen branch (Q1) and a kei_status option (Q2); JS in base.html updateSubmitState + change listener; CSS .kei-poll__submit:disabled. _macros.html, base.html, base.css, app-overview. |
| 2026-02-28 | Kei poll: progress, benefit, submit CTA (completion boost) | **Completion:** Progress indicator (step dots 1/2), benefit line ("Your input helps us explain to legislators who's really affected. Numbers speak louder than words."), submit "Submit and see results". _macros.html (progress nav, intro, button); base.html branch script updates progress; base.css .kei-poll__progress*, .kei-poll__intro. app-overview Key concepts. |
| 2026-02-28 | Kei poll: dynamic header, black strip, single card | **Headers:** Step 1 = "Do you have a kei vehicle?"; step 2 = "What's your registration status?" or "Would you want one?" (JS updates from panel data-step-heading); results = "Results". **Header strip:** .kei-poll__header-strip with black bg (#1a1a1a), white text, top rounded corners. **Single card:** Card padding moved to .kei-poll__body; success fragments no longer wrap in outer div (innerHTML into wrap = one card, no double container). base.html branch script sets heading on branch + init; _macros.html, _kei_poll_*_success.html, base.css. |
| 2026-02-28 | Kei poll Results: collapsible drawer | **Sidebar/results:** Results section is a dropdown: "Results" header with chevron toggles body (open/close). Native `<details>`/`<summary>`; no JS. _kei_poll_anonymous_success.html, _kei_poll_logged_in_success.html wrap header+body in details; base.css .kei-poll-drawer, .kei-poll-drawer__summary, .kei-poll-drawer__chevron (rotate when open). |
| 2026-02-28 | Footer: kei poll removed | **Removed:** Kei poll block from footer (base.html). Poll remains on home ("Why you care") and /updates?prompt=kei. Deleted unused .footer-kei-poll CSS; comment in base.css updated. |
| 2026-02-28 | Admin stories 500 fix cleanup | Removed debug instrumentation from routers/stories.py (_debug_log, forced template render try/except, json import); admin/stories 500 was fixed by adding missing template globals (get_current_action_campaign, meta_description, etc.) so base.html renders. |
| 2026-02-28 | Timeline 500 fix + cleanup | **Fix:** content router’s Jinja env lacked `get_milestone_by_id` and `get_next_deadline`; base.html includes _current_action_banner.html which calls them → UndefinedError. Added both to content.py template globals (import from session_schedule). **Cleanup:** Removed debug instrumentation from timeline_page (_debug_log helper and all step/exception logs). |
| 2026-02-28 | Shared UI components refactor | **Design tokens:** variables.css — added --shadow-sm/md/lg, --radius-sm/md/lg/pill. **components.css:** New base classes .card, .badge, .btn, .input, .score-bar (with modifiers), .sidebar; score-bar fill color classes (score-high, score-bar-high, etc.). **Macros:** templates/_macros.html — faq_section, state_status_table, session_deadlines_table, subscribe_form, share_buttons (with inline script), strategic_points, score_bar. **Adopted:** FAQ in the_issue (faq_law, faq_advocacy), legislator_brief, fact_sheet; state table in the_issue, legislator_brief; deadlines table in the_issue (inside faq_session), timeline; subscribe forms in base, _mission_cta, updates, the_issue, _advocacy_drawer_call; share in home (full), the_issue/legislator_brief (sidebar); strategic points in home (cards), the_issue, legislator_brief, fact_sheet (banner); score_bar in _intelligence_predictions, intelligence_bill, _intelligence_accuracy, _intelligence_influence, _intelligence_committees, _intelligence_anomalies. **Deleted:** _share_section.html, _share_section_sidebar.html (merged into share_buttons macro). **CSS migration:** base.css score-bar-container tokens; intelligence-dashboard.css intel-summary-card, intel-empty tokens; components.css score-bar-container override for macro layout. |
| 2026-02-28 | content.py progress renames | **Import fix:** Renamed CAMPAIGN_TIMELINE_CHECKPOINTS → PROGRESS_CHECKPOINTS, CAMPAIGN_TIMELINE_ACHIEVED_COUNT → PROGRESS_ACHIEVED_COUNT in content.py so updates.py import succeeds (updates.py already used new names). |
| 2026-02-28 | Admin API key exempt (prod campaign create 401) | **Bug:** In prod with `ILGA_API_KEY` set, POST /admin/campaigns (create campaign) returned 401 "Invalid or missing API key" because /admin was not exempt. **Fix:** middleware.py api_key_middleware now exempts `path.startswith("/admin")`. Admin remains protected by session (require_admin). |
| 2026-02-28 | Lint + test fix (batch commit) | **Lint:** E501 fixes in db_models, updates.py (docstrings, cookie_opts); email_utils E501 per-file-ignore; I001 import sort (content, home, updates) via ruff --fix. **Test:** test_record_without_campaign_id_stores_null 401 fix — patch rate_limit_verify_code in test so verify-code returns 200 and session is set before /outreach/record. |
| 2026-02-28 | Git batch commit + PR #43 | Batched all sprint-3/campaign-updates changes into 2 commits: (1) feat(sprint-3): campaigns session_milestone, admin dashboard, timeline, glossary, updates, banners (83 files); (2) docs: glossary, app-overview, db-and-outreach, status-report, TODOS, no-hallucination rule. Pushed branch; created PR #43 with title/body from commits + TODOS. Tests: 395 passed, 2 skipped. |
| 2026-02-28 | Kei poll in sidebar (the-issue, legislator-brief, fact-sheet, glossary) | **Sidebar poll:** Same Kei poll (form or results) at bottom of sidebar, above Share, on The Issue, Legislator brief, Fact sheet, Glossary. Single poll id `sidebar-kei-poll` so state matches across pages. **Backend:** updates.py `get_kei_poll_sidebar_context()`, `SIDEBAR_KEI_POLL_ID` in `_KEI_POLL_IDS`; content routes (the_issue, legislator_brief, fact_sheet, glossary) async + Depends(get_db), get_current_user_optional, lazy import get_kei_poll_sidebar_context, merge into template ctx. **Templates:** _sidebar_kei_poll.html (form or _kei_poll_initial_results); the_issue.html, legislator_brief.html add poll section above share_buttons; fact_sheet.html, glossary.html refactored to extend _brief_skeleton with sidebar (Community poll + Share). **CSS:** .legislator-brief-sidebar__poll margin-top. **Docs:** app-overview Key concepts + table sidebar notes. |
| 2026-02-28 | Kei poll in advocacy sidebar | **Advocacy results:** Poll shown in the "Your goal" sidebar on /advocacy when results are displayed (GET with zip + in_district, POST /search success). **Backend:** advocacy.py imports get_kei_poll_sidebar_context; GET advocacy_page and POST advocacy_search merge poll context into template ctx. **Template:** _results_partial.html — new section .results-aside__poll (includes _sidebar_kei_poll.html) after outreach-goal, before My Outreach. **CSS:** advocacy-cards.css .results-aside__poll margin-top. |
| 2026-02-28 | Admin dashboard: active campaign | **Bug:** Dashboard "Current action" showed "No active campaign" while Campaigns list showed an active campaign. **Cause (1):** get_active_campaign enforces start_at ≤ now ≤ end_at; campaign had start_at in the future. **Fix (1):** get_active_campaign(db, for_admin=True) on dashboard skips time window. **Cause (2):** base.html overwrote active_campaign with get_current_action_campaign(request) (middleware value, None when before start_at). **Fix (2):** base.html now uses view-provided active_campaign when defined, else middleware value. |
| 2026-02-27 | Home CTA restyle         | Mission CTA block (_mission_cta.html): clearer hierarchy — primary (Start outreach + caption) and secondary (subscribe in a card). Replaced harsh line separators with "or" divider; subscribe in a soft card (bg, border-radius). Teal primary btn (.home-cta-btn) to match site accent; removed .mission-statement-cta-btn/.mission-statement-cta-text. Section semantics: <section> + visually-hidden "Take action" heading; a11y focus states kept. mission-statement.css .home-cta* only. |
| 2026-02-27 | Subscribe: one on mobile, unified styling | **Mobile:** Hide .legislator-brief-sidebar__stay-informed at max-width: 768px so The Issue shows one subscribe (footer only). **Styling:** Shared subscribe component in base.css — .footer-subscribe-*, .stay-informed-*, .subscribe-form/.subscribe-input/.subscribe-btn share same rules (purple button, 6px radius input). **Template:** the_issue.html Stay informed button uses .stay-informed-btn only (removed btn btn-primary). |
| 2026-02-27 | Key points banner (shared style) | **Key points** (STRATEGIC_FIVE_POINTS list) now use a shared banner style everywhere: The Issue, legislator brief, fact sheet. Subtle strip: light bg, 4px left border (primary), padding, rounded right corners; not flashy. strategic-points.css: `.strategic-points--banner`; templates use same section/list classes. Fact sheet five-points section uses `.strategic-points.strategic-points--banner` + `.strategic-points-list`/`.strategic-points-item`; removed `.fact-sheet-points-list`/`.fact-sheet-points-item` from base.css. |
| 2026-02-27 | Subscribe component consistency | **Base:** All subscribe blocks use shared base in base.css (form, input, button). **Templates:** _mission_cta.html, updates.html, _advocacy_drawer_call.html, index.html add .subscribe-form, .subscribe-input, .subscribe-btn alongside context classes; updates page button no longer uses btn btn-primary. **Variations:** Home CTA (mission-statement.css): centered form, 8px radius (color from base purple). Drawer (advocacy-drawer.css): gap 8px, green accent, 8px radius, larger padding. Footer, sidebar Stay informed, updates page use base (purple). Removed duplicate .updates-subscribe-email-form/input from base.css. |
| 2026-02-27 | Mission CTA button colors      | **Consistency:** Mission CTA "Start outreach" uses hero orange (--color-hero-cta, --color-hero-cta-hover, --color-hero-cta-focus-ring); Subscribe uses shared purple from base.css. variables.css: added --link-color; --color-cta-primary remains for other uses. mission-statement.css: .home-cta-btn and subscribe input focus use hero-cta tokens. |
| 2026-02-27 | Why should you care; removed What's happening | **Home:** Replaced "What's happening and how to fix it" section with "Why should you care?" (teaser + links to /the-issue and /advocacy). **The Issue:** Added "Why should you care?" section (intro + why your voice matters) before "Where Other States Stand." **Content:** content.py WHY_SHOULD_YOU_CARE_* from constituent brief + FAQ_ADVOCACY + STRATEGIC_FIVE_POINTS. **Deleted:** _whats_happening.html, whats-happening.css. **New:** _why_you_care.html, why-you-care.css; base.html links why-you-care.css. mission-statement.css comment updated. |
| 2026-02-28 | Why you care marquee: second real photo + 500 fix | **Photo:** christian-eduardo-huerta.jpg in marquee (after tyler_morales.png). **500 fix:** Moved marquee image list from inline Jinja `{% set %}` (fragile with quotes/special chars) to backend: content.py MARQUEE_IMAGES; template uses `marquee_images` from globals; home + content routers set `marquee_images`; template uses `marquee_images|default([])` so missing var is safe. |
| 2026-02-28 | Marquee image blurb: location | **Content:** MARQUEE_IMAGES now includes `location` (where that person is). Tyler & Christian → "Illinois"; placeholders → city (e.g. Chicago, Peoria). **Template:** _why_you_care.html shows location in figcaption via `.why-you-care-marquee-location`. **CSS:** why-you-care.css — location on its own line, muted. Placeholder captions shortened (city moved to location). |
| 2026-02-28 | 500 on all pages: macro template path | **Cause:** Jinja2 loader root is `.../templates`, so `{% from "templates/_macros.html" %}` resolved to `.../templates/templates/_macros.html` (missing) → TemplateNotFound. **Fix:** Replaced `templates/_macros.html` with `_macros.html` in base.html, home, the_issue, legislator_brief, fact_sheet, updates, timeline, _advocacy_drawer_call, _mission_cta, intelligence templates, _macros.html comment. |
| 2026-02-27 | Why you care: marquee + design | **Marquee:** Scrolling image strip (first image: tyler_morales.png in static/images/; rest placeholder picsum.photos until more real photos); duplicate strip for seamless loop; 50s linear animation; prefers-reduced-motion: no animation, single strip, overflow-x scroll. **Design:** Header max-width (--max-width-hero), stronger title (clamp, letter-spacing), clearer CTA link weight; marquee cards with 8px radius, light shadow. **a11y:** aria-hidden on marquee (decorative); alt text placeholders for future real stories. **Pause:** Hover pauses animation; Pause/Resume button visually hidden until :focus-visible (tabbing only), aria-pressed/aria-label; control hidden when prefers-reduced-motion. Marquee items: no card shadow or border-radius (flat image + caption). why-you-care.css, _why_you_care.html. **Feature flag:** `ILGA_FEATURE_IMAGES_MARQUEE` (config.py); dev default on, prod default off; _why_you_care.html shows marquee only when `features.images_marquee` is true. |

---

## Backlog

| Area              | Summary |
|-------------------|--------|
| **White-label / bare metal (Warlord)** | **Litestream + SQLite:** Use Litestream to stream .db changes to S3 in real time (~$0.10/mo). On Vultr failure, spin up new box, restore from Litestream backup → back online in ~60s, zero data loss. Makes bare metal safe enough to sell to clients. Optional follow-up: Caddyfile for routing landofkei.com and housingrights.org to FastAPI with automatic SSL. Do when ready to white-label. |
| Trust/supporters  | When we have supporters: add "Led by [X]" or "Supported by [Y, Z]" near hero or footer. Not implemented yet — no supporters to list. |

---

## Current

| Date       | Area                         | Summary |
|------------|------------------------------|--------|
| 2026-02-28 | Git: update/welcome-email-unsub batch + PR #47 | **Batch:** Squashed 4 commits into one: feat(email,why-you-care): welcome email unsub, sign-in nudge, outline CTAs, pill icons. Welcome email: unsubscribe link, typo fix, sign-off. Why-you-care: single sign-in nudge when anon, outline CTAs, pill icons by selection, Share only when logged in. Pushed with --force-with-lease; updated PR #47 title and body from commit + TODOS. |
| 2026-02-28 | Git: feature/polls-stories commit + PR #44 | **Commit:** feat(polls-stories): admin polls, community stories/statements, why-you-care flow (43 files). **PR:** https://github.com/tyler-morales/ilga-graph/pull/44. Lint fixes: config/db_models/stories/updates/admin line length + E741 (admin options list comp). .gitignore story uploads; uploads/stories/.gitkeep. |
| 2026-02-28 | Stories/statements hardening (CSRF, email, magic-bytes) | **CSRF:** POST /community-stories, /community-statements, /admin/stories/{id}/review, /admin/statements/{id}/review now validate CSRF (token in form + cookie); 403 or redirect with flash=csrf. Hidden csrf_token in _story_submit_modal.html, _statement_submit_modal.html, admin_stories.html, admin_statements.html. **Email:** Review handlers wrap send_story_review_email/send_statement_review_email in try/except; log warning on failure, still redirect (best-effort). **Magic-bytes:** _save_story_image() verifies JPEG/PNG/WebP magic bytes match declared content-type to reject spoofed uploads. **Tests:** All story/statement/review POSTs use _data_with_csrf; added test_post_without_csrf_returns_403. |
| 2026-02-28 | Branch status report (feature/polls-stories) | **Verification:** Full test suite (425 passed). **Tests added:** test_updates.py — stories router in test app; TestCommunityStories (401 without auth, 400 without consent, 200 + pending story, admin list 200, review approve + already_reviewed); TestCommunityStatements (403 owner, 200 non-owner + pending, admin list, review approve); TestAdminPollsEdgeCases (duplicate slug 400, no options 400, results invalid id 302). Re-login as admin in tests that need both submitter and admin. **Docs:** app-overview.md — new section "Polls and community content" (admin polls, stories/statements, marquee, env ILGA_FEATURE_IMAGES_MARQUEE, ILGA_STORY_*). |
| 2026-02-28 | Admin poll creation + multi-poll | **Feature:** Admins can create and manage multiple polls. **DB:** Poll, PollOption, PollResponse (migration 20260228130000); kei poll seeded, backfill from kei_poll_responses. **Admin:** GET /admin/polls (list), GET/POST /admin/polls/new (create), GET/POST /admin/polls/{id}/edit (edit), GET /admin/polls/{id}/results (verified + all pie/table). Nav "Polls"; GET /admin/poll redirects to kei results or list. **Dashboard:** "Current poll(s)" stat (active count, total responses, link to /admin/polls). **Dual-write:** POST /updates/kei-status also inserts PollResponse for kei. **Templates:** admin_polls.html, admin_poll_form.html (options add/remove), admin_poll_results.html; _admin_poll_chart_and_table.html generalized for options. **Docs:** db-and-outreach.md updated. |
| 2026-02-28 | Share your story for everyone (owners + non-owners) | **Feature:** Every user who completes the poll sees "Share your story". Owners (registered/revoked/denied) → photo+story modal → POST /community-stories → CommunityStory → image on marquee. Non-owners (would_want/would_not_want) → text-only modal → POST /community-statements → KeiInterestStatement → text card on marquee. **DB:** KeiInterestStatement model; migration 20260228140000. **Admin:** GET /admin/statements, POST /admin/statements/{id}/review; send_statement_review_email(). **Marquee:** get_marquee_items(db) returns unified list (type=image | text); _why_you_care.html renders image figure or text div; why-you-care.css .why-you-care-marquee-item--text. **Templates:** _statement_submit_modal.html, _statement_submit_success.html, admin_statements.html; base.html opens story vs statement dialog via data-marquee-type; poll success templates show CTA for all with owner vs non-owner hint. **Nav:** _admin_nav.html Statements link. |
| 2026-02-28 | Community Stories (marquee submissions) | **Feature:** Kei owners (poll status registered/revoked/denied) can submit photo + story via "Share your story" on poll results. **Flow:** Modal (dialog) with photo upload, name, location, story (500 chars), consent checkbox; POST /community-stories (require_user); image saved to static/uploads/stories/; CommunityStory row status=pending. **Admin:** GET /admin/stories lists pending/approved/denied; POST /admin/stories/{id}/review (action=approve|deny, optional message); approval sends email and adds story to marquee; denial sends email with message. **Marquee:** get_marquee_items(db) in content.py (image items + text items from KeiInterestStatement); home and the_issue pass marquee_items. **Email:** send_story_review_email() in email_utils. **DB:** CommunityStory model; migration 20260228120000. **Templates:** _story_submit_modal.html, _story_submit_success.html, admin_stories.html; base.html includes modal + script; poll success cards show CTA for everyone (owner vs non-owner opens correct modal). **CSS:** base.css .story-submit-*, .kei-poll-story-cta; admin-dashboard.css .admin-stories-*. |
| 2026-02-28 | Poll sign-in nudge: yellow + alert icon | **UX:** .kei-poll-nudge-block now yellow/alert style: background var(--color-warning-bg), border var(--color-warning-border); alert-triangle SVG icon (aria-hidden) before copy; flex layout with .kei-poll-nudge-block__content wrapper. _kei_poll_anonymous_success.html, base.css. |
| 2026-02-28 | Git batch commit + PR #43 update | **Batch 1:** refactor: content_constants, kei_poll_context, site→home, legal→content (pyproject E501 ignore for content_constants; content.py re-exports + __all__). **Batch 2:** docs (app-overview, project-structure), templates/CSS/email tweaks, TODOS. Pushed sprint-3/campaign-updates; updated PR #43 title and body. Ready for merge; next: admin features and deeper poll integration. |
| 2026-02-28 | PR #43 merge conflicts resolved | Merged `main` into `sprint-3/campaign-updates`; kept branch Refactor/Current in TODOS, branch version of session_schedule (deadline ids), deleted _share_section*.html (macro); kept branch for all other conflicts. Push to clear GitHub PR conflicts. |
| 2026-02-28 | Kei poll: completion boost (progress, benefit, CTA) | **UX:** Step indicator (dots 1/2), benefit line, "Submit and see results" button (disabled until Q2 answered). _macros.html, base.html branch script, base.css. app-overview Key concepts. |
| 2026-02-28 | Poll: show results when already voted | **UX:** If user has completed the kei poll (logged-in or anonymous), home and /updates?prompt=kei show results only, not the form. **Backend:** Cookie `kei_poll_voted=1` set on anonymous submit (1-year); `get_kei_poll_initial_state(request, user, db)` returns show_results from user.kei_status or cookie. Home and updates routes pass poll state; templates use `kei_poll_done` to include _kei_poll_initial_results.html (wrap + logged-in or anon success partial). **Tests:** test_kei_status_anonymous_sets_voted_cookie, test_updates_page_shows_poll_results_when_cookie_set, test_updates_page_shows_poll_form_when_no_vote. |
| 2026-02-28 | Poll: dynamic message when signed in | **Fix:** When user signs in from the poll card (anonymous → verify), the card no longer shows "Your response isn't counted yet." **Backend:** GET /updates/kei-poll-results?poll_id=… (require_user) returns _kei_poll_logged_in_success.html with user.kei_status and results. **JS:** hero-auth-strip.js after verify success in poll context: find .kei-poll-card wrap, fetch kei-poll-results, set wrap.innerHTML and htmx.process(wrap). User sees "Thanks for sharing." and no sign-in nudge. updates.py, hero-auth-strip.js. |
| 2026-02-28 | Poll inline sign-in + sign up for updates | **Anonymous poll results:** Sign-in is in-component (click "Sign in" → inline email/code form). Verify with `from_poll=1` defers welcome email. After sign-in, card is replaced with logged-in results view (see "Poll: dynamic message when signed in"); sign-up prompt removed with that swap. Template: _kei_poll_anonymous_success.html (Sign in button, .poll-signup-prompt). Auth: verify_code accepts from_poll, skips welcome when set. Updates: subscribe_post sends welcome when not sent, returns HTML for HX-Request. JS: hero-auth-strip.js adds from_poll to verify in poll context. **UX:** Sign-in nudge is a prominent CTA block (.kei-poll-nudge-block): light blue strip, primary button "Sign in to save your answer"; init on DOMContentLoaded and htmx:afterSwap so flow opens in card. **Container query:** .hero-signin-inline uses @container signin-inline (min/max 400px) for layout; #auth-strip and .kei-poll-signin-wrap have container-type: inline-size + container-name: signin-inline; narrow = stacked/full-width, wide = centered. advocacy-form.css, base.css. |
| 2026-02-28 | Kei poll: two-step branch (have kei vs don’t) | **UX:** Poll is now two quick questions: (1) “Do you have a kei vehicle?” Yes/No; (2) If Yes → “What’s your registration status?” (Registered / Had one; registration was revoked / Was denied registration); if No → “Would you want one?” (Yes, I’d want one / No, I wouldn’t want one). Same 5 slugs and result buckets; backend unchanged. **Macro:** kei_poll step-1 branch buttons + step-2 panels; JS in base.html (initKeiPollBranch on load + htmx:afterSwap) shows/hides panel and clears step-2 selection when branch changes. **CSS:** .kei-poll__step1, .kei-poll__step-legend, .kei-poll__branch-btn, .kei-poll__step2-panel, .kei-poll__step2-panel--hidden. **Docs:** glossary.md State of kei updated. |
| 2026-02-28 | Kei poll UI: card layout, Submit, results state | **Poll state:** Card (white, rounded, shadow); question, radio options, footer with "Total Votes: N" (or — when unknown) and Submit button (not "Vote"). **Results state:** Same card; question, horizontal bars with "X% Label" and green bar for highest %, blue for others; checkmark next to user's choice; footer "Total Votes: N" only; "Need to change your answer?" kept. **Removed:** Any "time left" copy. **Backend:** GET /updates/kei-poll-form passes kei_status_total; POST success templates pass kei_status_selected to chart macro. **Macro:** kei_poll(..., kei_status_total); kei_poll_results_chart(..., selected_slug). **CSS:** .kei-poll-card, .kei-poll__footer, .kei-poll-chart__bar--highest (green), --other (blue), .kei-poll-chart__check. base.css, _macros.html, _kei_poll_* success templates, updates.py. |
| 2026-02-28 | Kei poll: change your answer | **Change answer:** After submitting the kei poll, success view shows a small link "Need to change your answer?" that loads the poll form again via GET /updates/kei-poll-form?poll_id=… and swaps it in (outerHTML on the poll wrap). Form includes hidden `poll_id`; POST passes it to success templates so the button targets the correct wrap. New template _kei_poll_form_partial.html; CSS .kei-poll-change-wrap, .kei-poll-change-btn.link-style in base.css. Test: test_kei_poll_form_get_returns_form_html. **Fix:** After outerHTML swap the HTMX target is the removed node; re-init kei poll branch from `document` in htmx:afterSwap so the newly inserted form gets Yes/No panels and radios (value only updates on new POST). |
| 2026-02-28 | Kei poll: results chart after submit | **Chart:** After user submits kei poll choice, success view shows a horizontal bar chart of results (label, bar by %, count). Macro `kei_poll_results_chart` in _macros.html; used in _kei_poll_anonymous_success.html and _kei_poll_logged_in_success.html. CSS: .kei-poll-chart__* in base.css. Replaced list-only results with chart; a11y: list structure + aria-label on chart, bar-wrap aria-hidden. |
| 2026-02-28 | Poll sign-in nudge: prominent CTA (hardballer) | **UX:** Anonymous poll results nudge no longer inline text + link; now a distinct CTA block: .kei-poll-nudge-block (light blue bg #e8f0fe, border, padding), copy "Your response isn't counted yet. Sign in to save your answer to the results.", full-width primary button "Sign in to save your answer". Removed .kei-poll-nudge / .kei-poll-signin-link; button keeps .auth-strip-signin-btn for JS, gets primary styling in block. _kei_poll_anonymous_success.html, base.css. |
| 2026-02-28 | Kei poll: removed "Save your choice" button | **Removed:** "Save your choice" button from poll results chart (anonymous view). Chart still shows "Your choice" badge with hover "Sign in to save your choice"; header CTA "Sign in to save your answer" remains. **Dead code:** _macros.html (button block); base.html initPollSignin trigger delegation (`.kei-poll-trigger-signin` → #poll-signin-btn); base.css .kei-poll-chart__your-choice and button variants + responsive override. |
| 2026-02-28 | Kei poll: verified badge above percent (no row highlight) | **Simplified:** Removed full-row styling for user's choice. Selected choice now shows a small "Verified" icon + text above the percent only. **Macro:** Dropped `kei-poll-chart__row--selected`; added `.kei-poll-chart__pct-wrap` with optional `.kei-poll-chart__verified-badge` (checkmark SVG + "Verified") above `.kei-poll-chart__pct`; kept "Save your choice" button for anonymous. **CSS:** Removed .kei-poll-chart__row--selected; added .kei-poll-chart__pct-wrap, .kei-poll-chart__verified-badge, .kei-poll-chart__verified-icon (green #059669, small uppercase). _macros.html, base.css. **Refined:** "Verified" only when signed in; anonymous sees "Your choice" with .kei-poll-chart__verified-badge--pending (opacity 0.55) until they sign in/up. |
| 2026-02-28 | Kei poll results section cleanup | **Layout:** Chart macro: label row split into .kei-poll-chart__pct, .kei-poll-chart__label-text (flex: 1, wraps), .kei-poll-chart__check (fixed right). Bar wrapper changed to div for semantics. **CSS:** List uses gap 0.75rem for row spacing; label/text/check flex with min-width 0 so long labels wrap cleanly; bar track 6px height, #e5e7eb; pct min-width 3em, subtle color; check margin-left. _macros.html, base.css. |
| 2026-02-28 | Kei poll chart: single column (no two-column) | **Layout:** Option label text (e.g. "I have a kei (registered)") must sit under the percent; "Your choice" badge must not create a two-column layout. **CSS:** .kei-poll-chart__label now flex-direction: column (badge + percent in .kei-poll-chart__pct-wrap, then .kei-poll-chart__label-text below). .kei-poll-chart__pct-wrap min-width removed. Removed redundant sidebar-poll override for .kei-poll-chart__label. base.css. |
| 2026-02-28 | 500 on GET /: share_buttons script in _macros.html | **Cause:** Inline script in share_buttons macro was corrupted (broken getElementById, incomplete Jinja vars, mangled strings) → TemplateSyntaxError. **Fix:** Restored script from git (copy-to-clipboard + Web Share API) so Jinja expressions and JS are correct. _macros.html. |
| 2026-02-28 | Mitigate template/macro corruption (future) | **Tests:** tests/test_templates.py — (1) parametrized compile test for _macros.html and home.html (catches Jinja TemplateSyntaxError in share_buttons or any included macro); (2) share_buttons macro render test with minimal context. Run with `make test` or `PYTHONPATH=src pytest tests/test_templates.py`. Add other fragile templates to TEMPLATES_USING_SHARE_BUTTONS. **Optional later:** move share script to static JS + data-attrs to reduce inline Jinja/JS surface. |
| 2026-02-28 | Why you care: spacing and poll column | **Spacing:** Removed top padding from both grid columns (reasons + poll) and from .why-you-care-teaser-list (explicit padding: 0 0 0 1.5em). Increased .why-you-care-home-grid margin-top to 3rem to push columns below the section header. **Poll column:** align-self: center in CSS (mobile + desktop) so poll card is centered; no inline style. why-you-care.css. |
| 2026-02-28 | Home: poll in Why you care, two-column | **Layout:** Kei poll in "Why should you care?" on home; two-column: poll (left), reasons + CTA (right); stacks on mobile. **Title:** "Why should you care?" lives over the list (right column) only, not over the poll. **Poll:** Skip button removed from kei_poll macro (footer + home); removed Skip click/sessionStorage logic and .kei-poll__skip CSS. _why_you_care.html, _macros.html, base.html, base.css. |
| 2026-02-28 | Why you care: section title + subtitle | **Home:** "Why should you care?" moved to top of section and center-aligned. New smaller subheading "Three reasons" above the numbered list (content.py WHY_SHOULD_YOU_CARE_TEASER_HEADING). CSS: .why-you-care-title--section (text-align center), .why-you-care-subtitle (1.125rem/1.2rem, font-weight 600). Template: section title above two-col-inner; reasons column has h3.why-you-care-subtitle + list + CTA. _why_you_care.html, why-you-care.css, content.py. |
| 2026-02-28 | Poll tracking + admin results | **DB:** New table `kei_poll_responses` (id, user_id nullable, session_id nullable, kei_status, created_at); migration 20260228110000. **Write:** POST /updates/kei-status always inserts one row; if logged-in also sets User.kei_status. Optional form param session_id for anonymous. **Admin:** GET /admin/poll (nav "Poll") shows verified-only (from User) and all-responses (from kei_poll_responses), each with pie chart (CSS conic-gradient) + table toggle. Templates: admin_poll.html, _admin_poll_chart_and_table.html; CSS in admin-dashboard.css. **Docs:** db-and-outreach.md, glossary.md updated. |
| 2026-02-28 | Poll DB verification (dev + prod) | **Verified:** Migration 20260228110000 in chain (down_revision 20260228100000); init_db() runs Alembic on startup so kei_poll_responses exists; fallback create_all() also creates it (model on Base.metadata). **Docs:** db-and-outreach.md — added kei_poll_responses to tables checklist; new checklist item "Poll record + display" for dev (make dev + submit poll + SELECT) and prod (same ILGA_DB_PATH, migrations on startup or run manually); test list includes test_updates.py for poll. |
| 2026-02-28 | State of kei + welcome email | **State of kei:** User.kei_status (nullable), KEI_STATUS_OPTIONS/SLUGS in constants.py. POST /updates/kei-status: **logged-in** → persist kei_status; **anonymous** → no DB write, return thanks + results + nudge + inline sign-in (vote not counted). **Poll counts only verified users** (last_login_at IS NOT NULL). GET /updates/kei-status-results returns by_status/total_responses for verified users only. Poll UI: footer (base.html), home, /updates?prompt=kei; macro kei_poll (no email field); success fragments _kei_poll_logged_in_success.html, _kei_poll_anonymous_success.html with results + inline_signin macro; htmx:afterSwap inits poll sign-in. **Reusable inline sign-in:** macro inline_signin(id_prefix) in _macros.html; hero-auth-strip.js initInlineSignin(container) for hero and poll fragment. **Welcome email:** send_welcome_email() in email_utils; trigger on first sign-in; User.welcome_email_sent_at set after send. Migration 20260228100000. Tests: test_kei_status_authenticated_sets_status, test_kei_status_anonymous_does_not_persist, test_kei_status_results_only_verified_users. |
| 2026-02-27 | Domain glossary single source | **Glossary:** `DOMAIN_GLOSSARY` in content.py (after SESSION_SCHEDULE_TERMS) — ~25 terms with id, term, definition, category; optional context, see_also, not. Categories: advocacy, legislative, product, data. Single source for campaign vs advocacy-effort, milestone vs session-milestone, phase, master-timeline vs progress-checklist, etc. **Docs:** docs/reference/glossary.md (human-readable); app-overview.md Key concepts → Terminology bullet + link to glossary. **Renames:** CAMPAIGN_TIMELINE_CHECKPOINTS → PROGRESS_CHECKPOINTS, CAMPAIGN_TIMELINE_ACHIEVED_COUNT → PROGRESS_ACHIEVED_COUNT; template var campaign_timeline → progress_checklist; /updates CSS .campaign-timeline → .progress-checklist (and __item, __num); aria-label "Progress toward Keis be legal". content.py, updates.py, updates.html, base.css, test_updates.py. |
| 2026-02-28 | Session FAQ: tooltips, no terms block, styling | **The Issue** session calendar FAQ: (1) Removed "Terms used in this calendar" section; definitions live in /glossary; terms in answer and table descriptions get inline tooltips via `_faq_session_and_deadlines_with_tooltips()`. (2) Styling: more spacing (brief-faq margin/padding, list gap, answer padding), line-height 1.6, session-deadlines table with border-collapse, cell padding, Date/Chamber columns nowrap + min-width so no awkward wrapping. content.py, the_issue.html, base.css. Tests: test_faq_session_and_deadlines_with_tooltips_*. |
| 2026-02-28 | All FAQs: unified brief-faq styling | **FAQ layout:** The Issue (faq_law, faq_advocacy, faq_session), Legislator brief, and Fact sheet now all use the same `.brief-faq` component: `.brief-faq__answer-text` on answer paragraphs, details/summary pattern, same spacing/typography. Fact sheet "Key FAQs for volunteers" refactored from dl/dt/dd to brief-faq (details/summary, optional answer_list and sources). Removed redundant .fact-sheet-faq-list, .fact-sheet-faq-item CSS. the_issue.html, legislator_brief.html, fact_sheet.html, base.css. |
| 2026-02-28 | Admin: top legislators as bar chart | **Admin dashboard:** "Top legislators by contact volume" is now a horizontal bar chart (name, bar scaled to max, count). Template: admin_dashboard.html — figure + ul.admin-dashboard-contact-chart, each row grid (name, bar-wrap, count); CSS admin-dashboard.css .admin-dashboard-contact-chart__*; a11y: role="list", aria-label on bar-wrap, aria-hidden on count. Replaced bullet list; removed .admin-dashboard-top-members-list and -id/-count. |
| 2026-02-28 | Timeline: key session dates in drawer | **Key session dates:** Table moved under a collapsible drawer (details/summary "Show session dates table"); table uses shared `.brief-faq__table.session-deadlines-table` and cell classes (__date, __chamber, __desc) like The Issue. Removed duplicate `.timeline-page__deadlines-table` CSS; drawer styling in timeline.css (summary chevron, focus-visible). timeline.html, timeline.css. |
| 2026-02-28 | Timeline: waterfall (Gantt) | **Redesign:** Timeline is a waterfall (Gantt-style) table — time axis by month, one row per phase with bar spanning start–end. Template: .timeline-waterfall scroll container, table with sticky phase column; backend _timeline_waterfall_data() for months + start_col/end_col; block-y bars, horizontal scroll, responsive. Replaced cards and track. |
| 2026-02-28 | Timeline: month cells open phase, offset details | **Month as title:** Month header cells are clickable; clicking a month opens the phase that spans that month and shows its summary + milestones. **Offset:** Opened phase details text is padded so it aligns horizontally with the clicked month column (measured via getBoundingClientRect). **Context:** "For [month]" line at top when opened from month; cleared when opened from phase label or bar. Phase rows have data-start-col/data-end-col for JS; aria-label updated to "Click a month or a phase to show that phase's details." timeline.html, timeline.css. |
| 2026-02-28 | Timeline: scroll opens phase by date | **Load:** Current phase (from server `current_phase_id`) opens automatically; container scrolls so that phase's start month is in view. **Scroll:** On horizontal scroll (debounced 120ms), the phase whose bar spans the month at viewport center opens; others close. Same-phase scroll updates the "For [month]" context and sticky offset. data-current-phase-id on container; getMonthIndexAtViewportCenter, getPhaseIdForMonthIndex, syncOpenPhaseToScroll. Intro and aria-label note that the visible phase opens automatically. timeline.html. |
| 2026-02-28 | Timeline: phase definition block when opened | **Phase styling:** When a phase is opened, the details row shows a distinct phase block (name + date range + definition text from phase.summary) so the phase reads as a section, not like a timeline item. Template: `.timeline-waterfall__phase-definition` with heading (label + dates) and definition text; milestones list below. CSS: left border, light teal tint. timeline.html, timeline.css. |
| 2026-02-28 | Timeline: phase definition in label cell (white area) | **Placement:** Phase definition (summary text) moved from details row into the phase row’s label cell, literally under the phase name in the waterfall’s white/sticky column. Shown only when phase is expanded; JS toggles `phase-definition-{id}` hidden. Details row now only has optional “For [month]” context + milestones. Removed unused phase-definition-heading/label/dates CSS. timeline.html, timeline.css. |
| 2026-02-28 | Timeline: phase definition in details row under phase name | **Row split:** Phase definition no longer in same row as phase name/bar. It lives in the details row: first cell = label column (phase definition only, aligns under phase name); second cell = milestones. So phase row = name + bar only; details row = definition in left column + milestones in rest. Removed phase-definition from phase row and JS show/hide; details row now has two cells (details-label-cell + details-cell). timeline.html, timeline.css. |
| 2026-02-28 | Timeline: phase description style match | **Styling:** Removed banner styling from phase definition (left border, teal tint background). Phase description now plain text matching milestone/body style (font-size sm, color text-muted). timeline.css. |
| 2026-02-28 | Public glossary page (/glossary) | **Route:** GET /glossary in content router; template glossary.html. **Data:** DOMAIN_GLOSSARY (app/organizational terms), KEI_GLOSSARY (kei vehicle & policy terms), SESSION_SCHEDULE_TERMS. Sections: Kei vehicles & policy, Session calendar & deadlines, Terms used on this site (advocacy, legislative, product). Meta terms (Target, Progress, Action) removed for digestibility. **Kei terms:** Kei vehicle, Kei class, 25-year rule, Highway-built, 625 ILCS 5/3-401(c-1), Shaken, One-pager — from canonical content only. Footer nav + sitemap; docs/reference/glossary.md updated; base.css .glossary-* styles. **Source on page:** Terms with optional `source` (and `source_url`) show “Source: …” on the glossary; KEI_GLOSSARY entries use “Canonical content (STRATEGIC_*, FAQ_*, briefs)”, 625 ILCS term links to ILGA; SESSION_SCHEDULE_TERMS use “ILGA rules (104th GA)”. .glossary-source CSS. |
| 2026-02-28 | Home 5-points cards: full width on mobile | **Bug:** "Our message in 5 points" cards were not full width on mobile (centered, side margins). **Fix:** strategic-points.css — below 900px (and in container query when section is narrow): .strategic-points-rows width 100%; .strategic-points-row align-items stretch, width 100%; .strategic-points-card width 100%, max-width none. Desktop (≥900px) unchanged: 2+3 grid, cards max-width 28rem. |
| 2026-02-28 | Inline glossary tooltips (The Issue, Timeline, legislator brief) | **Backend:** `_inline_glossary_terms(include_domain)`, `apply_inline_glossary(blocks, terms)` in content.py; first occurrence of each term replaced with button+popover (KEI_GLOSSARY, SESSION_SCHEDULE_TERMS; Timeline also DOMAIN_GLOSSARY). **Pages:** The Issue (intro_html, paragraphs_html, bullets_html, strategic_five_points_html); Timeline (label_html, summary_html, title_html, description_html; session_deadlines + session_label); Legislator brief (issue_one_sentence_html, core_ambiguity_html, paragraphs_html). **Frontend:** base.html uses Tippy.js (theme `glossary`) for .glossary-inline-term — click/focus, interactive, appendTo body, singleton (one at a time); Escape to close and other Tippy behavior from library. base.css: .glossary-inline-term (dotted underline, cursor help, focus-visible); inline .glossary-inline-def hidden (content cloned into Tippy); .tippy-box[data-theme="glossary"] max-width min(32rem, 92vw) for readable definitions. **Docs:** glossary.md inline definitions note; single source in content.py. |
| 2026-02-27 | Timeline: current phase full opacity | **Timeline page:** Only the current phase's circle has full opacity; other phases' nodes use opacity 0.45. content.py: TIMELINE_PHASES now include start_date/end_date (YYYY-MM-DD); _current_timeline_phase_id() returns phase id for today (or first/last if before/after range). Template adds campaign-timeline__phase--current when phase.id == current_phase_id; timeline.css .campaign-timeline__node default opacity 0.45, --current overrides to 1. (Superseded by waterfall: current phase row gets .timeline-waterfall__phase-row--current.) |
| 2026-02-27 | Timeline page (/timeline)     | **2027 campaign master timeline** — waterfall (Gantt-style) from Feb 2026 through bill signed. Route GET /timeline; template timeline.html; CSS timeline.css (horizontal scroll, time axis by month, phase bars, sticky phase column). Data: content.py TIMELINE_PHASES (Build, Intro, Committee & floor, Governor); _timeline_waterfall_data() for months + column indices. Footer nav: Timeline link. Update TIMELINE_PHASES when 105th GA schedule is published. |
| 2026-02-27 | Fact sheet: remove Supporters section | Removed Supporters block from /fact-sheet (template + content.py). Deleted FACT_SHEET_SUPPORTERS_PLACEHOLDER; updated no-hallucination rule and docs/canonical/README.md. |
| 2026-02-27 | Sprint 3: ED / coalition dashboard (gaps) | **Admin dashboard** at `/admin` is the single “status of the advocacy effort” view (Hardball-style). **Added:** (1) **Outreach trend** — last 7 days and last 30 days (calls + emails) in a small block under Outreach mix. (2) **Last update sent** — date and title of most recent sent update in Email section (or “No updates sent yet”). (3) **Active campaign** — already had title and action count; now also shows **end date** (or “No end date”). (4) **Top legislators by contact volume** — top 5 member_ids by call+email count from OutreachEvent. Backend: admin.py `_outreach_volume_for_window`, `_latest_sent_update`, `_top_members_by_outreach_count`; template and admin-dashboard.css for trend, email-last-sent, top-members. Docs: app-overview, db-and-outreach. |
| 2026-02-27 | Share section redesign        | **Home:** Share block is a card (bg, border, 10px radius, light shadow) so it reads as a clear CTA. **Buttons:** Copy link = primary (hero-cta orange, white text, hover lift); Share = secondary (outline). Inline SVG icons (link, share-arrow) replace emoji; .share-section-btn-icon for sizing. **Sidebar:** Same button styles (compact min-height/padding); no card. **a11y:** focus-visible rings, aria unchanged; copy feedback and [aria-pressed] for "Copied!". share-section.css, _share_section.html, _share_section_sidebar.html. |
| 2026-02-27 | Subscribe copy (no spam tone) | Mission CTA and Updates page: "Get one update when we move—no spam, only when it matters." Replaced "when the bill moves" with "when we move"; note line on updates: "One email when we move." _mission_cta.html, updates.html. |
| 2026-02-27 | Email list building (Hardball) | **Subscribe-email endpoint:** POST /updates/subscribe-email (public, no auth code); validates email, get-or-create User with wants_updates=True; HTMX returns success/error fragment, else redirect /updates?subscribed=1. **Drawer:** Call drawer wrap-up shows "Stay informed" block (email + Subscribe) after poll/draft CTA; done-view template has subscribe block (form or "You're on the list" when signed in); htmx.process(body) after done view. **Standalone forms:** /updates (inline email form for signed-out users), The Issue sidebar "Stay informed", footer "Get updates" — all POST to subscribe-email, swap innerHTML with success. **Home CTA:** Mission CTA block (_mission_cta.html) now offers "Not ready to call or email? Get one update when the bill moves." with inline email + Subscribe (same endpoint); .home-cta-subscribe* in mission-statement.css. **Auth strip:** Hero copy "Get notified when the bill moves" (home + advocacy). **Update email:** Forward to a friend link (mailto: with subject/body + updates_url). CSS: drawer-subscribe*, footer-get-updates, stay-informed, updates-subscribe-email, .visually-hidden in base.css; advocacy-drawer.css drawer-done-subscribe. |
| 2026-02-27 | Session schedule single source of truth | **ILGA session schedule:** `reference/session_schedule.json` is the single source for House/Senate 104th GA Spring 2026 dates and deadlines. Loader: `session_schedule.py` (load_schedule, get_all_deadlines, get_events_by_chamber, next_deadline_on_or_after, session_label). The Issue: FAQ "Session calendar & deadlines" + key-deadlines list from schedule. Docs: docs/canonical/README.md §7, docs/advocacy/focused-next-steps (timeline row), docs/reference/project-structure.md reference/. Tests: test_session_schedule.py. |
| 2026-02-27 | Footer last updated: auto from git | **Footer "Last updated"** stays accurate in dev and prod. When `ILGA_FOOTER_LAST_UPDATED` / `ILGA_FOOTER_LAST_UPDATED_ISO` are unset, config derives date from last git commit (`_footer_last_updated_from_git()`); prod deploy does `git pull` so date reflects deploy. Set both env vars to override. config.py; tests/test_config_footer_date.py; docs/features/app-overview.md. |
| 2026-02-27 | Session pill (top-left, all public pages) | **Session pill:** Collapsed: slow blink dot + "ILGA Session 2027"; expanded: full hero_urgency_line (content.py). **Animation:** Opacity-only crossfade in overlay slot (like fire pill); one width transition on .session-pill-text-slot; no competing max-width so no chop. _session_pill.html, base.css .session-pill*, .session-pill-text-slot. Copy alternatives noted in content.py HERO_URGENCY_LINE comment. |
| 2026-02-27 | Goal section + campaign language | **Goal section** aligned with campaign and site copy: "Outreach" → "Contact" (Contact your district legislators, Contact the Power Broker). Heading shows "This week's goal" when active_campaign present, else "Your goal". Template: _results_partial.html (heading, default label, completed summary, aria-labels, data-goal-label-broker). Backend: advocacy.py current_goal_label strings. JS: index.html uses data-goal-label-broker for broker-phase label; drawer "You've contacted your district members", aria "Contact steps" / "Contact: call script and email template". advocacy-cards.css comment updated. |
| 2026-02-27 | Advocacy sidebar: remove "How we pick targets" | Removed "How we pick targets" &lt;details&gt; block from advocacy results sidebar (_results_partial.html). Deleted related CSS: .how-it-works*, .hiw-*, .moneyball-aside-trigger, .moneyball-definition-block; .results-aside .how-it-works in media query (advocacy-cards.css). Sidebar now shows Your goal (and "Who is the Power Broker?" when in broker phase) then My Outreach when signed in. |
| 2026-02-27 | Hero ticker: total outreach  | **Hero ticker:** "Illinois constituents have done X outreach actions." on both home and advocacy heroes (_hero_home.html, _hero_advocacy.html). Backend sends total actions (calls + emails) as calls_total. |
| 2026-02-27 | Hardball hero + conversion   | **Hero (no redesign):** Urgency line from content.py (HERO_URGENCY_LINE: next session early 2027; HERO_URGENCY_THIS_SESSION for Spring-through-May-31); clarity line "Enter your ZIP — we'll show you who to call and what to say. Takes 2 min."; ticker "X total outreach so far (calls and emails)" / "Join others. Enter your ZIP to start."; .hero-urgency, .hero-clarity in advocacy-form.css. **Drawer:** Replaced "One bubble at a time…" with "Call this number. Here's the script. Takes 2 min." (_advocacy_drawer_call.html + index.html introCall). **Sign-up:** Updates page subscribe copy "Get one update when the bill moves." **Primary CTA:** Unchanged (Start outreach; Learn/Read below form on home). content.py, main.py globals, _hero_advocacy.html, _hero_home.html, updates.html. **Removed from advocacy hero:** Urgency line ("Next session starts early 2027…") and clarity line ("Enter your ZIP — we'll show you who to call…") — subhead goes straight to ticker + form. |
| 2026-02-27 | Advocacy: district phone first | **Phone in drawers/cards:** Prefer district office phone; fallback to Springfield (or first office with phone). `get_preferred_phone_for_member()` in advocacy_helpers; used by drawer (advocacy.py) and member cards (member_to_card). Tests: test_advocacy_phone_preference.py. No new scrape — ILGA data already has both offices. |
| 2026-02-27 | 500 error page (Kei images)  | **500.html** aligned to 404: same layout with random Kei vehicle fact + image, figcaption, "Fun fact" text, and refresh button; added nav links (Home, Advocacy). **main.py:** `_error_page_context(request)` returns request + random `kei_fact`; 404 uses it via `_404_context`; HTTP 5xx and uncaught-exception handlers pass `_error_page_context(request)` to 500.html so the page shows a random Kei image and fact. |
| 2026-02-27 | Public pages audit           | Copy and consistency: fixed hero eyebrow "This week's priority" (_hero_advocacy.html); legislator brief CTA "Contact Tyler Morales" (was "point of contact"); legislator brief page title "Legislator brief" (was "Brief for legislators"); Coalition page main heading "Supporting legislators" + intro line for recognized supporters; fact sheet "Key FAQs for volunteers"; Updates "Sign in on the Advocacy page" to subscribe. Coalition left out of footer nav per plan (no people to list yet). |
| 2026-02-27 | make dev timeout (ERR_CONNECTION_TIMED_OUT) | **Cause:** Worker restarted by reload when debug log wrote to .cursor/; then lifespan blocked in init_db (Alembic migration). **Fix:** (1) `make dev` runs `alembic upgrade head` first (same env as app) so worker’s init_db is a no-op; (2) `--reload-dir src` so only src/ triggers reload; (3) debug log moved to /tmp so writes don’t trigger reload. |
| 2026-02-27 | Updates: Where we are copy + spacing | **Redundancy:** Banner (inline) and CAMPAIGN_STATUS paragraph said the same thing. **Change:** CAMPAIGN_STATUS shortened to one line ("We're in the outreach phase."); banner carries detail + CTA. **Spacing:** .current-action-banner--full margin-bottom 1.5rem; .updates-campaign-status margin-top 1.25rem. content.py, base.css. |
| 2026-02-27 | Updates: remove old current-action block | Removed legacy light-blue "Current action" section (`.updates-current-action`) from updates.html; only the teal _current_action_banner.html (inline slot) remains. Deleted unused `.updates-current-action*` CSS from base.css. |
| 2026-02-27 | Action banner matches beta; full vs short | **Action banner** uses dedicated campaign palette (teal: #e6fffa → #99f6e4 gradient, #0d9488 border, #0f766e pill/link/dismiss); same layout as beta: left emoji (📢) + ACTION pill, centered message, dismiss right. **Two versions:** Short (all pages): title + "Contact your rep" link; full (Updates page only): title + message + link. **Template:** _current_action_banner.html uses `current_action_banner_slot`: `inline` = full, `top` = short. **Layout:** base.html wraps banner in `{% block current_action_banner %}`; updates.html overrides block empty and includes full banner inline in content so only one banner on Updates. CTA link kept on both; sessionStorage dismiss per slot. |
| 2026-02-27 | Robust banner component: same layout, Updates full card | **Slim bars (beta + action):** Same layout — emoji + pill left, **centered** message, **dismiss (×) right**; padding 10px 32px; `justify-content: center`; dismiss `position: absolute` right. Action banner had no × and used `justify-content: flex-start`/`text-align: left`; fixed. **Action dismiss:** Added `.current-action-banner-dismiss` + sessionStorage script in base.html (key from `data-dismiss-key`); `.current-action-banner--dismissed`. Pill "ACTION" (uppercase); beta pill "BETA" for consistency. **Updates page:** Replaced inline slim bar with **full campaign banner** (_campaign_banner.html): card with eyebrow, title, message, CTA (design user preferred). docs/reference/performance-resilience.md: "Site banners" section documents slim bar + full card. |
| 2026-02-27 | Priority callout on Updates | **Callout component:** Updates "Where we are" block is now a **priority callout** (banner-like but in-content): teal palette and 4px left border like current-action banner, "PRIORITY" pill + eyebrow, title, message, underlined link CTA (not btn-primary). New `_priority_callout.html`, `.priority-callout` CSS in base.css. updates.html includes _priority_callout.html instead of _campaign_banner.html. _campaign_banner.html and .campaign-banner retained for potential Advocacy use. Docs: performance-resilience.md "Site banners" updated; TODOS Refactor/Current. |
| 2026-02-27 | Priority card dynamic (logged-in outreach) | **Updates priority callout** shows **what you did** + **next step from advocacy**: (1) Status message: "You've completed X of Y steps.", "You've contacted your district legislators.", or "You've contacted your rep — thank you!" when all done; (2) CTA is the actual next step (e.g. "Next: Call your Senator", "Next: Email your Rep", "Next: Call Power Broker") from same logic as advocacy page (district steps then Power Broker when district complete). Link goes to /advocacy?zip=… when user has ZIP. `_get_priority_card_status()` in routers/updates.py uses advocacy_helpers.find_power_broker and same district/broker step order; _priority_callout.html uses status_message, cta_text/cta_href, goal_complete for "Next:" prefix. |
| 2026-02-27 | Branch bug-check + lint (sprint-2/campaigns) | Ruff: removed unused imports (main.py json/time; campaigns get_active_campaign; test_campaigns CSRF_COOKIE_NAME/generate_csrf_token); E501 line length (main, middleware, advocacy, auth, startup, test_campaigns); I001 import order (campaigns, startup, test_campaigns); auth log block switched to json.dumps. All 372 tests pass; ruff check clean. |
| 2026-02-27 | Sprint 2: Campaigns & contact periods | **Data:** Campaign model (title, message, ask, target_type all\|by_district, target_member_ids/target_district_ids JSON, is_active, start_at/end_at); OutreachEvent.campaign_id (nullable FK). Alembic 20260227000000. **Helpers:** campaign_helpers.get_active_campaign, is_campaign_visible_to_zip (ZIP→district), campaign_outreach_count, deactivate_other_campaigns. **Admin:** GET/POST /admin/campaigns, /admin/campaigns/new, /admin/campaigns/{id}, activate/deactivate; admin_campaigns.html, admin_campaign_form.html; nav link. **Advocacy:** "Current action" banner (_campaign_banner.html) when active campaign visible (all or by_district+ZIP); CTA focuses ZIP input; active_campaign in advocacy_index context. **Current action bar:** New _current_action_banner.html — same structure as beta banner (pill, text, optional CTA link, dismiss), campaign-effort styling (primary blue, left accent), sessionStorage dismiss; included at top of advocacy index and /updates when active_campaign present. **Outreach:** record_outreach accepts campaign_id, validates against active campaign, stores on OutreachEvent; client passes campaign id from __ILGA_ACTIVE_CAMPAIGN. **Updates:** Current action card on /updates with link to /advocacy when campaign active. **Reporting:** Admin campaign detail shows outreach count/breakdown by kind; admin dashboard "Current action" card (name + count or "No active campaign"). **Tests:** test_campaigns.py (visibility, get_active_campaign, count, deactivate, record with/without campaign_id); test_db outreach_events schema includes campaign_id. Hardball: "Call on them at crucial moments"; "don't cry wolf"; targeted segments (by_district). |
| 2026-02-27 | Docs: campaigns and admin | **app-overview.md:** Admin row now includes Current action card and Campaigns (list, new, edit, activate/deactivate, outreach count/breakdown). New Key concept **Campaigns (contact periods)** — one active campaign, visibility by all or by_district, current-action bar and priority callout, goal heading "This week's goal", outreach attribution, Hardball alignment; link to performance-resilience.md Site banners. **db-and-outreach.md:** Components table adds Campaign model and OutreachEvent.campaign_id; Outreach write row notes optional campaign_id; write-path bullet explains campaign validation and storage. |
| 2026-02-26 | Pitch one-pagers             | **docs/pitch/:** Four audience one-pagers (Markdown): advocacy-nonprofit, lobbyist, candidate, investor. Hardball tone, tactful and genuine; consistent structure (problem, why it works, what you get, proof, CTA). docs/index.md links to pitch/. Optional later: README in pitch/, PDF export. |
| 2026-02-26 | Branch bug-check + PR batch  | Lint pass: E501/I001 fixed in config, email_utils, updates, test_updates; ruff format. All tests pass (352 passed, 10 skipped). Git: squashed main..HEAD into one commit; force-pushed; PR #41 title/body updated from TODOS + commit. |
| 2026-02-26 | Sprint 1 loose ends: static/updates | **Policy:** Campaign update images (admin uploads at compose) are not committed. `.gitignore`: `static/updates/*.jpg`, `*.png`, `*.webp`; `!static/updates/.gitkeep` so the directory exists on clone. Migrations (update_type, image_path) already in branch. |
| 2026-02-26 | Where we are: campaign timeline | **/updates:** Under "Where we are" text, added a campaign timeline: checkpoints from outreach to "Keis be legal" (content.py: CAMPAIGN_TIMELINE_CHECKPOINTS, CAMPAIGN_TIMELINE_ACHIEVED_COUNT). Achieved steps at 100% opacity, pending at 0.45. Update checkpoints and achieved count in content.py as the campaign advances. Template: updates.html campaign-timeline list; CSS: base.css .campaign-timeline; tests: test_updates_page_shows_campaign_timeline; docs: app-overview.md. |
| 2026-02-26 | Email robustness (updates/campaign) | **Bug check:** (1) Skip users with blank/whitespace email in send loop and _send_to_users; (2) Set update email image_url only when APP_BASE_URL is set (avoid broken relative URLs in mail); (3) email_utils.send_email validates recipient (non-empty, contains @, ≤320 chars) and skips when SMTP configured but SMTP_FROM empty; (4) _send_to_users wraps per-user token/render/send in try/except so one failure doesn't prevent commit; run_send_loop already did per-user try/except. **Tests:** TestEmailRobustness (send_email invalid recipient returns False; user with blank email skipped; partial send still commits sent_at/sent_count); test_render_update_email_html_no_image_when_image_url_none. |
| 2026-02-26 | Email update types: major/minor/other | **Data:** Update.update_type (slug: major \| minor \| other), default "other"; Alembic 20260226110000. **Admin:** Compose form has "Update type" fieldset with radio buttons; create handler validates and stores type; drafts/sent list shows type label. **Public:** Sidebar shows type · date per update; article shows type phrase above title (e.g. "Major update"). **Constants:** UPDATE_TYPES, UPDATE_TYPE_PHRASES in routers/updates.py. **Tests:** test_create_draft_with_update_type_major_stores_type, test_create_draft_invalid_update_type_falls_back_to_other, test_updates_page_shows_update_type_in_sidebar_and_article. **Docs:** app-overview.md Updates row + Key concepts "Email update types". |
| 2026-02-26 | Updates page: one page, TOC sidebar, image attachment | **Single page:** All sent updates rendered on /updates; no "View this update in a new page" link. **Sidebar:** Blog-style TOC: links `#update-{id}`; date prominent; minimal link styling (no button look). **Redirect:** GET /updates/{id} → 303 /updates#update-{id}. **Image:** Update.image_path (nullable); admin compose optional file upload (JPEG/PNG/WebP, max 5 MB); saved under static/updates/; shown on public page and in email. Config: UPDATE_IMAGE_UPLOAD_DIR, UPDATE_MAX_IMAGE_BYTES. **Migration:** 20260226120000. **Tests:** no view-in-new-page link, sidebar anchors, detail redirects, create with image, page/email show image. **Docs:** app-overview.md Updates row. |
| 2026-02-26 | Admin dashboard: stats + layout | **Dashboard at /admin:** At-a-glance stats grid (users, subscribers, new this week, campaigns sent, drafts, emails delivered, drawer opens, outreach actions, advocates — last 90d for outreach). Quick-action cards below (Send email, Users, Outreach stats) with meta stats. Backend: admin_dashboard() passes Update counts, new_users_7d, outreach_summary from _get_outreach_conversion_data. Template: admin_dashboard.html stats section + stat tiles; CSS: admin-dashboard.css stat grid/tile, design tokens, responsive. docs/features/app-overview.md updated. |
| 2026-02-26 | Email sender avatar (favicon split) | **Why:** Email clients that show a sender image for non-Google addresses often use the domain’s favicon (e.g. Gmail/Apple Mail). Our favicon was the truck emoji, so sent emails showed the truck as the sender avatar. **Fix:** Serve a simple letter “L” (Land of Kei) at `/favicon.ico` (favicon-email.svg) so domain-based avatar is a clean letter. Site tab icon unchanged: base.html links to `/static/favicon.svg` (truck). site.py favicon route, base.html link, static/favicon-email.svg. |
| 2026-02-26 | Auth UX: 401 → login redirect | **HTML 401:** For `/admin/*` (except `/admin/login`), redirect to `/admin/login?next=<path>`. For other paths, redirect to `/?next=<path>`. **Post-login:** Home sign-in (goVerified) or admin login page redirect to `next` when safe (same-origin). main.py _http_exception_handler; admin_login.html; index.html goVerified. **403:** Admin paths → redirect to `/admin/login?error=forbidden`; other 403 → 403.html. |
| 2026-02-26 | Admin: dedicated login, dashboard, users, stats | **Login:** GET `/admin/login` — standalone email-code sign-in; redirects to `/admin` or `next`. **Dashboard:** GET `/admin` (require_admin) — cards: Send email → /admin/updates, Users → /admin/users, Stats → /admin/outreach. **Users:** GET `/admin/users` — paginated list (email, wants_updates, last_login_at, created_at). **Outreach:** GET `/admin/outreach` — server-rendered conversion/volume (last 90 days); `/admin/outreach/conversion` JSON now require_admin and available in prod. **Nav:** _admin_nav.html (Dashboard \| Email \| Users \| Stats \| Mocks \| Log out) on dashboard, users, updates, outreach, mocks. config ILGA_ADMIN_EMAILS. |
| 2026-02-26 | Sprint 1: Updates + notifications | **Public /updates:** Campaign status (CAMPAIGN_STATUS from content.py), past updates archive (sent_at IS NOT NULL), email subscribe (POST /updates/subscribe when logged in) and unsubscribe (GET /updates/unsubscribe?token=...). **Admin:** GET /admin/updates (compose + list drafts/sent), POST create draft, GET preview, POST send; protected by require_admin (ILGA_ADMIN_EMAILS). **Data:** User.wants_updates (default True), Update model (title, body_plain, body_html, sent_at, sent_count); Alembic migration 20260226100000. **Email:** email_utils.send_email() + send_message() (auth + feedback refactored to use); update emails with per-user unsubscribe link. **Footer:** Updates link in primary nav. **Tests:** test_updates.py (page, subscribe/unsubscribe, admin gate, create draft, send). **Dev mocks:** In dev with no SMTP, send_email logs the message and returns True so send count increments; when no users have wants_updates=True, send_update_to_subscribers falls back to all users so mass send can be tested locally. test_send_update_dev_fallback_when_no_subscribers covers fallback. |
| 2026-02-26 | Admin send: status page + recipients | **Status page:** POST /admin/updates/{id}/send starts background send and immediately redirects to GET /admin/updates/{id}/send/status?job=uuid. Status page polls GET /admin/updates/send/status/{job_id} (JSON: total, sent, failed, done); shows "Sent X of Y" and when done "Back to compose" with optional 3s auto-redirect. **Recipients step:** Send button on drafts links to GET /admin/updates/{id}/recipients; page lists subscribers with checkboxes (all checked), Select all / Deselect all; POST send with from_recipients_page + recipient_ids; empty selection redirects to recipients?flash=no_recipients. **Backend:** app.state.send_jobs in lifespan; run_send_loop background task; _get_subscriber_list(recipient_ids) for send-to-all or selected list. **Tests:** test_updates.py (send redirects to status, poll until done; recipients page 200; status JSON 404 for unknown job; zero recipients redirect). |
| 2026-02-26 | Home: section spacing + inner padding (consistent vertical rhythm) | **Between sections:** `.page-home #main-content > * + *` — 2rem / 4rem (768px+) / 5.5rem (1024px+). **Within sections:** CSS vars `--home-section-padding-block` (2rem / 2.5rem / 3rem by breakpoint) and `--home-section-title-gap` (1.5rem) for consistent top/bottom padding and title-to-content gap. Applied to mission-statement, vision-and-success, whats-happening, strategic-points--home, home-cta, share-section. Vision line→text and all section titles use title-gap. base.css, mission-statement.css, whats-happening.css, strategic-points.css, share-section.css. |
| 2026-02-26 | Home: success measures → The Issue FAQ | **"How we measure success"** removed from home mission block. Content (Constituent contacts, Co-sponsors secured, Witness slips filed, A coalition ready to act) moved to The Issue page under **FAQ — Advocacy & how we work** as new item "How we measure success?" (id `success-measures`) with STRATEGIC_SUCCESS_MEASURE + STRATEGIC_SUCCESS_MEASURE_ITEMS. Template supports optional `answer_list` in advocacy FAQ items. home: _mission_statement.html (vision only), home.py (no strategic_success_measure/items). content.py: FAQ_ADVOCACY + success-measures; the_issue.html answer_list. docs/features/app-overview.md updated. |
| 2026-02-26 | Home: "21+ states" card — state-shapes icon + pill tooltips + click to front | **Strategic point 2:** Inline SVG of 3 state shapes (CO, MI, TX). **Tooltips:** Custom pill tooltips on hover (no native delay): small, minimal, pill-shaped; fade in + slide up (0.18s), fade out + slide down on leave; data from data-state/data-how (content.py). Single shared tooltip in .strategic-states-icon-wrap; script in partial shows/positions/hides; prefers-reduced-motion short-circuits animation. **Interactivity:** Click (or Enter/Space) a state to bring it to the front (script in base.html). **A11y:** role="button", tabindex="0", aria-label; focus shows tooltip; focus-visible ring. _strategic_states_icon.html (wrap, tooltip span, data attrs, script), strategic-points.css (.strategic-states-tooltip, --visible, --leaving). |
| 2026-02-25 | Home: 5 points — Heroicons icon pack, 3-line clamp | **Our message in 5 points:** Icons from **Heroicons (solid)** via Iconify icon pack (base.html already has iconify-icon script). Icons: check-circle, map, document-text, cog, user-group; colors unchanged (red #E53935, blue #1E88E5, yellow #FDD835, green #43A047, orange #FB8C00). Size 11rem; card text clamped to 3 lines (line-clamp: 3). No custom inline SVGs. home.html (five_point_icons + five_point_colors, iconify-icon), strategic-points.css. |
| 2026-02-25 | Code cleanup                 | Ruff: import order fixed (admin, explore, feedback, intelligence); format (advocacy_helpers, main, content). Lint and tests pass. |
| 2026-02-25 | Call script + email use 3–5 conclusions | **Focused next steps §4:** Call script and email now reference the 3–5 conclusions everywhere. **Drawer call script** (_advocacy_drawer_call.html): THE PROBLEM / LEGAL WHY / THE ASK rewritten to mirror conclusions 1–5 (federally legal, IL blocking; 21+ states; ambiguity not prohibition; narrow amendment; real residents). Added "Three points to remember (elevator)" bubble. Voicemail script aligned to same conclusions. **Email** (_kei_email_body_core): "The attached one-pager outlines:" bullets now use STRATEGIC_FIVE_POINTS when provided; advocacy router and dev playground pass one_pager_points=STRATEGIC_FIVE_POINTS. **Script sections** (advocacy_helpers): the_ask for senator/rep/broker includes "21+ states" and elevator framing; SCRIPT_ELEVATOR_THREE and "conclusions" key added for reference. |
| 2026-02-25 | Fact sheet as doc in Issue sidebar | **Sidebar:** Fact sheet is a document (PDF) in The Issue sidebar, same treatment as constituent brief. content.py: FACT_SHEET_PDF_URL = `/static/advocacy/Kei_Registration_Fact_Sheet.pdf`; the_issue passes fact_sheet_pdf_url; sidebar first doc = "Fact sheet for volunteers" → PDF (icon-pdf, target=_blank). docs/canonical/README.md + no-hallucination rule: fact sheet PDF path and canonical content (content.py FACT_SHEET_*). User places PDF at that path (e.g. print /fact-sheet to PDF). |
| 2026-02-25 | Fact sheet page + language double-check | **Fact sheet:** New public page at /fact-sheet with content from focused-next-steps doc §5 (issue, position, five points, supporters placeholder, five FAQs, links). content.py: FACT_SHEET_ISSUE, FACT_SHEET_POSITION, FACT_SHEET_SUPPORTERS_PLACEHOLDER; route and fact_sheet.html; base.css .fact-sheet-*; footer nav + The Issue sidebar link; sitemap. **Consistency:** the_issue.html fallback copy updated to doc language (no "currently", "which means"; Narrow Fix from doc); all public copy aligned to focused-next-steps. |
| 2026-02-25 | Language aligned to focused-next-steps doc | **Everywhere:** Messaging updated to match `docs/advocacy/focused-next-steps-1-2-4-5-6.md`. content.py: STRATEGIC_VISION, STRATEGIC_FIVE_POINTS (em dash, "Illinois"), STRATEGIC_SUCCESS_MEASURE/ITEMS ("constituent contacts"), FAQ_ADVOCACY (fact-sheet wording). advocacy_helpers.py: call scripts and email use "narrow statutory clarification to 625 ILCS 5/3-401(c-1)", "kei vehicle registration", benefit-to-district (Option C/B); email subject and body. advocacy.py: home hero "Fix the statutory gap. Allow kei vehicle registration."; _HERO_SUBHEAD from doc "The issue". Legislator brief .txt: benefit-to-district (Option B) added; off-highway hyphen fix. Constituent brief .txt: "The Issue" and "The Narrow Fix" from doc. letter_template.html, index.html: "kei truck" → "kei vehicle registration" where advocacy. |
| 2026-02-25 | Focused next steps (Hardball 1,2,4,5,6) | **Docs:** `docs/advocacy/focused-next-steps-1-2-4-5-6.md` — written sections for (1) Objective + 7 questions, (2) One-page strategy outline, (4) 3–5 conclusions, (5) One-page fact sheet for base, (6) Benefit-to-district line for brief/script. Sources: Hardball spec, legislator/constituent .txt briefs, content.py. Calendar left as placeholder in strategy outline. |
| 2026-02-25 | Message marquee (5 points)   | **Marquee:** Single-line scrolling "Our message in 5 points" under the beta banner, site-wide. Content from STRATEGIC_FIVE_POINTS (content.py); template global in main.py; base.html marquee block + message-marquee.css. CSS animation with seamless loop; prefers-reduced-motion shows static line. Home "Our message in 5 points" section kept as full list. **Feature flag:** `ILGA_FEATURE_MESSAGE_MARQUEE` (config.py); dev default on, prod default off; base.html shows marquee and loads message-marquee.css only when `features.message_marquee` is true. |
| 2026-02-25 | Home: Vision statement section (reference layout) | **Vision block:** Redesigned to match reference: small uppercase "Vision Statement" label, thick black horizontal line (~3.5rem × 4px), then large bold statement (clamp 1.75rem–2.5rem, tight line-height). Sans-serif; left-aligned; ample whitespace. "How we measure success" list remains below. _mission_statement.html (vision-statement-block, -label, -line, -text), mission-statement.css. |
| 2026-02-25 | Home: Vision & 5 points layout (form/layout only) | **Vision & success:** Stacked layout: one full-width Vision lead (no box), then "How we measure success" as a list from STRATEGIC_SUCCESS_MEASURE_ITEMS (content.py). Replaced 2-col equal boxes. **5 points:** Single-column editorial numbered list on home (no cards, no grid); first point slightly emphasized; max-width 65ch. **Canonical:** STRATEGIC_SUCCESS_MEASURE_ITEMS added; home passes strategic_success_measure_items. _mission_statement.html, mission-statement.css, strategic-points.css (.strategic-points--home), home.py, content.py. docs/features/app-overview.md updated. Mobile-first; no new color or decorative UI. |
| 2026-02-25 | State table: notes two-column | **Table notes:** Two columns — "Indicators" (✓/●/—/⚠ and meanings) and "Notes" (Speed/Limited definitions). Grid layout; stacks on narrow viewports (&lt;35rem). legislator_brief.html, the_issue.html, base.css (.brief-table-notes__col, grid, media query). |
| 2026-02-25 | State table: notes (journal-style) | **Table keys/legend:** Replaced inline legend with NYT/journal-style "Notes:" block below state table. Semantic `<footer class="brief-table-notes">` with label and `<ul>`; each key on its own line; small type (0.8125rem), muted color, thin top border. legislator_brief.html, the_issue.html, base.css (.brief-table-notes, .brief-table-notes__label, .brief-table-notes__list). |
| 2026-02-25 | Home: What's happening — icon + title + desc; 5-point colors | **What's happening** section: three columns with **icon → title → description** (no cards, light background). Inline SVGs for What (prohibition), Why (document), Fix (wrench). Icon colors match 5 points section: red #E53935, blue #1E88E5, yellow #FDD835 (.whats-happening-icon--0/1/2). Intro line "Registration blocked, statutory gap, narrow fix." _whats_happening.html, whats-happening.css. |
| 2026-02-25 | Home: mission + strategic sections match What's happening | **Mission:** Quote larger (clamp 1.75rem–3.25rem), line-height 1.5, max-width 18em for more vertical lines; body gap/min-height increased. **Vision, success, 5 points:** Minimal layout to match What's happening — same border-top, title style; vision/success in 2-col grid with bordered parts (#fafafa, 1px #ddd, 6px radius); 5 points as grid of bordered parts, numbered 1.–5. via label-style ::before, no accent color. _mission_statement.html (vision-and-success-row wrapper), mission-statement.css, strategic-points.css. |
| 2026-02-25 | Legislator brief: Key points section | **Key points on leg brief:** Per Hardball Ch7 (making the case, concise arguments), added a "Key points" section at top of brief main content. **Evaluate whether to show:** Section renders only when `strategic_five_points` is present (`{% if strategic_five_points %}`); if STRATEGIC_FIVE_POINTS were empty, section would not appear. **Content:** Points come only from STRATEGIC_FIVE_POINTS in content.py (canonical); no invented copy. content.py passes `strategic_five_points` to legislator_brief_page; legislator_brief.html uses same `.strategic-points` / `.strategic-points-list` pattern as The Issue. docs/canonical/README.md already specifies leg brief key points from STRATEGIC_FIVE_POINTS. |
| 2026-02-25 | Advocacy success goals (FAQ + mission) | **FAQ — Advocacy:** New item “What does success look like for this advocacy?” and refined adv1 (campaign objective vs what we control). Success = legislator contacts, co-sponsors, witness slips, coalition readiness; road-legal = objective we work toward. **content.py:** STRATEGIC_SUCCESS_MEASURE added; FAQ_ADVOCACY adv1 + adv1b. **Home:** Mission block shows success measure line (STRATEGIC_SUCCESS_MEASURE) when present. **Docs:** app-overview notes where goals live; mission-statement.css .mission-statement-success-measure. |
| 2026-02-25 | Coalition page hidden + brief copy | **Coalition hidden from nav:** Footer link to /coalition removed so the page is not in public view until there are supporters to list. Route /coalition still works. **Legislator brief copy:** All references to "coalition page" and "recognized on our coalition page" removed from CTA block and Office outreach section; copy now says "point of contact" only. base.html (footer), legislator_brief.html (CTA + Office outreach), docs/features/app-overview.md. |
| 2026-02-25 | Legislator brief: CTA visibility | **Above-the-fold CTA:** Legislator brief now shows a prominent "Endorse, sponsor, or get in touch" block in the header (right after intro) with Call and Email primary buttons. **Point of contact:** `.brief-contact` section styled as a card (background, border, padding) so it stands out when scrolling. Skeleton: `brief_header_after` block; legislator_brief.html CTA block; base.css `.legislator-brief-cta`, `.legislator-brief-cta__*`, `.brief-contact` card. |
| 2026-02-25 | Brief pages source from .txt files | **Canonical .txt:** Legislator brief page and The Issue page now load content from repo-root `IL_Kei_Vehicle_Registration_Fix_Brief 1.txt` and `Illinois_Kei_Vehicle_Registration_Constituent_Brief.txt`. content.py: CONSTITUENT_BRIEF_PATH, LEGISLATOR_BRIEF_PATH; _load_constituent_brief(), _load_legislator_brief()` parse and return structured data. Routes pass legislator_brief / constituent_brief to templates. **Templates:** legislator_brief.html and the_issue.html render from these when present, with fallback to existing hardcoded content if files missing. docs/canonical/README.md and no-hallucination rule updated to name the .txt files as source of truth. |
| 2026-02-25 | Canonical content + no hallucination | **Canonical sources:** All substantive content (copy, key points, FAQs) must come only from approved sources: content.py (STRATEGIC_*, FAQ_*, BRIEF_*), legislator_brief.html (official one-pager text), the_issue.html (constituent narrative). **Rule** `.cursor/rules/no-hallucination-content.mdc` (alwaysApply): never invent; cite source. **Doc** `docs/canonical/README.md`: list of sources, one-pager vs constituent brief, how to add/change. **Skill** `.cursor/skills/canonical-content-sources/SKILL.md`: when writing site content, use only these sources; one-pager and constituent brief continuity. Hardball rule updated: use spec for *whether* to do something, canonical sources for *what* to say. |
| 2026-02-25 | Hardball spec as source of truth | **Spec:** *Hardball Lobbying for Nonprofits* (hardball.txt) is the product/advocacy source of truth. **Chunked:** `docs/hardball-spec/` — 11 markdown chunks (foreword, intro, ch1–8, postmortem, bibliography) + README index; generated by `scripts/split_hardball_spec.py`. **Cursor:** Rule `.cursor/rules/hardball-spec-source-of-truth.mdc` (alwaysApply) instructs the agent to use the spec when planning features/bugs and to cite chunks. Skill `.cursor/skills/hardball-spec-reference/SKILL.md` tells the agent to read the relevant chunk and cite spec text when implementing advocacy/lobbying-related code. |
| 2026-02-25 | Strategic plan (mission, vision, 5 points) | **Single source of truth:** Mission, vision, and 5-point message in `content.py` (STRATEGIC_MISSION, STRATEGIC_VISION, STRATEGIC_FIVE_POINTS). **Home:** Mission + vision in `_mission_statement.html` (from template vars); new "Our message in 5 points" section between mission and CTA; What's happening kept. **The Issue:** "Key points" section at top (same 5 bullets); vision callout in "The Narrow Fix". Both routes pass strategic_* into template context. CSS: `.mission-statement-vision`, `strategic-points.css` (`.strategic-points`, `.strategic-points-list`, `.strategic-vision-callout`). home.py imports from content; base.html links strategic-points.css. |
| 2026-02-25 | Legislator brief: CTA + coalition page | **Legislator brief:** New "Office outreach" section between Point of contact and Outreach. Offices invited to contact POC to (1) request more info, (2) discuss endorsing the cause, (3) discuss supporting the bill; all contact through coalition POC; offices that engage may be recognized on coalition page (name and photo) with consent. **Coalition page:** GET /coalition (Supporting legislators): placeholder content; route in content.py, template coalition.html (brief skeleton, no sidebar); footer nav link. Design follows Hardball Ch 4 (invitation to contact = not lobbying) and Ch 7 (official contact through office). legislator_brief.html, content.py, coalition.html, base.html, docs/features/app-overview.md. |
| 2026-02-25 | Kei states map: fix wonky fill at borders | **Map fill:** State fills now extend to path edges: (1) SVG `.state` given `stroke:none` by default. (2) In applyFills we only style state paths (class without hyphen); set `fill` and `stroke` to the same color with `strokeWidth: 0.5` so the stroke covers anti-aliasing gaps. Border paths (e.g. ne-wy) are skipped so they don’t get status colors. us-states.svg, legislator_brief.html + the_issue.html applyFills. |
| 2026-02-25 | Kei states: legal vs AAMVA-fix views | **Single source of truth:** BRIEF_STATE_STATUS unchanged; added **aamva_fix** (CO, TX, MI, MA, OR, ME) and **explicit_kei_law** (CO, TX). **Two views:** Filter toggle "All legal to register" (default) / "Countered AAMVA" — table rows and map grays non-AAMVA states when "Countered AAMVA" selected. **Table:** AAMVA fix column removed; filter and row visibility (data-aamva-fix) unchanged. content.py (_brief_aamva_fix_state_abbrs, brief_aamva_fix_state_abbrs_json), both templates (filter toggle, data-aamva-fix on rows; no AAMVA column or legend), map JS (applyFilter, gray non-AAMVA), base.css (.brief-states-filter-toggle, .brief-states-filter-btn). |
| 2026-02-25 | State table: speed limit in Speed column | **Speed column:** Speed limit (or route restriction) text moved from "How" into "Speed" where applicable. content.py: added optional `speed` field to BRIEF_STATE_STATUS (e.g. "55 mph or less", "55 mph", "45 mph max", "No Interstates"); trimmed mechanism strings so "How" does not duplicate. Templates (the_issue.html, legislator_brief.html): Speed cell shows `s.speed` when set, else "Limited" when speed_limited, else "—". Legend updated: "value shown when set; Limited when restricted but no value; — when no restriction". |
| 2026-02-25 | Kei states map + table expansion | **Map/table:** Added 21 full-road-use states to BRIEF_STATE_STATUS (AZ, AR, CO, DE, ID, IN, LA, ME, MD, MA, MI, MS, MT, NE, NC, ND, OK, OR, SC, TN, TX, WA, WV, WY). Map and table on The Issue and legislator brief now show all; map fills by bill_status/policy. **Speed column:** New "Speed" column denotes states where max speed or route is limited (55 mph or less, or no Interstates): AR, CO, IN, LA, NE, NC, ND, OK, WY. Legend: "Limited = max speed or route restricted (e.g. 55 mph or no Interstates)". content.py (BRIEF_STATE_STATUS + speed_limited), the_issue.html, legislator_brief.html. |
| 2026-02-25 | Kei states: restricted (yellow) category | **Restricted states:** Added 7 states with antique/collector or speed/radius caps only: CT, MO, NH, PA, UT, VA, WI. New map fill "restricted" (yellow #c9a227); _brief_map_fill_status returns "restricted" when s.restricted; table Bill column shows "Restricted" for these; map legend + table legend updated. base.css --brief-color-restricted, .brief-map-swatch--restricted, .brief-table-indicator--restricted; both templates. |
| 2026-02-24 | Legislator brief: table default + first option | **States view:** Default view is table (was map). Toggle order: Table first, Map second; aria-label "View as table or map". legislator_brief.html. |
| 2026-02-24 | Legislator brief: official one-pager content | **Leg brief page:** Replaced main content with official two-page brief (IL_Kei_Vehicle_Registration_Fix_Brief). Sections: Issue in one sentence + core ambiguity; What SOS is relying on; Why a narrowly tailored statute; Proposed legislative concept; What we are asking your office to do (3 items); Attachments & reference (625 ILCS link); Point of contact (Tyler Morales); Outreach CTA unchanged. State map/table and FAQ kept. legislator_brief.html. |
| 2026-02-24 | Legislator brief: internal summary PDF | **Brief documents:** Added "IL Kei Vehicle Registration Fix Internal Summary" to legislator brief sidebar (BRIEF_DOCUMENTS in content.py); file at /static/IL_Kei_Vehicle_Registration_Fix_Internal_Summary.pdf. |
| 2026-02-24 | Public-facing copy audit      | **Copy (no ML/intelligence):** Mission statement "kei vehicles" → "Kei vehicles". Hero and CTA button "Start Outreaching" → "Start outreach" (home + advocacy heroes, mission CTA). Advocacy subhead "start outreaching" → "start outreach". Explore nav "ML Intelligence Dashboard" → "Intelligence Dashboard". _mission_statement.html, _hero_home.html, _hero_advocacy.html, _mission_cta.html, explore.html, advocacy.py. |
| 2026-02-24 | Terms page + footer last updated | **Terms of use** (/terms): Added What this site does (advocacy tool, sign-in, bug report, GraphQL/API); Acceptable use now includes no misleading communications; Contact section (Report a bug /report-bug). **Footer:** "Last updated: [date]" at bottom of all pages via base.html; config FOOTER_LAST_UPDATED and FOOTER_LAST_UPDATED_ISO; template global in main + all routers that render full pages; base.css .footer-last-updated. |
| 2026-02-27 | Privacy policy (subscription + campaign) | **Privacy policy** (/privacy): What we collect — "Sign-in email" → "Email address" to cover both sign-in and campaign-update subscription (footer, Updates page, in-drawer); added unsubscribe note. Outreach you save — added campaign attribution when a campaign is active. How we use it — subscription email use and unsubscribe. docs/features/app-overview.md Legal and trust updated to match. |
| 2026-02-24 | Privacy page updates            | **Privacy policy** (/privacy): Added What we collect — account ZIP code, community-sourced legislator emails; How we use it — uses for both; new sections Data sharing and no sale, Security (HTTPS, secure cookies), Your rights (access/correction/deletion); Retention now references stored ZIP and community emails. Contact reframes as exercising rights. |
| 2026-02-24 | Issue & brief sidebar: Share section | **Share:** Sidebar on The Issue and legislator brief pages now includes a "Share" section (copy link + native share when available). Target-specific copy: **Constituent** (the_issue): "Share this page with neighbors so they can learn about the issue and contact their legislators." **Legislator** (legislator_brief): "Share this brief with colleagues or the constituent overview with constituents." Partial `_share_section_sidebar.html` accepts `target` and `section_id`; unique IDs per page (issue-share, brief-share). CSS: `.share-section--sidebar` inside `.legislator-brief-sidebar` (margin-top, border-top). the_issue.html, legislator_brief.html, _share_section_sidebar.html, share-section.css. |
| 2026-02-24 | The Issue: map + table (other states) | **Map/table:** Same states map and table as legislator brief added to The Issue page in new section "Where Other States Stand" (after Why This Matters). Data: content.py passes `brief_state_status` and `brief_state_map_status_json` to the_issue; shared `_brief_map_fill_status` used by both routes. **Constituent copy:** Section intro explains many states allow kei registration; table intro "States that have allowed or are moving toward kei registration. This list is not exhaustive."; map legend "Law passed" / "Bill in progress" / "Policy change"; table legend "Law passed" / "Bill in progress" / "No bill or policy change". Toggle (Table/Map), IDs issue-view-map, issue-view-table. extra_scripts block on the_issue.html runs same map fill + toggle script. the_issue.html, content.py. |
| 2026-02-24 | The Issue page sidebar + PDF | **Sidebar:** Added Documents sidebar to The Issue page (same layout as legislator brief). Single document: "Illinois Kei Vehicle Registration Constituent Brief" linking to `/static/images/Illinois_Kei_Vehicle_Registration_Constituent_Brief.pdf`. Uses `legislator-brief-sidebar`, `legislator-brief-docs`, `legislator-brief-doc`, `icon-pdf.svg`; removed `legislator-brief-layout--no-sidebar`. the_issue.html. |
| 2026-02-24 | The Issue sidebar: Sources section | **Sources:** Sidebar now includes a "Sources" subsection listing every source mentioned on the issue page: statute 625 ILCS 5/3-401(c-1) first, then all unique FAQ source links (deduped by URL). `_issue_sources_from_faq(FAQ_ADVOCATES)` in content.py builds `ISSUE_SOURCES`; the_issue template renders them with `legislator-brief-sources` / `icon-link.svg`; aria-labelledby and aria-label on links for a11y. |
| 2026-02-24 | The Issue page main content | **Content:** Replaced main content in the_issue.html with full kei registration explainer: hero title "Illinois Kei Vehicle Registration", main heading "What Illinois Residents Need to Know". Sections: The Issue in Plain English, Why This Is Happening, What This Means for Residents, What This Is NOT About, The Narrow Fix, Why This Matters, What You Can Do (bulleted list + CTA to /advocacy/). Semantic sections with aria-labelledby; FAQ block unchanged. |
| 2026-02-24 | FAQ sections (the-issue, legislator-brief) | **Content:** FAQ data in content.py: `FAQ_ADVOCATES` (7 items, title "FAQ — For Advocates", intro + sources per item) for The Issue; `FAQ_LEGISLATORS` (6 items, "FAQ — For Legislators & Staff") for legislator brief. Templates use `faq.title`, `faq.intro`, `faq.items` (id, question, answer, sources); each answer shows "Sources:" with links (target=_blank, focus-visible). CSS: `.brief-faq__intro`, `.brief-faq__sources-label`, `.brief-faq__sources`, `.brief-faq__source-link`. Section wrapped in `{% if faq %}` for optional render. |
| 2026-02-24 | Your goal: Power Broker explainer only in broker phase | "Who is the Power Broker?" details toggle is shown only when the goal has advanced to the broker phase (4/4 district actions complete). CSS: .outreach-goal:not(.outreach-goal--phase-broker) .outreach-goal__power-broker-details { display: none } in advocacy-cards.css. Works for both server-rendered (goal_phase == 'broker') and client-applied phase (updateGoalBlockFromStorage adds class when 4/4 + broker). |
| 2026-02-24 | Your goal: bar label spacing + "Now" CTA | **Spacing:** Increased vertical gap between "X of 4 actions complete" and the progress bar (.outreach-goal__bar-label margin-bottom 6px → 14px). **Copy:** "Next: [task]" → "Now: [task]" in goal sidebar (district + broker phases), JS updateGoalBlockFromStorage, and admin mocks. advocacy-cards.css, _results_partial.html, index.html, admin_mocks.html. |
| 2026-02-24 | Your goal: Power Broker phase | **Goal section:** When all 4 district actions are complete, the goal advances to "Outreach the Power Broker" (2 actions: call and email). Removed celebration block ("You've reached both your district members" / "Want more impact? Call the Power Broker"). Added "Completed goals" heading with the 4 district steps struck out and lower opacity; "Who is the Power Broker?" explainer below. Dynamic goal line: "Your goal: Outreach your district legislators" or "Your goal: Outreach the Power Broker". Backend: advocacy.py (goal_phase, current_goal_label, broker_goal_steps, completed_goal_steps, district_steps). Template: _results_partial.html (two-phase structure, data-goal-broker-id). CSS: phase visibility, completed list, explainer; removed celebration styles. JS: updateGoalBlockFromStorage switches to broker phase when 4/4 + broker id, updates broker bar and Next. docs app-overview. |
| 2026-02-24 | Mock Control Panel (admin)   | **Mock Switchboard:** DEV_MODE-only GET /admin/mocks with visual control panel to set outreach state for dev user (funky_mama11@gmail.com). ZIP resolver (HTMX GET /admin/mocks/resolve), scenario presets (Fresh Start, Called Both, Full Completion, Mixed, Senator Only, Email Only), switchboard grid with Call/Email jacks per legislator (Senator, Rep, Power Broker), support score, contact name, constituent, community heat sliders. POST /admin/mocks/apply clears dev user events and inserts configured OutreachEvents + heat seeds. Templates: admin_mocks.html, admin_mocks_grid.html; CSS: admin-mocks.css (retro dark panel, glowing jacks). |
| 2026-02-24 | Single ZIP (revert home zip)  | **Rule:** One ZIP only — the value the user enters in the hero form. That ZIP drives both search results and "Your goal" / your legislators. No separate "home zip" vs "search zip." **Backend:** advocacy_index uses single `zip` from URL/form or user.zip_code for prefill when no param. No hero_zip in context. **Frontend:** Hero has only the form input (#zip_code); no "Home ZIP" display or edit line; no ZIP row in "Your goal" sidebar. Form submit updates URL and refreshes results; when signed in, PATCH /advocacy/api/me/zip can update saved zip. commitGoalZip() kept in JS for future use (e.g. geolocation). docs/features/app-overview.md (User ZIP). |
| 2026-02-24 | Email: no editable span after mad lib inputs | **Mad lib body:** Whitespace-only segments (e.g. newlines between `[CALLER_NAME]`, `[CALLER_PHONE]`, `[CALLER_EMAIL]`) were rendered as contenteditable spans, creating invisible editable regions after each input. renderMadLibsBody now appends plain text nodes for whitespace-only parts and keeps contenteditable spans only for non-whitespace prose. index.html renderMadLibsBody. |
| 2026-02-24 | Email: no outline on non–mad-lib content | **Focus outline:** Removed for non–mad-lib editable content (paragraphs, bullets). Mad lib blanks (input/textarea.madlib-blank) keep focus-visible outline. advocacy-email.css: dropped `.gmail-compose .madlib-body [contenteditable]:focus-visible` from unified rule; added `.madlib-body [contenteditable]:focus-visible { outline: none; }`. |
| 2026-02-24 | Email "Why this matters" mad lib layout | **Why this matters:** Period on same line via .madlib-why-wrapper. **All mad lib inputs resize with content:** Single-line width (ch-based); multiline height from scrollHeight after appendChild. **Mobile overflow:** Multiline mad lib uses width: 100%; max-width: 44ch; box-sizing: border-box so it stays within container and spills to next line; overflow-x: hidden and word-break/overflow-wrap on textarea; .madlib-body overflow-x: hidden. advocacy-email.css .madlib-body, .madlib-blank-multiline; index.html resizeMadLibInput. |
| 2026-02-24 | Mad lib input width dynamic from placeholder/value | **Single-line mad lib blanks:** Width is now driven by measured text width instead of character count. A hidden .madlib-measure span (same font as input) measures placeholder when empty and value when filled; input width = max(measured + cursor padding, minCh in px). Sizing updates on input/focus/blur so space grows and shrinks as the user edits. advocacy-email.css .madlib-measure; index.html renderMadLibsBody (measure span), resizeMadLibInput (measure-based width). |
| 2026-02-24 | Email drawer: remove call notes | Green "Call first when you can" and yellow "This legislator's email isn't publicly listed" banners removed from the email compose section. _advocacy_drawer_email.html: gmail-banner-tip and email_first warning blocks removed. |
| 2026-02-24 | Email drawer: 75% width on tablet | Email drawer uses 75% viewport width on tablet (769px–1024px); desktop remains 50vw, ≤768px remains bottom sheet (100%). advocacy-drawer.css: @media (min-width: 769px) and (max-width: 1024px) .phone-panel.phone-panel--email { width: 75vw; max-width: 75vw; }. |
| 2026-02-24 | Advocacy: one-pager terminology | Standardized on "one-pager" for the attached PDF everywhere: email body ("writing to share a one-pager", "The attached one-pager outlines", "Consistent with the one-pager"); call script pushback ("I'll email a one-pager", "send the one-pager"). UI already said "One-pager to attach when you send". advocacy_helpers.py, _advocacy_drawer_call.html. |
| 2026-02-24 | Advocacy hero: magnifying glass on mobile | Lens and intro sweep now visible on mobile (≤768px). Removed .magnify-lens { display: none } from magnify-lens.css; removed window.innerWidth <= 768 early return in runIntroAnimation (magnify-lens.js). Cursor-follow remains desktop-only; mobile gets intro sweep then outro. |
| 2026-02-24 | Magnifying glass: 30% smaller on mobile | At max-width 768px, lens and frame scaled to 70%: :root --magnify-size 105px; .magnify-lens --magnify-frame-size 161px, frame offsets scaled. JS reads --magnify-size from root so intro/cursor math stays correct. magnify-lens.css. |
| 2026-02-24 | Magnifying glass: slower intro animation | Intro sweep: desktop 2.8s (was 2.2s), mobile 2.9s. magnify-lens.js runIntroAnimation. |
| 2026-02-23 | Legislator brief: single Next step CTA | **Next step** section only: one block with combined copy (constituents use the tool; view or share it), single primary CTA "View outreach tool" → /advocacy/, secondary "The Issue" link. "More information" removed to avoid redundancy. legislator_brief.html. |
| 2026-02-23 | Advocacy: relevant committees only in "Why we recommend" | **Why we recommend them:** Chair chips and prose now show only committees relevant to the topic (e.g. Transportation) or general committees most bills go through (Appropriations, Assignments). constants.py GENERAL_COMMITTEE_CODES; recommendation_chip_order + member_to_card accept relevant_committee_codes; _build_search_results_context passes topic + general codes; _results_partial uses leadership_roles_relevant for prose and chair chips. |
| 2026-02-23 | Advocacy hero: ZIP + Use location under email | (Superseded by 2026-02-24 Single ZIP: hero has only #zip_code; no hero-zip line, no sidebar zip cell. Dead code removed 2026-02-24.) |
| 2026-02-23 | Hero: remove auth-strip progress line | "Called X legislators and sent Y emails" line under verified email removed from advocacy and home hero. Template: `#auth-strip-progress` div removed from _hero_advocacy.html and _hero_home.html. hero-auth-strip.js: refreshAuthStripProgress is now a no-op; updateAuthStrip no longer references stripProgress. advocacy-form.css: .auth-strip-progress removed. |
| 2026-02-23 | Mad lib ONE_SENTENCE_WHY multi-line | **Why this matters to you:** Mad lib blank is now a multi-line textarea so it wraps instead of overflowing and breaking the sentence (period on same line). MADLIB_PLACEHOLDERS ONE_SENTENCE_WHY multiline: true; renderMadLibsBody creates textarea with class madlib-blank-multiline; getComposedBody handles TEXTAREA; sync uses [data-madlib-key]; resizeMadLibInput grows rows for textarea. advocacy-email.css .madlib-blank-multiline (width/max-width, resize none, wrap). |
| 2026-02-23 | Advocacy: district cards stack at tablet | **Your district members:** Senator + Rep cards stack in one column at viewport ≤900px (was 768px) so iPad Air (820px) and similar tablet widths show stacked layout. advocacy-cards.css `.district-members-row` @media (max-width: 900px). |
| 2026-02-23 | Secret dev command *dev       | **Secret shortcut (dev only):** Type `*dev` (no focus in input/textarea) anywhere on the site to open /dev/playground in a new tab. Sequence resets after 2s or on wrong key; does not fire when typing in inputs. base.html inside {% if dev_available %}. |
| 2026-02-23 | Playground: magnifying glass  | **Magnify playground:** New scene at /dev/playground?scene=magnify. Hero headline + lens with controls for Lens size (--magnify-size, 80–220px) and Frame size (--magnify-frame-size, 120–320px). Variables applied to .magnify-playground wrapper so lens/frame inherit. magnify-lens.css --magnify-frame-size now overridable (var(..., 205px)). dev_playground/_scene_magnify.html, dev_playground_scenes.py. |
| 2026-02-23 | Brief skeleton + The issue page | **Reusable layout:** `_brief_skeleton.html` extends base; blocks: brief_title, brief_intro, brief_main_heading, brief_main, brief_sidebar, brief_layout_class. **Legislator brief:** Refactored to extend skeleton; content in blocks; sidebar unchanged. **The issue page:** Now uses same skeleton; content (What's happening, Why it matters, The fix, What you can do) in brief_main; main heading "Overview"; no sidebar. CSS: `.legislator-brief-layout--no-sidebar` at 769px uses single column and hides divider. templates/_brief_skeleton.html, the_issue.html, legislator_brief.html, base.css. |
| 2026-02-23 | Legislator brief states table: effective + How + Detail | **State table:** Column "When / where" → "Effective" (effective date or "In committee" only; dropped "Signed …"). How cells expanded (full mechanism text: statute/policy + scope); Detail simplified to "—" (content folded into How). content.py BRIEF_STATE_STATUS, legislator_brief.html headers/data-label. |
| 2026-02-23 | Legislator brief map/table toggle + shared key colors | **Map/table:** Single "States with bills passed or in progress" section with Table (default) / Map toggle. Same key colors for map and table via CSS variables on `.legislator-brief`: `--brief-color-passed`, `--brief-color-pending`, `--brief-color-policy`, `--brief-color-none`. Map swatches and table indicators (`.brief-table-indicator--yes`, `--pending`) use these; inline script reads variables for SVG fills. Toggle: `.brief-states-view`, `.brief-states-view-toggle`, `.brief-states-view-btn`; `data-view="map"|"table"` switches panels; aria-pressed and focus-visible. legislator_brief.html, base.css. |
| 2026-02-23 | Legislator brief US map by bill status | **US map:** Blank US states SVG (Wikimedia CC0) in `/static/images/us-states.svg`. Section "States with bills passed or in progress" above the state table: map fills states from table data — green = bill passed (TX, CO), orange = bill in progress (MA, OR, ME), blue = policy change (MI), gray = other states. Single source of truth: `BRIEF_STATE_STATUS` in content.py drives table rows and map; sidebar passed/current bills derived from it. Combined status: bill_status > policy > none. JS on object load applies fill from `data-state-status` JSON. Accessible: role="img", aria-label, map legend. base.css .brief-us-map-wrap, .brief-map-legend, .brief-map-swatch (--passed, --pending, --policy); legislator_brief.html map block + extra_scripts. **2026-02-23:** Zoom and pan removed; map is static (no drag/wheel). |
| 2026-02-23 | Legislator brief states table copy unified | **State table:** Headers renamed to How / When / where / Detail; intro shortened. Cell copy shortened and deduped. Data moved to content.py BRIEF_STATE_STATUS; template loops over brief_state_status. |
| 2026-02-23 | Legislator brief table responsive (compact scroll) | **State mechanisms table:** Table remains a table at all viewports. Wrapper has overflow-x: auto so narrow viewports scroll horizontally. On &lt;48rem: smaller font, tighter cell padding (4px 6px); State/Bill/Policy nowrap; How/When/Detail columns max-width 140px so they wrap and table stays compact. base.css .bill-actions-table-wrap, .legislator-brief-states-table @media (max-width: 47.9375rem). |
| 2026-02-23 | Legislator brief table Bill/Policy indicators | **State mechanisms table:** Two columns "Bill" and "Policy" with ✓ (yes) / — (no). Bill = legislation (statute or bill); Policy = administrative policy. Texas, CO, MA, OR, ME have Bill ✓; MA and MI have Policy ✓. Accessible: role="img" + aria-label on indicators; caption. Legend removed per request. base.css .brief-table-*. |
| 2026-02-23 | Brief table: orange for in-progress, legend re-added | **Bills not yet passed:** Orange ● (`.brief-table-indicator--pending`) for MA, OR, ME (bill in progress). Green ✓ (`.brief-table-indicator--yes`) for TX, CO (passed into law). **Legend:** Re-added key above table: green ✓ = Passed into law, orange ● = Bill in progress, — = No bill / no policy. legislator_brief.html, base.css. |
| 2026-02-23 | Legislator brief sidebar content from table | **Sidebar:** "Brief documents" h3 title for PDF list. Passed bills: Texas SB 1816, Colorado HB 25-1281. Current bills: Oregon SB 1213, Maine H.4053. Sources: 625 ILCS 5/3-401(c-1), table primary sources (TX, CO, MA RMV, MI SOS, OR, ME) with notes. content.py BRIEF_BILLS_*, BRIEF_SOURCES; legislator_brief.html brief-docs-heading. |
| 2026-02-23 | Legislator brief Documents Sources subsection | **Sidebar:** "Sources" subsection under Documents; same card style as bills/docs. Data: `BRIEF_SOURCES` (title, url). Under-link notes removed from Sources (notes kept for brief docs list). content.py, legislator_brief.html, base.css (.legislator-brief-sources). |
| 2026-02-23 | Legislator brief Documents bills subsection | **Sidebar:** Under Documents, subsections "Passed bills" and "Current bills" using same card style as PDF/doc items (`.legislator-brief-doc`, icon-doc). Data: `BRIEF_BILLS_PASSED` / `BRIEF_BILLS_CURRENT` in content.py (title, url); template `brief_bills_passed` / `brief_bills_current`. Wrapper `.legislator-brief-sidebar__content` holds docs list + bill lists; `.legislator-brief-sidebar__subtitle` for h3; `.legislator-brief-bills` same list layout as docs. base.css, legislator_brief.html, content.py. |
| 2026-02-23 | Legislator brief responsive mobile-first | **Layout:** Mobile-first: default single-column grid (header → Documents → main); at min-width 769px switch to 3-column grid (main \| divider \| sidebar). Touch: doc/source links min-height 2.75rem. Overflow: main body min-width 0, max-width 100%; table wrapper overflow-x auto. base.css .legislator-brief-layout, wide @media (min-width: 769px); no template changes. |
| 2026-02-23 | Legislator brief two-column  | **Layout:** Two-column (main content left, documents sidebar right). **Grouping:** (1) Title block: h1 + intro in `.legislator-brief-header` full-width; (2) Documents aside; (3) Main content (Executive summary onward). Documents and main content start on same line (tops aligned). Extra gap (2.5rem) between title section and content row. Main column: brief sections. Sidebar: "Documents" with file-type icon (PDF/Word SVG) + title per row. Responsive: stack at ≤768px (header first, then sidebar, then main); print: single column, sidebar below. base.css .legislator-brief-header, .legislator-brief-layout; legislator_brief.html. docs app-overview. |
| 2026-02-23 | Anonymous funnel + conversion (wired) | **Wired end-to-end:** Client creates/stores `ilga_anon_sid` in sessionStorage (hero-auth-strip.js getAnonSessionId); recordOutreachStep sends session_id when anon (index.html); both verify-code flows (hero + drawer) send anon_session_id and clear sessionStorage on success; sign-out clears anon sid. **Server:** validate_anon_session_id in security.py (shared); auth verify_code backfills step events; POST /outreach/step accepts anon session_id. **Conversion report:** GET /admin/outreach/conversion returns full shape: conversions (drawer_to_outreach, phone_to_call, drawer_to_email, signed_in_to_outreach) and volumes (identities_opened_drawer, users_completed_outreach, total_calls, total_emails, total_outreach_actions, identities_clicked_phone); 90-day window; denominator zero → 0.0. **Privacy:** Policy updated (usage/conversion measurement, ilga_anon_sid, 12-month retention, no sale); footer soft notice; tracking first-party only, disclosed, retention-bounded. docs db-and-outreach.md updated. |
| 2026-02-23 | Modern CSS (progressive enhancement) | **Container queries** (gated by `@supports (container-type: inline-size)`): drawer (`.phone-panel` → `@container drawer`), What's happening (`.whats-happening` → `@container section`), intel summary/committee/story/coalition cards. **:has()** (gated by `@supports selector(:has(*))`): drawer form groups and report-bug fields with `[aria-invalid="true"]` get invalid styling; `.card:has(.heat-score-pill)` gets extra padding. **aspect-ratio** (gated by `@supports (aspect-ratio: 1)`): member photo and placeholder (100/152), drawer call photo (1:1); fallback keeps explicit width/height. advocacy-cards.css, advocacy-drawer.css, advocacy-email.css, base.css, whats-happening.css, intelligence-dashboard.css, intelligence-tables.css. |
| 2026-02-23 | Outreach checkpoint tracking | **Funnel:** Step definitions in `outreach_steps.py` (call answered/no-answer, email). `OutreachStepEvent` model + migration 20260223100000; POST /outreach/step (member_id, outreach_type, step_slug). Server emits call_recorded/email_recorded/no_answer_recorded from record_outreach; client records drawer_opened, phone_clicked, staffer_name_captured, office_email_captured, end_call_clicked, interest_selected, wrapup_*, voicemail_toggled, end_call_clicked_vm; email: signed_in, subject_confirmed, details_filled, pdf_grabbed, send_clicked. **Email UX:** Guide pulse is one-time subtle animation then static highlight; focus-visible in drawer; prefers-reduced-motion. docs/development/db-and-outreach.md, TODOS. |
| 2026-02-23 | Your Senator/Rep not a badge | "Your Senator" and "Your Representative" moved out of the badge row into the name block as a constituency label (Option B). Badge row for senator/rep shows only member.badges + member.power_badges. Power Broker unchanged. _results_partial.html (card-constituency-wrap, card-constituency-label under name; badge row skips district pill for senator/rep); advocacy-cards.css (.card-constituency-label, focus-visible). |
| 2026-02-23 | Your Senator/Rep label above name | Constituency label ("Your Senator" / "Your Representative") moved above member name in card header. _results_partial.html (constituency block before h3.card-name); advocacy-cards.css (.card-constituency-wrap margin-top → margin-bottom). |
| 2026-02-23 | Your Senator/Rep pill styling | Constituency label styled as pill: .card-constituency-pill + role-senator (slate/blue) vs role-rep (soft purple); border-radius 9999px, minimal padding/border. _results_partial.html (pill + role class on label); advocacy-cards.css. |
| 2026-02-23 | Card badges: container query for width | Badges stack and go full width only when the **card** is narrow (container query). .card has container-type: inline-size, container-name: card. @container card (max-width: 400px) applies stacked full-width badge rules; wide cards get content-sized badges (max-width by content). Removed viewport-based badge rules from 768px media query so container query is single source of truth. advocacy-cards.css. |
| 2026-02-23 | Your goal: ZIP on same row, geolocation | **Layout:** ZIP moved to top row with "Your goal", right-aligned (.outreach-goal__head-row, .outreach-goal__zip-cell). **Geolocation:** "Use location" button uses browser geolocation and GET /advocacy/api/zip-from-coords (Nominatim reverse geocode) to set ZIP and refresh results. Status message (Locating… / Location unavailable) in aria-live region. _results_partial.html, index.html (setGoalZipStatus, zip-locate click), advocacy-cards.css (.outreach-goal__zip-locate, __zip-status); advocacy.py (_reverse_geocode_to_zip, zip-from-coords). |
| 2026-02-23 | Your goal: editable ZIP in sidebar | Below "Your goal" the current ZIP is shown (underlined). Click switches to a small input; on blur or Enter with valid 5-digit ZIP the hero form and URL update and results re-fetch. _results_partial.html (.outreach-goal__zip-display, .outreach-goal__zip-edit); index.html (showGoalZipEdit, hideGoalZipEdit, commitGoalZip); advocacy-cards.css (.outreach-goal__zip-*). |
| 2026-02-23 | Card grayed when called + emailed | Member cards with both call and email complete get class `card-outreach-complete`: grayed (opacity 0.7, gray bg/border), still interactive; hover/focus-within restores to 0.9. Optional aria-label for screen readers. _results_partial.html (outreach_complete, class + aria-label); advocacy-cards.css (.card-outreach-complete, .card-your-legislator variant). |
| 2026-02-23 | Call your Senator/Rep button dynamic in email drawer | **Label:** Button text and aria-label match the **current** card's member role: "Call your Senator" on the Senator's email drawer, "Call your Representative" on the Rep's email drawer. **Visibility:** The nudge renders only when the current member has **not** already been called (`current_member_already_called` = inverse of `show_call_nudge`). After call wrapup the button is always hidden (already called). **Backend:** `_role_label_for_member(member)` derives "Senator"/"Representative" from `member.chamber`; `current_member_role_label` and `current_member_already_called` passed to email template context in GET drawer view=email and POST wrapup. **Template:** _advocacy_drawer_email.html renders gmail-call-other-nudge only when `current_member_role_label` is set and not `current_member_already_called`; label is `{{ current_member_role_label }}`. JS "I sent it!" still uses getDrawerDoneContext + getGoalProgress for client-side show/hide. |
| 2026-02-23 | Call your Senator/Rep: hide when member already called | **Client:** "Call your Senator/Rep" nudge and done-view CTA now disappear when that member was already called for both anon and signed-in. Added `memberAlreadyCalled(memberId, zip)`: anon uses localStorage (getGoalProgress); signed-in also checks goal block DOM (`.outreach-goal__action--done` for that member's call). Used in showDrawerDoneForMember (otherAlreadyCalled), "I sent it!" nudge reveal (otherCallDone), and initDrawerEmailActions (call-first banner). On "I sent it!" nudge we set `data-nudge-member-id`; on `ilga:goal-updated` (detail.memberId, kind) we hide the nudge if it targets that member. setGoalActionDone now includes memberId and kind in event detail. index.html. |
| 2026-02-23 | Member-to-member drawer nudge | **Email path:** After "I sent it!" the email UI stays in place; a "Call your [Senator/Representative]" nudge appears next to the "I sent it!" button and opens the other district member's call drawer. **Call path:** "I'll do it later" now closes the drawer (no done view). Goal sidebar "Next: Call your [Rep/Senator]" unchanged. index.html (getDrawerDoneContext used for nudge; "I sent it!" shows in-email nudge instead of tryShowDrawerDoneForMember; later-btn handler calls closeAdvocacyDrawer); _advocacy_drawer_email.html (gmail-call-other-nudge, gmail-call-other-btn); advocacy-email.css (.gmail-action-bar-nudge, .gmail-call-other-btn); drawer-done template still used for "Call your Senator/Rep" CTA from email path. **Fix:** Bottom "Call your Senator/Rep" pill only appears after "I sent it!" and only when the other district member has not been called; init forces nudge hidden; show logic uses memberAlreadyCalled. |
| 2026-02-23 | Call-first + CTA for signed-out / done view | **Signed-out:** Server always sends show_call_nudge=True (no DB for anon), so "Call first when you can" stayed visible after calling. **Fix:** In initDrawerEmailActions, hide the .gmail-banner-tip when getGoalProgress(zip)[currentMemberId].call so localStorage goal progress hides the banner. **Done view (both signed-in and signed-out):** "Call your [Senator/Rep]" CTA was always shown when an other member exists; it now checks getGoalProgress(zip)[otherId].call and hides the CTA (shows district-done / broker nudge) when the other was already called. advocacy.py (debug log only); index.html initDrawerEmailActions, showDrawerDoneForMember. |
| 2026-02-23 | Your goal: numbered steps by moneyball | Goal checklist is an ordered list (1–4): call then email for the **higher moneyball** member, then call then email for the other. "Next" shows the first incomplete step in that order. advocacy.py (goal_steps, goal_next_step from your_legislators order); _results_partial.html (\<ol\>, goal_steps loop, single #outreach-goal-next); index.html updateGoalBlockFromStorage (first incomplete row → next link); advocacy-cards.css (.outreach-goal__actions--numbered). |
| 2026-02-23 | Your goal progress bar truck emoji | Pickup truck (🛻) at end of **green fill** (inside `.outreach-goal__bar-fill`), facing right (`scaleX(-1)`). Positioned absolute right: 0 so it moves with progress; bar overflow: visible so icon can show. _results_partial.html, advocacy-cards.css (.outreach-goal__bar-truck). |
| 2026-02-23 | Your goal 4-action gamified   | Your goal sidebar: 4 actions (call/email Senator, call/email Rep), progress bar (X of 4 complete), actionable Call/Email links opening drawer. Completed actions grayed out (opacity/color transition), moved to bottom via flex order; link gets aria-disabled and pointer-events: none. Logged-out progress in localStorage (ilga:goal:{zip}); logged-in from server. advocacy.py (senator_emailed, rep_emailed, district_goal_done, district_goal_total); _results_partial.html (goal block, bar, checklist, data-goal-*, outreach-goal__action--done); index.html (getGoalProgress, setGoalActionDone, updateGoalBlockFromStorage, ilga:goal-updated, initGoalBlockFromStorage); advocacy-cards.css (.outreach-goal__bar*, .outreach-goal__action*, .outreach-goal__action--done); docs app-overview. |
| 2026-02-23 | 2/2 celebration + Power Broker CTA | When both district members are called: celebration block "You've reached both your district members" and optional "Want more impact? Call the Power Broker" CTA that opens the Power Broker card's call drawer. Gate is 2/2 called (not 4/4 actions). advocacy.py (both_district_members_called); _results_partial.html (.outreach-goal__celebration, CTA link); advocacy-cards.css (.outreach-goal__celebration*, .outreach-goal__cta-broker); docs app-overview. |
| 2026-02-23 | Your goal: server progress on load | Goal and progress bar now reflect **account data** on page load. Backend: advocacy.py normalizes member IDs to strings for OutreachEvent lookup so senator_called/rep_called/senator_emailed/rep_emailed are correct for signed-in users. Client: updateGoalBlockFromStorage only applies localStorage when hasStoredProgress (anon); when no stored data we leave server-rendered HTML. ilga:auth-change → refreshAdvocacyResults re-fetches results after sign-in. |
| 2026-02-23 | Goal + cards: signed-in vs signed-out | **Signed-in:** Progress from server (OutreachEvent). Goal block and member cards both get user_called_member_ids/user_emailed_member_ids from context; template renders "Called!" / "Emailed!" on cards and ✓ in goal. **Signed-out:** Progress in localStorage (ilga:goal:{zip}). updateGoalBlockFromStorage applies it to the goal block and now also syncs member cards by calling setOutreachDone(memberId, kind) for each stored action so cards show "Called!" / "Emailed!" and match the goal section. index.html updateGoalBlockFromStorage. |
| 2026-02-23 | Transfer localStorage progress on sign-in | When the user logs in or creates an account, any call/email progress stored in localStorage for the **current ZIP** is transferred to their account: `mergeLocalStorageGoalToServer()` POSTs each (member_id, kind) to `/outreach/record`, then removes that ZIP’s key from localStorage and refreshes the auth strip and results. Only the constituent ZIP (from getConstituentZip()) is merged. index.html (postOutreachRecord, mergeLocalStorageGoalToServer, ilga:auth-change); docs app-overview. |
| 2026-02-23 | Card grayed (opacity): logged-in + signed-out fix | **Logged-in:** Template was comparing int `member.id` to set of strings; `member.id in user_called_member_ids` was always false. Fixed by normalizing in macro: `member_id_str = member.id|string` and using `member_id_str in user_called_member_ids` so `card-outreach-complete` is applied when server sends both IDs. **Signed-out:** updateGoalBlockFromStorage only called setOutreachDone (button state); it did not add `card-outreach-complete` to the card. After syncing buttons, we now add/remove `card-outreach-complete` and aria-label per card from localStorage so grayed opacity matches. _results_partial.html, index.html updateGoalBlockFromStorage. |
| 2026-02-23 | Your Legislator re-imagine    | Advocacy results: “Your district members” (Senator + Rep side-by-side), “Strategic Targets” (Power Broker) below. Sidebar: “Your goal” + progress (District members X/2 called, Senator ✓ · Rep ○); when 2/2, optional Power Broker nudge. advocacy.py (senator_called, rep_called, district_called_count, district_goal_total); _results_partial.html (goal block, district row); advocacy-cards.css (.outreach-goal, .district-members-row); docs app-overview. |
| 2026-02-23 | Community legislator email   | Community-sourced emails: when callers enter an office email in the call wrap-up and record the call, it is stored in `community_member_emails` for members with no public email. Drawer and wrap-up use effective email (public or best community); UI shows "From a previous caller" / "Verified by multiple constituents" attribution. DB: migration 20260223000000, `CommunityMemberEmail`; persist in POST /outreach/record (optional `legislator_email`); read path in `community_email.py`; client passes email into `recordOutreach`. Templates: _advocacy_drawer_email.html, _advocacy_drawer_call.html; docs app-overview, TODOS. |
| 2026-02-23 | Called!/Emailed! buttons open drawer | "Called!" and "Emailed!" on member cards are clickable; they open the drawer to call or email view and show the last state (wrap-up for call, email tab for email). Template: use buttons with same data-* and onclick as action buttons; setOutreachDone updates button in place instead of replacing with span; CSS: remove pointer-events: none, add cursor pointer and focus-visible. _results_partial.html, index.html, advocacy-form.css. |
| 2026-02-23 | Call drawer wrap-up state on reopen | Reopening the call drawer for a member the user already called shows the wrap-up state: Nice work + poll (with saved interest level), notes, staffer name, and draft-email CTA. GET /advocacy/drawer queries last call event for user+member; template renders wrap-up visible and pre-filled when call_completed. JS restoreDrawerWrapupState() fetches interest-poll and renders results. advocacy.py, _advocacy_drawer_call.html, index.html, dev_playground_scenes.py. |
| 2026-02-23 | Call recorded on poll submit | Call is recorded when the user selects an interest level (1–5) after End call; draft-email flow no longer records the call. Primary CTA "Draft your follow-up email", optional "I'll do it later" shows confirmation. index.html pill handler + draft handler; _advocacy_drawer_call.html, advocacy-drawer.css. |
| 2026-02-23 | Email drawer gate "I sent it!" on sign-in | Anonymous users can use the drawer and mailto; "I sent it!" does not call the API. Instead they see "Sign in to save this outreach" (text stays; no revert), guide moves to Step 0 (Sign in), From row scrolls into view and From input is focused when visible. No localStorage. Signed-in flow unchanged (POST /outreach/record, card update). index.html initDrawerEmailActions. |
| 2026-02-23 | Constituent flag for email and call outreach | recordOutreach now accepts optional constituent (8th param); when 'true' or 'false' it is sent to POST /outreach/record. Email "I sent it!" and call wrap-up both read data-drawer-constituent from the drawer and pass it so the API stores constituent for both kinds. index.html. |
| 2026-02-23 | No-answer path: record + outcome screen | Voicemail "End call" records no_answer via POST /outreach/record (when signed in), then fetches POST /advocacy/call/{id}/no-answer and replaces drawer body with no-answer partial ("No problem — offices get busy", Send email instead / Close). Anonymous users see the same screen without recording. recordOutreach only runs setGoalActionDone/setOutreachDone for kind call/email so no_answer does not mark the card as "Called!". index.html initDrawerEndCall (#drawer-end-call-vm); docs db-and-outreach. |
| 2026-02-23 | How we pick targets closed by default | "How we pick targets" &lt;details&gt; section now loads collapsed; removed \`open\` attribute in _results_partial.html so users expand it on demand. |
| 2026-02-23 | Outreach sidebar card refinement | My Outreach cards: no rounded corners (border-radius 0 on card and photo); slimmer height — photo 100px→64px, tighter info padding and gaps, smaller type and badge spacing. advocacy-cards.css .outreach-sidebar-card*, _results_partial.html img 64×64. |
| 2026-02-21 | Hero–results separator (advocacy) | Horizontal line between advocacy hero and members content; same padding (32px) below the line as hero bottom. .hero-results-sep in index.html + advocacy-form.css; aria-hidden on divider. |
| 2026-02-21 | User ZIP code (account)       | Zip is now stored on the **user account** (`User.zip_code`) as well as per outreach event. Advocacy page pre-fills ZIP for logged-in users from `user.zip_code`; visiting /advocacy with a valid ?zip= or recording outreach with a zip updates the user's saved zip. Migration 20260221100000; fallback ALTER in db.py. |
| 2026-02-21 | Hero link styling             | "Learn about the issue" and "Brief for legislators" (.hero-learn-link) and "Pick up where you left off..." (.auth-strip-signin-btn) no longer underlined: plain-text color (#555 / inherit) with hover color change to #1a73e8. advocacy-form.css. |
| 2026-02-21 | Hero / What's happening UI    | Removed double lines between What's happening and "Pick up where you left off": .advocacy-hero border-bottom and .whats-happening border-top removed; gap kept (margin-top 2rem on section). Hero learn links on one row: "Learn about the issue or Read the Brief for legislators" with only "Learn" and "Read" as links. _hero_home.html, advocacy-form.css, whats-happening.css. |
| 2026-02-21 | Hero → What's happening gap   | Gap tightened: .whats-happening margin-top 2rem → 1.25rem; subtle border-top on section for clear break. Intro line under h2: "Registration blocked, statutory gap, narrow fix." (.whats-happening-intro) so the section feels fuller and the transition less empty. whats-happening.css, _whats_happening.html. |
| 2026-02-21 | Hero ZIP pre-fill bug        | Fixed: hero and advocacy hero ZIP inputs now pre-fill with valid 60007 (in dev/mocks); was 60601 which is not in district data. DEFAULT_HERO_ZIP in advocacy.py; base.html dev bar fallbacks; seed notice and placeholders updated. |
| 2026-02-21 | Hero outreach box alignment  | Outreach box (ZIP input + Start Outreaching) and "Pick up where you left off" centered: .advocacy-hero--centered .hero-form margin auto; mobile hero-meta no longer forced to text-align/justify left so auth strip stays centered. advocacy-form.css. |
| 2026-02-21 | Home hero sign-in            | "Pick up where you left off or start tracking your outreach" on home now starts sign-in: hero auth-strip + inline sign-in moved to shared static/js/hero-auth-strip.js, included from base.html so home and advocacy both get it. index.html duplicate getCsrfToken, updateAuthStrip, initAuthStrip, initHeroInlineSignin removed; refreshAuthStripProgress implemented in shared script (GET /outreach/my-stats). |
| 2026-02-21 | Advocacy hero H1 refined    | Advocacy page uses its own headline: "Find your legislators." / "Take action on Kei vehicle registration." (with "Take action" highlighted via .hero-headline-mark). Home keeps issue-focused H1 ("Fix Illinois Law." / "Allow Kei Vehicle Registration."). _hero_context_advocacy() in advocacy.py; used for advocacy index and search-error responses. |
| 2026-02-21 | Home/advocacy hero fix       | Home and advocacy 500s fixed: both referenced missing _hero_shared.html. Home now includes _hero_home.html; advocacy (index.html) includes _hero_advocacy.html. "What's happening now" not used; pages show hero only (home: split hero + learn links; advocacy: centered hero + results). |
| 2026-02-21 | Home page + shared hero     | Home at GET / with shared hero (routers/home.py, templates/home.html). Hero in _hero_home.html (split layout, learn links); advocacy hero in _hero_advocacy.html (centered). Trust badges removed. Form on home redirects to /advocacy/?zip=. advocacy-form.css: .hero-learn-link-wrap, .hero-learn-link + focus. |
| 2026-02-21 | What's happening (home)     | Section below hero on home only: one row, 3 parts (What / Why / Fix). What: registration denied or revoked; Why: statutory interpretation gap; Fix: narrow clarification amendment. One plain-English bullet per part (625 ILCS, statutory clarification, narrow amendment). templates/_whats_happening.html, static/css/whats-happening.css, linked in base.html; responsive stack at ≤768px. |
| 2026-02-21 | Mission statement (home)   | Section below What's happening on home: "Mission Statement" heading (top-left), large italic Helvetica bold quote left-aligned, attribution "– Land of Kei Illinois advocacy group:" bottom-right. templates/_mission_statement.html, static/css/mission-statement.css; included in home.html after _whats_happening.html. |
| 2026-02-21 | Share section (home)        | Section below What's happening on home: "Share" heading, "Help spread the word.", Copy link (clipboard API) + optional native Share button when navigator.share exists. Accessible: aria-label, role=status/aria-live for copy feedback, focus ring. templates/_share_section.html, static/css/share-section.css; inline script in partial for copy + share. |
| 2026-02-21 | URL/query param security     | Audit: zip already validated (_ZIP_RE). **photo_url** on GET /advocacy/drawer was passed into \<img src\> unvalidated — fixed with validate_photo_url_for_drawer (security.py): allow only relative paths or https://www.ilga.gov/; reject protocol-relative, javascript:, data:, other origins. Dev playground not in scope (dev-only, never exposed). feedback **submitted** is boolean-only. No open redirects. Tests: test_advocacy_photo_url_validation.py. |
| 2026-02-21 | Dev sign-in code visibility  | When SMTP unset, auth code printed to **server** stderr (banner + LOGGER.warning) so it appears in the terminal where uvicorn runs. Hero sign-in code step shows dev hint: "Dev mode: your code appears in the server terminal (where you ran the app), not in the browser." advocacy-form.css .hero-signin-dev-hint; index.html {% if dev_available %}. |
| 2026-02-21 | Dev request echo               | In DEV_MODE, every request is echoed to stderr (e.g. `GET /advocacy/ 200 (12.3ms)`) so incoming routes are visible in the terminal; LOGGER.info unchanged. middleware.py. |
| 2026-02-21 | Fire pill text + animation   | Heat-score pill text size increased (--font-size-xs → --font-size-sm, padding 4px 10px). Fire icon: static emoji by default; on hover and when pill is expanded (data-expanded), animated fire GIF is shown (Giphy direct URL; can be replaced with self-hosted static/images/fire.gif). advocacy-cards.css .heat-score-pill, .heat-score-pill-fire; _results_partial.html. |
| 2026-02-22 | My Outreach hook-up (member)  | "My Outreach" shows for signed-in users even with 0 calls/emails (show_my_outreach in context; template uses show_my_outreach\|default(false)). After recording call/email, results partial is re-fetched (POST /advocacy/search with HX-Request) so sidebar counts and cards update without full reload. refreshAdvocacyResults() in index.html; recordOutreach calls it after success. advocacy.py, _results_partial.html, index.html. |
| 2026-02-22 | Outreach cards refresh on auth | After sign-in or sign-out, outreach cards now update without page refresh: hero-auth-strip.js dispatches custom event \`ilga:auth-change\` (after sign-in success and after logout completes); index.html listens and calls refreshAdvocacyResults() so the results partial is re-fetched with current session. hero-auth-strip.js, index.html. |
| 2026-02-22 | Advocacy page scroll behavior | Auto-scroll to member results only when the user just submitted the zip form, not on plain navigation. SessionStorage flag \`__ilga_scroll_to_results\` set on form submit; on load we scroll only if that flag is set (then clear it). Navigating to /advocacy/ or /advocacy/?zip=... stays at top; entering zip and submitting scrolls to results. index.html. |
| 2026-02-21 | Outreach sidebar (advocacy)   | Right aside on advocacy results: "My Outreach" section below "How we pick targets". Stacked grid of white rounded cards (one per member contacted); each card shows member photo/initials, name, chamber–district, call/email badges. Gold border for constituent members, gray for others. Data from unscoped user outreach query + state.member_lookup_by_id; outreach_sidebar in context. advocacy.py, _results_partial.html, advocacy-cards.css. |
| 2026-02-21 | Outreach sidebar photo + glow | Sidebar card photos: height 56px→92px, object-position center top so faces not cut off; main card .member-photo 128px→152px (responsive 122→144, 112→132). Constituent card (.outreach-sidebar-card--constituent, .card-your-legislator): 3px border #b8860b + layered box-shadow glow. advocacy-cards.css, _results_partial.html img height. |
| 2026-02-21 | My Outreach card height + type | More vertical height: sidebar card min-height 200px, photo 92px→112px, info padding and grid gap increased. Name 0.8125rem→0.9375rem, district 0.75rem→0.875rem; call/email badges 0.8125rem→1.25rem. advocacy-cards.css, _results_partial.html img height=112. |
| 2026-02-21 | Advocacy hero magnifying lens  | Liquid Glass–style magnifying glass over the hero headline on /advocacy: follows cursor, magnifies content underneath via cloned scaled headline; click toggles classic handle on/off. static/css/magnify-lens.css, static/js/magnify-lens.js; wrapper .hero-headline-lens-wrap; prefers-reduced-motion: static over "F"; hidden on touch/mobile (≤768px). base.html + _hero_advocacy.html. |
| 2026-02-23 | Lens fade at end of line        | Magnifying glass fades out once it reaches the end of the text line (outro at end of intro). Re-enabled when the user navigates back into the headline (mouseenter → showLens; mouseleave → hideLens). magnify-lens.js. |
| 2026-02-21 | Lens intro animation           | On load (desktop, no reduced motion): lens sweeps across the entire first headline line (left to right over .hero-headline-line), then fades out. Duration 2.2s; --magnify-intro-duration set in JS; getLineBoundsInWrapper for start/end; works for any first-line text. magnify-lens.js runIntroAnimation; magnify-lens.css --magnify-intro-duration. |
| 2026-02-20 | Error experience             | Custom 404/500 (and 422) HTML pages (same chrome as site); global exception handlers in main.py for HTTPException, RequestValidationError, and uncaught Exception; _wants_html(request) for HTML vs JSON; catch-all route for unmatched paths; server-side logging for 500s (no stack trace in response). Templates: 404.html, 500.html, 422.html; base.css .error-page. |
| 2026-02-21 | 404 Kei vehicle facts        | 404 page no longer shows Home/Advocacy links; shows a random Kei vehicle fun fact (KEI_VEHICLE_FACTS in main.py), hint to refresh for another; _404_context(request), .error-page-fact, .error-page-hint in base.css. |
| 2026-02-21 | 404 fun fact images          | Facts are dicts with text + optional image, image_alt, image_credit; 4 facts have CC-licensed Wikimedia Commons photos (N-BOX, N-BOX Custom, Hijet truck, Jimny). 404.html shows figure + img + figcaption when present; base.css .error-page-fact-figure, .error-page-fact-img, .error-page-fact-credit, .error-page-fact-text. |
| 2026-02-21 | Branch sync & PR #21         | Merged main into feature/refactor-data-source-playground-2026-02-21 (PR #20: CSRF reuse, async Turnstile, explore/intelligence template globals, rate-limiter deque). No conflicts. PR #21 title/body updated to describe branch changes and pulled-from-main fixes. |
| 2026-02-21 | Dev component playground     | Dev-only GET /dev/playground (and /dev/playground/{scene_id}) to isolate UI components (truck animation, drawer call/email) without full flows. Scene registry in dev_playground_scenes.py; truck partial _report_bug_truck.html; drawer wrappers with mock context. Docs: development/component-playground.md. |
| 2026-02-21 | Beta banner copy             | Banner no longer says "may have bugs" or shows "Report a bug" link (footer + drawer nudges keep report link). New copy: "You're early — we're in beta. Thanks for being here." config.py comment updated. base.html. |
| 2026-02-20 | Performance & resilience     | Static assets: StaticFilesWithCache in main.py sets Cache-Control: public, max-age=3600 for /static. Offline/failure: connection-error banner in base.html (role=alert, aria-live=polite) shown on htmx:responseError and htmx:sendError; dismissible. Offline indicator shown when navigator.onLine is false, hidden on online. window.showConnectionError() for optional fetch use. base.css .connection-error-banner, .offline-indicator. Docs: reference/performance-resilience.md. |
| 2026-02-20 | Truck animation → bug report | Removed all ZIP/member-cards loading behavior; HTMX direct swap. Bug report: truck + status during submit via fetch; success page (?submitted=1) is green box only, no truck. Fix: server responds in &lt;20ms so redirect was at 600ms—animation never visible. Now 3.5s truck animation with dashed horizontal path; truck drives left → stops at center ("Picking up bug") → drives to end ("Sent!"); 🐛 at center fades when truck stops (picked up). Status steps: "Sending…" → "Picking up bug" (1.1s) → "Sent!" (2.8s). base.css report-bug-truck-path (dashed), report-bug-bug, report-bug-truck-drive 3-phase keyframes; report_bug.html statusSteps, setTimeout timing. Cleanup: responsive track (width 100%, max 280px, min 200px); --report-bug-truck-duration; prefers-reduced-motion (no movement, bug fades only); small-screen smaller emoji; JS TRUCK_DURATION_MS, clearStatusTimers before redirect. |
| 2026-02-20 | Beta banner (minimal)        | Solid #f0f0f0, max-width 1120px to match .container, no gradient/animation, minimal text + link + dismiss; responsive padding to align. base.css. |
| 2026-02-20 | Micro-interactions (confetti-like) | Pop/thunk on key moments: "I sent it!" (pop+shimmer), End call (thunk), card Called!/Emailed! (pop), reminder Copy/Calendar (pop). **Start My Outreach** CTA: canvas-confetti on click (origin at button), amber/gold palette; confetti script in base.html head; prefers-reduced-motion skips confetti. Report-bug success has no animation. base.css keyframes + utilities; advocacy-email, advocacy-form, index.html. prefers-reduced-motion respected. |
| 2026-02-20 | Beta banner + in-app bug report | Site-wide dismissible bar; “Report a bug” → in-app form at /report-bug (no GitHub/service). Form: description, optional email, stored in bug_reports; optional email to BETA_BANNER_EMAIL if SMTP set. Email body: timestamp, email, issue, page, screenshot (inline + link or “No image sent”), IP/User-Agent. Override with FEEDBACK_URL or EMAIL for external/mailto. |
| 2026-02-20 | Bug nudge in advocacy drawer | No longer in drawer header. Call: "Something went wrong? Report a bug" at end of script (after End call) and in voicemail (_advocacy_drawer_call). Email: same copy under action bar (Open in your email app / I sent it) (_advocacy_drawer_email). No-answer drawer same copy. Gated on show_beta_banner and beta_banner_feedback_url. advocacy-drawer.css .drawer-bug-nudge, .gmail-bug-nudge. |
| 2026-02-20 | Form submission security     | CSRF (double-submit cookie XSRF-TOKEN + body), rate limiting (bug report, request-code, verify-code), page_url validation (http/https only). /report-bug exempt from API key. security.py; docs env vars + Security section. |
| 2026-02-20 | Turnstile CAPTCHA (optional) | Cloudflare Turnstile on bug report form when ILGA_TURNSTILE_SITE_KEY + SECRET_KEY set. Free 1M/mo; server-side siteverify. feedback router + report_bug template. |
| 2026-02-20 | Report bug description min length | "What went wrong" requires ≥20 chars. Backend: BUG_REPORT_DESCRIPTION_MIN_LENGTH; redirect ?error=description_short. Template: minlength, inline error #description-error, aria-invalid/aria-describedby. Client-side validation before fetch; clear error on input when valid. base.css: .report-bug-field--error, .report-bug-field-error (red text, border, focus ring). |
| 2026-02-20 | Report bug email validation | Optional reporter email validated server-side (one @, dot in domain, length limits); redirect ?error=email_invalid. Template: inline error #email-error, aria-invalid/aria-describedby; client-side isValidEmail on input/blur and submit with graceful messages (e.g. "Please enter a valid email address or leave the field empty"). |
| 2026-02-20 | Branch cleanup               | Removed commented intel nav, consolidated tooltip init to `initAdvocacyTooltips`. |
| 2026-02-20 | Defect pass                  | test/lint pass; smoke GET /advocacy, POST search, drawer 404; power broker, Tippy verified. |
| 2026-02-20 | Recommendation chip tooltips | Tippy.js: one tooltip at a time, appendTo body, theme recommendation-chip; Popper 2 + Tippy 6. |
| 2026-02-20 | Power Broker logic           | Chair for topic (default Transportation) or highest Moneyball outside district; exclude_senate_district/exclude_house_district. |
| 2026-02-20 | Potential Ally removed       | 3 cards only: Your Senator, Your Rep, Power Broker; seating redo later. |
| 2026-02-20 | The Land of Kei branding     | SITE_NAME, META_DESCRIPTION, footer, auth email, .env.example, docs. Advocacy page title now uses ILGA_SITE_NAME ("{{ site_name }} — Find Your Targets"). |
| 2026-02-21 | Hero ZIP pre-fill fix        | Default hero ZIP was 60601 (not in dev district data). Now DEFAULT_HERO_ZIP=60007 in advocacy.py; home and advocacy heroes pre-fill with 60007 in dev/mocks; dev bar and advocacy search error also use 60007; seed notice and placeholders list valid dev ZIPs. |
| 2026-02-20 | Dev ZIP autofill             | ILGA_DEV_MODE=1 autofills hero ZIP when no ?zip= (now uses valid 60007). |
| 2026-02-20 | Auth strip outreach progress | Signed-in users see "Called X legislators and sent Y emails" under verified email. GET /outreach/my-stats; refresh on sign-in and after recording call/email. index.html #auth-strip-progress, advocacy-form.css .auth-strip-progress. |
| 2026-02-20 | ZIP in URL on search         | history.replaceState ?zip=XXXXX on form submit; shareable links. |
| 2026-02-20 | Member cards enter animation | card-fade-slide-up stagger; prefers-reduced-motion respected. |
| 2026-02-21 | Hero outreach stat = total actions | Hero ticker: single number = total outreach actions (each call and each email count; call+email for one member = 2). get_outreach_aggregate in outreach.py counts all call/email events. Docs: db-and-outreach.md. |
| 2026-02-20 | Hero copy refresh             | Subhead: statutory gap + pre-written script in under a minute; ticker "Join X+ Illinois residents who've already taken action this week"; CTA "Start My Outreach". advocacy.py hero_subhead (2 places), index.html ticker + find-btn. |
| 2026-02-20 | Hero animation sequence       | Sharpie fades in first on load (0s); after --hero-highlight-delay (0.5s) the underline/highlighter reveal runs. advocacy-form.css: .advocacy-hero --hero-highlight-delay, .hero-headline-mark::after animation-delay. |
| 2026-02-20 | Hero "Fix" sharpie circle     | Sharpie PNG overlay around word "Fix" in hero line 1. advocacy.py hero_headline_line1_* vars; index.html .hero-headline-sharpie-wrap + img sharpie.png; advocacy-form.css .hero-headline-sharpie, scale/position fixed to word. |
| 2026-02-20 | Hero headline responsive size | Desktop unchanged (clamp 1.75rem–2.75rem). Tablet ≤768px: clamp(2.25rem, 6.5vw, 3.5rem). Phone ≤480px: clamp(2rem, 11vw, 3.25rem). advocacy-form.css .hero-headline in media queries — max-width-as-possible at small breakpoints. |
| 2026-02-20 | Hardball advocacy landing    | Threat headline, Anton/Impact, two-column hero, CTA "Start My Outreach", ticker, trust badges, hero alignment. |
| 2026-02-20 | Refactor round               | Intelligence/explore routers, CSS split; main ~1700 lines. |
| 2026-02-20 | Advocacy router cleanup      | Removed debug logging; E501/F401 fixed; lint + test pass. |
| 2026-02-20 | Drawer open/close animation  | Snappy slide: --drawer-duration 0.28s, --drawer-ease cubic-bezier(0.33,1,0.68,1); overlay + panel in sync; no fade-in on panel. prefers-reduced-motion: 0.01s. advocacy-drawer.css. |
| 2026-02-20 | Drawer close-then-open       | Open another drawer → close first, then open after 300ms (DRAWER_CLOSE_MS). |
| 2026-02-20 | Call drawer overflow         | overflow-x hidden, min-width 0, word-break on mobile. |
| 2026-02-20 | Mobile drawer URL/scroll     | 95dvh, overscroll-behavior contain, body scroll lock + restore. |
| 2026-02-20 | Advocacy mobile responsiveness | Media queries moved from base.css into advocacy-drawer/cards/form. |
| 2026-02-20 | Privacy and Terms + footer   | Privacy policy at /privacy and Terms of use at /terms; legal router (routers/legal.py); templates privacy.html, terms.html with .legal-page styling; footer in base.html updated with Privacy and Terms links (always shown); docs app-overview Legal and trust. |
| 2026-02-20 | Security hardening           | CSP (Content-Security-Policy-Report-Only by default; ILGA_CSP_ENFORCE=1 to enforce; ILGA_CSP_REPORT_URI optional). HSTS opt-in (ILGA_HSTS_ENABLED=1 when site is HTTPS). Prod startup warning if ILGA_APP_BASE_URL is not https://. Docs: deployment Security headers, env vars (CSP/HSTS), status-report HTTPS+base URL. |
| 2026-02-20 | Post-prod                    | OG/Twitter cards, canonical, Umami, advocacy SEO globals. |
| 2026-02-20 | SEO sitemap and robots       | GET /sitemap.xml (key pages: /, /advocacy, /intelligence, /explore) and GET /robots.txt (allow all, Sitemap line); both use APP_BASE_URL. Exempt from API key. Docs: environment-variables, deployment, app-overview. |
| 2026-02-21 | Intelligence sitemap/redirect | Sitemap listed /intelligence (no slash) but mounted router only serves /intelligence/; GET /intelligence could 404. Added redirect GET /intelligence → /intelligence/ in site.py (same pattern as /advocacy); sitemap canonical set to /intelligence/. |
| 2026-02-20 | Automated deploy Vultr       | Push main → CI → SSH deploy; DEPLOY_HOST/USER/SSH_KEY. |
| 2026-02-19 | Deployment prep              | Lint, Procfile, status-report, deployment.md, vultr-deployment-guide, startup banner URLs. |
| 2026-02-20 | Typography normalization     | Removed fixed/inconsistent text sizes and styles that caused bugs. variables.css: added --line-height-tight. All static/css: font-size and line-height use design tokens (--font-size-base, --font-size-body, --font-size-label, --font-size-sm, --font-size-xs, --font-size-h1, --font-size-h2, --line-height-body, --line-height-tight). No more px line-heights or arbitrary em/rem; hero clamp() kept for responsive headlines. base, advocacy-cards, advocacy-drawer, advocacy-form, advocacy-email, intelligence-dashboard, intelligence-tables. |
| 2026-02-20 | Mobile experience overhaul  | Typography: variables.css font-size tokens, :root 17px at 480px; base.css body/headings use tokens. Cards: rem-based mobile text, larger member photos (96/88px), 44px touch targets (card-details-toggle, beta-banner-dismiss). Drawer/email: larger drawer photo (80px), bumped smallest font sizes. Intelligence: responsive summary grid, .intel-table-scroll-wrap, font-size floor 0.8rem at 480px. Templates: explanation partial classes, predictions/bill inline font-sizes to rem. |
| 2026-02-20 | Mobile typography scale-up   | variables.css: at 480px base 18px, h1 2rem, h2 1.375rem, body/label tokens. Hero: .hero-inner full-width on phone; .hero-headline 1.875rem (phone) / 1.75rem (tablet), eyebrow/subhead larger. base.css: explicit body/h1/h2/footer at 480px. advocacy-drawer.css: mobile labels 0.9375rem, panel title 1.2rem. Branch: feature/mobile-typography. |
| 2026-02-20 | Moneyball help circle (mobile) | .moneyball-help no longer forced to 44px on mobile; stays content-sized (1.1em) so circle fits the "?" only. advocacy-cards.css. |
| 2026-02-20 | Badges keyboard-accessible   | tabindex=0, role=button, aria-label; Tippy focus trigger; focus ring. |
| 2026-02-19 | Accessibility pass           | Drawer role=dialog, focus trap, landmarks, role=alert/status. |
| 2026-02-19 | Humanized advocacy copy      | Conversational tone across drawer, errors, guide steps. |
| 2026-02-19 | ZIP search loading animation  | (Removed 2026-02-20: truck moved to bug report success; ZIP now direct HTMX swap.) |
| 2026-02-19 | Advocacy error states        | Drawer load 404/5xx, I sent auth/network, wrapup error, empty results, drawer-email-open sync, email mobile. |
| 2026-02-19 | Mobile bottom sheet drawer   | ≤768px bottom sheet, drag handle, safe-area-inset for notch. |
| 2026-02-18 | Lint                         | All E402/E501 fixed; per-file ignores; make lint. |
| 2026-02-19 | Pre-commit                   | E501 in config/advocacy; make pre-commit. |
| 2026-02-18 | Status report                | status-report.md: broken/buggy/missing, deployment checklist. |
| 2026-02-19 | DB setup docs                | db-and-outreach.md: data flow, fire pill DB-driven, checklist. |
| 2026-02-18/19 | Guided Email (Mad Libs)   | Traveling outline, mad lib blanks, constituent toggle, To pill, after-call smart email, drawer header constituent. |
| 2026-02-18 | Outreach scripts             | Call/voicemail/email templates; target_type; wrapup call_date. |
| 2026-02-18 | Deployment readiness         | / → /advocacy; /intelligence exempt from API key; deployment.md. |
| 2026-02-18 | Outreach heat pill            | 🔥 N from COUNT distinct user_id; two-DB; seed mock advocates. |
| 2026-02-18 | Unified bill scrape          | scrape_bill_complete(), fulltext externalized, incremental_bill_scrape, _log.py. |
| 2026-02-18 | Auth + outreach DB           | SQLite, email-code auth, OutreachEvent, heat/called/emailed state, interest poll, seed, smoke test. |
| 2026-02-18 | Cursor subagents             | backend.md, tests.md. |
| 2026-02-18 | Dev Bar                      | ?dev floating toolbar; Advocacy/Intelligence/Explore panels; deep-link; Ctrl+Shift+D. |
| 2026-02-18 | Separate dev/prod cache      | cache/ sacred; cache/dev/ for dev; dev-reset. |
| 2026-02-18 | Dev mock data                | committees.json roster/bill_numbers; bills action_history/vote_events/slips. |
| 2026-02-18 | Member card no-email UX      | Hint under Email button; Call/Email same height; primary vs secondary. |
| 2026-02-18 | Favicon                      | /favicon.ico truck SVG. |
| 2026-02-18 | Member cards badges under name | Grid: photo col1, name+badges col2; mobile contents. |
| 2026-02-18 | Call drawer email/name width | 75% right-aligned. |
| 2026-02-18 | Call drawer gray             | #ebebf0 for them/capture bubbles. |
| 2026-02-18 | Email drawer click-to-copy To | Copy member email; green Copied feedback. |
| 2026-02-18 | Advocacy drawer width        | Call 420px; email 50vw. |
| 2026-02-18 | Gmail-style email drawer     | htmx.process() after fetch; compose UI, I sent, copy row, banners. |
| 2026-02-17 | Startup summary + URLs        | Steps 10–12 (graph, ML, influence); Services block APP_BASE_URL. |
| 2026-02-17 | Advocacy overhaul            | Hero, auto-submit ZIP, Call/Email buttons, phone panel, streamlined chips, HTMX drawer, wrap-up, error states. |
| 2026-02-17 | Member photos                | photo_url; scrape-members, refresh-photos. |
| 2026-02-17 | Responsive + sidebar         | Breakpoints; results sidebar <details>; panels full width mobile. |
| 2026-02-14 | CI ML extras                 | pip install -e ".[dev,ml]" in CI. |
| 2026-02-14 | Lint (ML)                    | features.py ft_tfidf_id_to_idx; member_value.py F841. |
| 2026-02-14 | Member Value Model           | Ridge LOO-CV, value residual, recruitment rankings; Step 6; /intelligence/recruitment. |
| 2026-02-14 | Full-text leakage fix        | FULLTEXT_DROP_COLUMNS; advance model no fulltext. |
| 2026-02-14 | Bill-to-law reference        | ilga_rules.json bill_to_law_process; intelligence_bill.html. |
| 2026-02-14 | Bill page last action        | From action_history; fallback latest; full actions table. |
| 2026-02-14 | Pipeline stage rollback      | HB3356 Rule 19(b); current_stage vs highest_stage. |
| 2026-02-14 | SHAP explanations            | GET /api/bills/{id}/explanation; Why This Score. |
| (various)  | GraphQL search              | search.py; SearchResultType, search() resolver. |

---

## Done (this session)

| Topic                          | Summary |
|--------------------------------|--------|
| Hardball roadmap (Phases 1–4) (2026-02-27) | **Phase 1:** Timeline page shows key session deadlines from session_schedule; session pill shows optional next deadline (get_next_deadline). docs/canonical README §7 updated. **Phase 2:** reference/session_schedule.json Deadline events have optional `id`; session_schedule.get_deadlines_for_campaigns(), get_milestone_by_id(); Campaign.session_milestone_id; Alembic 20260227100000; admin campaign form "End at session milestone" dropdown; create/update set end_at from milestone when selected; banner and priority callout show "Contact before [milestone] ([date])". **Phase 3:** app-overview "Production deployment" subsection (checklist, env vars, feature flags); TODOS Production pre-launch note. **Phase 4:** Backlog (Hardball-aligned): Coalition/supporters, Opposition awareness, Power Map scoped in TODOS. |
| Venv + make install (2026-02-25) | **Cause:** .venv was recreated with system Python 3.9; pip 21.2 doesn't support editable install from pyproject.toml-only (PEP 660). Project requires Python ≥3.10. **Fix:** Recreated .venv with `/opt/homebrew/bin/python3.12`, ran pip install -e ".[dev]". **Docs:** README manual install now shows `python3.12 -m venv .venv` so future venv creation uses 3.10+. Pre-commit hook was from other repo; re-run `pre-commit install` from this project's venv to fix. |
| Bugfix + cleanup (2026-02-25)  | **content.py:** Constituent brief bullet parser used corrupted U+FFFD; fixed to detect bullets via • (U+2022) and regex `^[-•]\s*` only; comment and lstrip updated. Ruff format applied. **Docs/canonical:** no-hallucination rule and canonical-content-sources skill said FAQ_ADVOCATES; corrected to FAQ_LAW + FAQ_ADVOCACY to match content.py. **app-overview:** User ZIP bullet updated for single-ZIP (no sidebar editable ZIP or Use location). |
| Dead code purge: single-ZIP UI (2026-02-24) | **Removed:** (1) JS: showGoalZipEdit, hideGoalZipEdit, setGoalZipStatus; click handlers for .outreach-goal__zip-locate and .outreach-goal__zip-display; blur/keydown handlers for .outreach-goal__zip-edit (sidebar no longer has zip cell or "Use location"). commitGoalZip retained for future use. (2) CSS: advocacy-form.css entire .hero-zip-* block (~95 lines); advocacy-cards.css .outreach-goal__zip-cell through .outreach-goal__zip-status (~90 lines). Hero has only #zip_code; "Your goal" has no editable ZIP row. TODOS.md: Single ZIP entry and Advocacy hero ZIP entry updated. make test 339 pass. |
| Code cleanup + PR-ready (2026-02-23) | **Dead code removed:** (1) Unused CSS `.role-ally` and `.role-super-ally` in advocacy-cards.css (Potential Ally removed earlier; 3 cards only). **Fixes:** (2) schema.py Query.bills() and members/all_vote_events used `_parse_bill_date`, `_safe_parse_date`, and `_resolve_chamber` without definition; added `from .date_parse import parse_bill_date as _parse_bill_date, safe_parse_date as _safe_parse_date` and local `_resolve_chamber()` so schema module is self-consistent when used (e.g. tests, schema export). make test 339 pass; lint clean. **PR:** Branch ready for update; run update-pr-from-commits skill if desired. |
| Legacy/outdated code removal (2026-02-23) | **Removed:** (1) `build_email_body_full()` in advocacy_helpers.py — never called; (2) `.drawer-outcome-step` CSS block in advocacy-drawer.css (~65 lines) — no template or JS uses it; (3) main.py backward-compat aliases `_parse_bill_date`/`_safe_parse_date` and date_parse import; tests now import `parse_bill_date`/`safe_parse_date` from `ilga_graph.date_parse`. Tests 339 pass; lint clean. |
| Pre-push code cleanup (2026-02-23) | **Tests:** 339 pass. **Lint:** Ruff check + format clean. **Fixes:** (1) Template `_results_partial.html`: heat pill used `outreach_heat.get(member.id, 0)`; keys are strings from advocacy context. Switched to `member_id_str` so heat count matches when `member.id` is int/str. (2) pytest: filtered aiosqlite DeprecationWarning ("There is no current event loop") in sync TestClient + async DB tests via `pyproject.toml` filterwarnings. **Review:** Advocacy ID handling verified (senator_id/rep_id and user_called/emailed sets are str-normalized); no dead code or logic bugs found. |
| Code status (feature/legislator-brief) (2026-02-23) | **Lint:** E501 in content.py fixed (shortened comment; BRIEF_DOCUMENTS entries split across lines). **Template:** legislator_brief.html — four sidebar headings had wrong closing tag (h4 open, h3 close); corrected to closing </h4>. **CSS:** base.css — merged duplicate `.legislator-brief .bill-actions-table-wrap` rules into one block. make lint and make test pass. |
| Pre-merge refactor (feature/home-page) | Lint: fixed E501 in advocacy.py (shortened comment; extracted user_zip for long condition). Format: make lint-fix on outreach.py. Home typography: all homepage copy sans-serif except mission statement (body.page-home + .mission-statement keeps serif). Tests 339 pass; make lint clean. |
| Pre-merge prod smoke test      | make test (303 pass), make lint clean. Prod-mode serve: /health ready, GET /→302 /advocacy, GET /advocacy/ and /intelligence/ 200, POST /advocacy/search (zip_code=60601) 200, GraphQL with API key 200 and without 401, /privacy, /terms, /sitemap.xml, /robots.txt, /report-bug, /explore 200. Fixed: with ILGA_API_KEY set, GET to unknown path with Accept: text/html now bypasses API key so catch-all returns custom 404 (Kei fact) instead of 401 (middleware.py browser_get). |
| Code cleanup (main.py)         | Removed dead `_load_stale_cache_fallback` (lifespan uses `etl.load_stale_cache_fallback`). Removed unused `get_bill_status_urls` re-export and wrong comment (scripts don't import from main). Dropped unused imports: `ILGAScraper`, `load_bill_cache`. Query.bills() now uses `safe_parse_date` directly; kept `_parse_bill_date`/`_safe_parse_date` aliases for tests. |
| Branch reconciliation (prod-improvements) | API key: added /privacy and /terms to exempt set so legal pages are public when ILGA_API_KEY is set. Sitemap: added /privacy and /terms to _SITEMAP_PATHS. status-report.md: routes list now includes feedback + legal; notes custom error pages, catch-all 404, CSP/HSTS, static cache; Legal and SEO paths documented as API-key exempt. CTA kept as "Start Outreaching" per product choice. |
| Advocacy/router cleanup        | `member_lookup.is_constituent_for_zip_member(state, zip_code, member)`; `advocacy_helpers.party_abbr_for_member(member)`; shared `_hero_context()` in advocacy router. Replaced 4× is_constituent blocks, 3× party_abbr blocks, 2× hero dicts; explore uses party_abbr_for_member. |
| Pre-push cleanup               | Removed leftover hero-image debug instrumentation (fetch to localhost:7246/ingest) from index.html; tests 303 pass, lint clean. |
| Full-text feature caps         | FULLTEXT_MAX_FEATURES 400, FULLTEXT_MAX_TOKENS 2000; env overrides. |
| ML Step 3 tuning                | n_iter 20, trimmed grid, verbose=2, ILGA_ML_SKIP_TUNE=1. |
| Smart tiered index scanning     | <24h skip, <7d tail-only, >7d full; scrape_metadata timestamps. |
| Pipeline resilience             | Preserve vote_events/witness_slips on re-scrape; unified scrape.py; Makefile 8 targets. |
| Vote PDF tally mismatch        | Middle-initial E/P/N/A disambiguation; SMART_A lookahead. |
| Vote/slip scraper robustness   | Data-verified progress, dual-track, heuristic skip, batch saves, merge at startup. |
| Lint hardening                  | E501/W293; pre-commit Ruff + make test. |
| Incremental scrape-votes        | Progress file, resumable, parallel, ETA. |
| Discovery-based bill index     | _discover_doc_types(); all doc types; chamber J for EO/JSR/AM. |
| Separation of concerns         | Bill vote_events/witness_slips; single-page index; unified committees.json; GraphQL IDs. |

---

## Previously done

| Topic                           | Summary |
|---------------------------------|--------|
| Bill index scraping             | Clear progress logs; checkpoint after every page. |
| Chamber-specific associated     | associated_senator / associated_representatives in GraphQL + exporter. |
| Full bill index + scrape-200    | Pagination; make scrape-200, make scrape-full. |
| Scrape → dev/prod pipeline      | make scrape (prod), make scrape-dev; ILGA_LOAD_ONLY=1. |
| Evidence-based script hints     | _build_script_hint_*; "How we pick targets"; Moneyball definition. |
| Scorecard / Moneyball clarity   | metrics_definitions.py; empirical first; metricsGlossary. |
| Advocacy full-data clarity      | Banner actual member_count/zip_count; make dev-full. |

---

## Next (when you're ready)

| Area        | Item                    | Notes |
|-------------|-------------------------|-------|
| Power Map   | Weighted edges           | Shared bill count for pruning/edge thickness. |
| Power Map   | House committee mapping  | _CATEGORY_COMMITTEES (currently Senate only). |
| Power Map   | Free-text topic search   | e.g. "kei truck" → bills → committees → highlight members. |
| Power Map   | Mobile detail panel      | Bottom sheet on small screens. |
| Power Map   | Path visualization       | Dotted paths from user legislators to chairs when topic+ZIP set. |
| Power Map   | Time-based animation     | Co-sponsorship network over session. |
| Pipeline    | Run full pipeline        | make scrape (daily ~2 min or FULL=1); verify GraphQL + advocacy. |
| Prod        | Shift to prod            | ILGA_PROFILE=prod, CORS, optional API_KEY. |

---

## Production (checklist for deployment)

| # | Check |
|---|--------|
| 1 | Pre-populate `cache/` (full or incremental scrape) so startup is load-only. |
| 2 | Set `ILGA_PROFILE=prod` (turns off dev + seed). |
| 3 | Set `ILGA_CORS_ORIGINS` to your front-end origin(s). |
| 4 | Set `ILGA_API_KEY` to protect GraphQL (prod warns if empty). |
| 5 | Use `GET /health` for readiness (`ready` when members loaded). |

**Pre-launch:** Confirm `reference/session_schedule.json` matches the session you are running for. Create at least one campaign with a **session milestone** (End at session milestone in admin) and verify banner/priority callout show "Contact before [milestone]". See [Production deployment](docs/features/app-overview.md#production-deployment) for env vars and feature flags.

---

## Pre-merge / prod readiness (advocacy branch)

Before merging advocacy changes to `main` so prod is set:

| # | Check | Status |
|---|--------|--------|
| 1 | **Migration** — `alembic/versions/20260302110000_user_call_pref_string_length.py` widens `users.call_pref` to String(16) for values `call_only`, `elevator`. On deploy, `init_db()` runs Alembic on startup so migration applies automatically after restart. | Migration in repo; no deploy script change needed. |
| 2 | **No new env vars** — Call pref is cookie + DB only; no new `ILGA_*` required for advocacy. | — |
| 3 | **Tests** — `tests/test_advocacy_goal_steps.py` covers `_visible_steps` for no/yes/call_only/elevator and email-only step filtering. CI runs pytest on push/PR. | In place. |
| 4 | **Docs** — app-overview Advocacy row and user-guide/advocacy-test-mode.md describe flow; env vars and Production checklist unchanged. | app-overview updated below. |
| 5 | **Smoke** — After merge, smoke-test `/advocacy`: ZIP → intro card (decision tree) → set pref → carousel (goal bar, truck, member cards, drawer). | Manual pre/post deploy. |

**Follow-up (non-blocking):** TODOS Refactor row "Goals + member cards, contact prefs" — Updates page CTA: align district_steps with email availability if we add effective-email to that context.

---

## ML Pipeline (`feature/ml-pipeline` branch)

**New `src/ilga_graph/ml/` package** — automated "Legislative Intelligence Engine" (v2). One command: `make ml-run`.

### v2 improvements (what changed)

| Problem in v1 | Fix in v2 |
|---------------|-----------|
| Test set 3 pos / 2900 neg | Evaluate on mature bills (120+ days); 412 advanced / 1650 stuck. |
| Single algorithm           | Compare 4 algorithms, 5-fold stratified CV; best auto-selected. |
| No hyperparameter tuning   | RandomizedSearchCV 40 iter; GBT n_estimators=300, max_depth=9, lr=0.05. |
| No calibration             | CalibratedClassifierCV isotonic. |
| 49% DBSCAN outliers        | Agglomerative clustering; 100% classified, k=10. |
| Anomaly = big = suspicious | Coordination features (name dup, unanimity, top org share); explains WHY. |
| No quality report          | model_quality.json with trust, comparison, strengths/issues. |

### Current results (v2)

| Metric | Value |
|--------|--------|
| Bill prediction CV ROC-AUC | 0.984 ± 0.005 |
| Bill prediction Test ROC-AUC | 0.910 |
| Test precision (advanced) | 96.3% |
| Accuracy on mature bills | 94.9% |
| Entity resolution | 100% |
| Coalition clustering | 10 blocs, 100% classified |
| Anomaly detection | 102 bills (8%) with reasons |
| Pipeline runtime | ~4 min |

### What it produces (in `processed/`)

| Output | Description |
|--------|--------------|
| bill_scores.parquet | Every bill scored; mature = labels, immature = forecasts. |
| model_quality.json | Trust assessment, metrics, top features. |
| coalitions.parquet | Legislators → voting bloc. |
| member_embeddings.parquet | 32-dim vectors. |
| slip_anomalies.parquet | Coordination signals + reasons. |
| fact_vote_casts.parquet | Vote casts with member_id. |
| dim_*.parquet, fact_*.parquet | Star schema. |

### How to run

```bash
make ml-setup    # One time
make ml-run      # Full pipeline ~4 min
```

### Pipeline steps (all automated)

1. **Data Pipeline** — Flatten cache to Parquet.
2. **Entity Resolution** — 385 names → member IDs; 100% resolved.
3. **Bill Outcome Prediction** — 4 algorithms, tune, calibrate; mature-bill eval.
4. **Coalition Discovery** — Agreement graph + Agglomerative; 10 blocs.
5. **Anomaly Detection** — Coordination features, Isolation Forest.

### Individual steps (optional)

```bash
make ml-pipeline
make ml-resolve    # AUTO=1 for non-interactive
make ml-predict
```

### v3: Intelligence Dashboard & Self-Correcting Feedback Loop

Backtester, chained scrape→ml-run, ml_loader.py, GraphQL 7 resolvers, /intelligence tabbed UI (Predictions, Coalitions, Anomalies, Model Quality & Accuracy).

### v4: Coalition Naming, Bill Pipeline Stage, Stuck-Bill Analysis

characterize_coalitions(), compute_bill_stage(), classify_stuck_status(); BillScore stage/stuck fields; GraphQL filters; predictions/coalitions dashboard updates.

### Next (ML backlog)

- Gold labels (bill_labels_gold.json): eval and/or override training.
- Low-confidence UX: uncertain rows, calibration curve.
- Individual vote prediction, poison-pill detector, committee prediction, accuracy trend, per-bill history, influence trend.

---

## Backlog / Future

| Item | Status |
|------|--------|
| Deploy from `production` branch (not main) | Open; plan in .cursor/plans. |
| Unified GraphQL `search` query | Done. |
| Search: fuzzy, index, vote/slip entities, autocomplete | Open. |
| Full bill index make scrape-full | Done. |
| make scrape-votes from cache | Done. |
| Advocacy: member photos, richer script, email links | Open. |
| Advocacy: CSS to static when it grows | Open. |
| Advocacy: HTMX drill-down on card | Open. |
| Advocacy: interactive map (ZIP/district) | Open. |
| Full Census crosswalk for prod | Open. |

**Backlog (Hardball-aligned):**

- **Coalition / supporters** — When we have names: add canonical list (content.py or JSON), render "Led by [X]" / "Supported by [Y]" on coalition page and optionally hero/footer. Unhide coalition from nav when content exists. No invented content.
- **Opposition awareness** — Optional canonical "opposition" or "likely opponents" note (content.py or admin) for staff-facing context (e.g. legislator brief or admin). Hardball Ch 7: "Identifying the opposition… is tactically essential." Single block or short bullets; canonical source only.
- **Power Map** — Next table items (weighted edges, House committee mapping, topic search, mobile detail panel, path visualization, time-based animation). When implementing path visualization, tie to session_schedule/committee deadlines for "contact before committee deadline" where relevant.

---

## Done (summary)

| Theme | What was done |
|-------|----------------|
| Modularity 1–2 | config.py ILGA_PROFILE, .env; try/except ETL; checkpoint every 50 bills. |
| Cache & seed | committees.json; mocks/dev/ SEED_MODE; normalized members/bills; vote/slip cache. |
| ETL | load_or_scrape_data, compute_analytics, export_vault; ILGA_LOAD_ONLY. |
| Resilience | HTTPAdapter retries, startup timings CSV, ETL-phase summary table. |
| Witness slips & GraphQL | billSlipAnalytics, memberSlipAlignment, advancement; graphql/README. |
| Bills-first & incremental | Legislation pages; incremental_bill_scrape; sponsor_ids. |
| Server & CI | main.py single app; ruff; tests. |
| Seating chart | seat_block_id, seat_ring, seatmate_names, seatmate_affinity; seating.py; Senate-only. |
| SSR Advocacy | zip_crosswalk, GET /advocacy, POST /advocacy/search, 3 cards, policy filter. |
| Steps 3–5 | etl.py, analytics_cache, loaders; batch_load in resolvers. |
| Moneyball v2 | Shell bill filter, institutional weight, chair-first Power Broker. |
